# Event icon provenance

Copied byte-for-byte from the user's original game-dev project:
`E:/无尽轮回/长期备份/2026-7-13-1/game-dev/assets/ui/event-icons/`.

The existing files are reused, not newly generated or redrawn. Source mapping:
`src/world/world-weather-system.js` and `src/world/weather-event-icons.js`.
Light rain uses rain-light.png, medium rain uses rain-moderate.png, and the
current thunderstorm profile uses rain-storm.png. Other original icons remain
available to registered real event providers. This note does not assign a new
license to the user's original assets.
