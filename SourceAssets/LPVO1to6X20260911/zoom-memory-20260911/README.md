# ADS 滚轮与倍率记忆验证

2026-09-11：现有运行实现已满足本次输入需求，保留实现并补充真实游戏回归，未新增存档字段。

- 仅可变倍率 LPVO 在 ADS 中处理滚轮；前滚 +0.5×，后滚 -0.5×，范围 1–6×，消费该次输入以避免切枪。
- 初始 1×。取消 ADS 不重置倍率，下次 ADS 恢复最后调整值。换瞄具或重建武器仍回到最小倍率；本次未扩展为跨进程倍率存档。
- 移除审计中人为设置 1× 的步骤，直接验证默认值。实际滚轮调到 4×，退出 ADS 6 秒后重新开镜，再后滚到 2× 并重复开镜，测量相机 FOV、调节环和分划中心。
- ADS 中固定二倍镜、全景红点、全息和机械瞄具均拒绝双向倍率调整。其他状态原有滚轮切枪行为保留。

build_zoom_memory.log：原生编译成功，模块后缀 9111547。
write.log：102 项、0 失败；reload.log：98 项、0 失败。使用独立测试存档，新进程实际输入回归共 200 项通过。
write/reload-lpvo-reopen-4x.png 与 write/reload-lpvo-reopen-2x.png 为再次 ADS 的实际游戏截图。新进程重载检查的是安装状态以及本进程的 ADS 记忆，并非跨进程保存倍率。
