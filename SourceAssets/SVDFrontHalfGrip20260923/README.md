# SVD 弹匣前半段抓握

## 2026-09-23 整理后的当前入口

旧换弹导出已由 `SVDThumbUp20260923` 替代；当前模型与材质入口为 `SVDRefinedFinish20260923`。本目录的旧制作记录保留其历史日期与验收边界。已经移走的旧导出、自动备份及恢复快照位于 `trash/svd-superseded-20260923/SourceAssets/` 下的同名目录，原路径、散列及保留替代物见 [归档清单](../../Docs/AssetArchives/svd-superseded-20260923.json)。当前所需 Blend、脚本、参数与原素材仍保留；不要直接重跑旧导入覆盖用户已认可的抓握。新 checkout 的恢复方式见 [SVD 发布说明](../../Docs/Weapons/svd-publication-20260923.md)。

按用户 2026-09-23 的明确要求，取消跨越弹匣长度的后缘拇指对握。左手改为包住弹匣前半段，拇指在前缘旁的近侧面自然收拢，不为碰到后缘而撑开虎口。

制作依据：`skills/ue5-fps-arms-animation/references/pose-contact.md` 的掌面优先、自然分组屈曲、骨长与局部位移不变、持匣关系固定、先开指再退手原则。使用 Manny 已有抓握供体的四指屈曲平面，拇指恢复实际 rest 局部的关节弯曲；不再使用上一轮指向弹匣后缘的世界坐标拇指框架。

- 输入：`../SVDReloadHandRepair20260923` 最近落盘的五份源场景及十条换弹动作。
- 制作：`fit_hand.py` 定义前半段接触区域；`author_hand.py` 烘焙原厂、垂直、斜握、棱形与倾角握把的普通/空仓换弹。
- 取匣、持匣、插匣及压实共享同一弹匣局部抓握关系；松手后向前外侧退开，再返回各自护木/握把。
- 保持手指骨长、局部位移、缩放、掌骨、蒙皮及弹匣尺寸；保留右手、枪械机械轨道及换弹时钟。
- `grasp_fit.json` 是正式制作手型；局部图为此前获准生成的制作对照，不是游戏画面。
- `import_animations.py` 替换当前运行路径的十个动画资产；`import_receipt.json` 逐项记录保存结果。旧资产备份在 `Before/`。

默认后台制作和导入，不启动编辑器界面或游戏；未进行实机测试，交由用户试玩判断。

## 实际落盘

2026-09-23 22:53（本地时间），原厂与四种握把的普通/空仓换弹共 10 个 UE 动画资产全部执行导入并保存。`import_receipt.json` 的十项 `saved` 均为 true，日志记录 `SVD_HAND_ANIMATION_IMPORT_COMPLETE 10`。源场景、FBX 与导入的抓握版本均来自本目录。

后台命令行进程退出码为 1：唯一汇总错误是 MCP HTTP 监听器无法绑定已占用的 `127.0.0.1:8000`，不是动画导入/保存失败。未为消除该端口错误重复覆盖资产，也未关闭占用端口的现有程序。启动期间取消的仅是本轮派生、正在等待共享构建锁的自动 SDK 探测子进程；未干预其他构建。

`ContactViews/after_*.jpg` 为已烘焙源动画第 220 帧的局部制作视图（包含前臂以避免把裁剪边缘误认成虎口破损），不代表实机测试或用户最终认可。
