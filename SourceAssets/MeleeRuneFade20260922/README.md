# 符文淡入淡出与金色强化限定

2026-09-22。延续已认可的蛮荒符文 V2 的柔和明灭、内外光晕与边缘渐隐。

- 金色符文强化仅向 `ue_rune_sword` 开放；旧存档其他武器的该改造在读档时通过现有 A/B 保存事务移除。预览、持握和掉落视觉以及属性计算共用限定规则。寒晶剑清除误装强化后恢复原有自带侵蚀效果。
- 共鸣：青绿，缓慢呼吸；侵蚀：紫色，裂隙错相渐隐；导魔：蓝色，沿剑身缓慢流动。原图案和定位保留，亮度与覆盖透明度一起变化，两层柔光减弱线条硬边。
- 精神迸发：延续冰蓝晶体与扩散回声，放慢迸发节奏、延长淡入淡出并柔化细线。
- 金色：沿原生剑身和护手 UV 遮罩柔和明灭，保留金属刻纹底色。
- 已认可的蛮荒符文 V2、符文图标以及其他符文数值不变。

源材质为 `material_sources.json` 中三项；`Before` 保留修改前材质包与实际图源记录。`install_materials.py` 经现有 UE 桥编译并保存原路径，运行引用无需改动。

只进行制作、保存与必要构建；未运行游戏测试或验收渲染，由用户测试。

## 本次接入状态

- 三项材质已编译并保存，记录见 `install_receipt.json`。
- 限定目录配置与运行时/读档代码已完成。已有编辑器的目录缓存需要重新进入游戏后读取。
- 常规构建日志：`D:/FPS3D/FPSGAME/Saved/BuildEditor/build-20260922-182842.log`。本次目标链接被不相关的 `AWitchRebuiltMonster::ConfigureDistanceLODs(USkeletalMesh*)` 未解析外部符号（LNK2019/LNK1120）阻断；没有修改该巫师模块，不能据此宣称新版运行时代码已加载。
- 曾尝试正常退出编辑器，被未保存的地牢资产阻止，已保留现场。随后同步 Live Coding 请求未找到节点，未执行；编辑器关闭后才进入上述常规构建。
- 待项目链接问题解决后继续常规 Editor 目标构建并加载新二进制；当前不启动游戏测试。
