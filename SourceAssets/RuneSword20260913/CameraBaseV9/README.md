# CameraBaseV9 — 符文剑镜头基础幅度增强

2026-09-14，按用户反馈直接提高镜头基础参数。保留 V8 的抬剑、0.2 s 定剑、快斩与收势节奏，本次只修改 `URuneSwordComponent::GetCameraMotion`。

所有位移为厘米，旋转为度；表中是基础参数，输出继续经过 V8 的 1.5 倍系数以及角色现有 `CameraMotionScale`（源码默认 0.45）。位移还经过角色已有的相机位置平滑；下表不是实机测量值。

| 参数 | V8 基础值 | V9 基础值 |
| --- | --- | --- |
| 蓄势前后、横向、竖向位移 | -1.8 / 1.2 / 0.4 | -5 / 3 / 1.2 |
| 蓄势俯仰、偏航、侧倾 | 1.15 / 1.2 / 0.8 | 3 / 3.5 / 2.5 |
| 斩后前移、横移、下沉 | 2.2 / 1.6 / -1.3 | 8 / 5.5 / -4.5 |
| 斩后俯仰、偏航、侧倾 | -1.4 / 1.75 / 1.55 | -4.5 / 6 / 5 |
| 释放时额外前冲脉冲 | 1.6 | 5 |
| 释放时俯仰、侧倾震动 | 0.22 / 0.28 | 0.85 / 1.1 |
| 命中后坐、横移、竖向冲击 | -1.1 / 0.45 / 0.25 | -4 / 1.5 / 1 |
| 命中俯仰、偏航、侧倾冲击 | 0.8 / 0.35 / 0.55 | 3 / 1.4 / 2.2 |

横向和旋转继续按左右斩方向取符号。快斩阶段的基础位移与转动约为 V8 的 3–4 倍，短促震动沿原有包络衰减，收势沿原方向回正。动画资产、攻击时间窗、伤害与通用镜头缩放配置沿用当前版本。

修改前组件保留在 `Before/Source/`。`build_install.ps1` 完成必要 Editor 编译，将同组模块及清单保存到 `NativeBuildSnapshot/`。无需重新导入动画。

按用户规则，未运行游戏、测试、截图或验收渲染，由用户重新打开工程后自行体验幅度。

必要 Editor 构建已完成，最终脚本退出码 0，日志为 `build.log`，三个模块统一使用后缀 `49149`。首次构建被快捷栏正在开发的代码阻塞：`Slot` 名称隐藏 UWidget 成员，以及 `auto*` 无法遍历 `TObjectPtr` 元素；仅在 `ColdSteelFireballHUD.cpp`、`ColdSteelQuickSlot.cpp` 调整局部变量名和循环声明，保留业务逻辑。对应修改前文件保存在 `Before/Source/`，首次失败记录为 `build_first_attempt.log`。
