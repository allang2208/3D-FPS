# LeftPrimaryV14

2026-09-14：蓄力输入从右键迁到左键，右键暂不绑定符文剑动作。

- 左键空闲按下开始蓄力计时；0.20 秒内松开走完整普通攻击，保留原普通攻击的左右交替规则。
- 超过点按时间、未满 2 秒松开，沿用 V13 的普通攻击衔接；满 2 秒松开，沿用 V12 的双倍伤害重击。2 秒均从原始按下时刻计算。
- 收势/装备期间的左键按下沿用已有普通攻击缓冲；同次松开不再提交第二次攻击。
- 物理松键使用 `FireInputReleased`，菜单/切装备/翻越内部调用 `FireReleased` 只中断动作，避免内部清理输入意外释放攻击。
- 普通与重击动画、伤害倍率、攻速、镜头、特效以及体力规则保留；本次无需重新导入资产。

实现：`FPSGAMECharacter.h/.cpp`、`RuneSwordComponent.h/.cpp`；操作提示：`ColdSteelItemTooltipData.cpp`。

必要 Editor 构建成功，退出码 0，耗时 121.99 秒。使用同批次 `49154` 游戏/插件模块并保存于 `NativeBuildSnapshot/`。用户编辑器保持打开，需要用户自行重启加载。按用户规则未运行游戏测试、预览或渲染。
