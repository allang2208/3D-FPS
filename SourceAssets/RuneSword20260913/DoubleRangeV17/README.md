# 符文剑范围翻倍 · DoubleRangeV17

当前统一参数：`Source/FPSGAME/Weapons/RuneSwordCombatTuning.h` 的 `RangeMultiplier=2.f`。普通挥砍、突刺和蓄力重击共用，物品说明使用同一计算函数。详细记录见 `Docs/Weapons/RuneSwordDoubleRange20260914.md`。

本轮为玩法判定与提示文字调整，未修改或重新导入动画。`Before` 保存涉及的已有源码；`build_install.ps1` 执行必要 Editor 原生构建，成功后保存后缀 `49157` 的三个模块和两份加载清单到 `NativeBuildSnapshot`。

本轮未执行游戏测试、审计、渲染或验收，由用户自行试用。
