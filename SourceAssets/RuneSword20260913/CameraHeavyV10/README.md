# CameraHeavyV10 — 镜头幅度继续增强

2026-09-14，按用户“镜头效果还需要增强，继续增强”的要求，将 V9 符文剑镜头全部基础位移、旋转和冲击参数再提高到 **2 倍**，保留既有 1.5 倍输出系数及角色镜头缩放。

- 蓄势位移基础值改为前后 -10 cm、横向 6 cm、竖向 2.4 cm；俯仰 6°、偏航 7°、侧倾 5°。
- 斩后位移基础值改为前移 16 cm、横移 11 cm、下沉 9 cm；俯仰 -9°、偏航 12°、侧倾 10°。
- 释放前冲脉冲改为 10 cm；俯仰与侧倾短促震动改为 1.7° / 2.2°。
- 命中冲击基础位移改为 -8 / 3 / 2 cm，基础旋转改为 6 / 2.8 / 4.4°。

横向及旋转符号继续跟随交替挥砍方向，原有曲线和衰减包络不变。上述是源码基础值，仍经过角色现有缩放及位移平滑，不是实机测量幅度。V8 动画、0.2 s 定剑停顿、收势、命中窗和剑气保持当前版本。

本次修改 `Source/FPSGAME/Weapons/RuneSwordComponent.cpp`，修改前文件保存在 `Before/Source/`。必要构建入口为 `build_install.ps1`，输出及模块清单保存在 `NativeBuildSnapshot/`；无需导入动画。

按用户全局规则，未运行游戏、自动检查、测试、截图或验收渲染，交由用户重新打开工程后自行体验。

必要 Editor 构建已完成，脚本退出码 0，`build.log` 记录成功；三个模块统一使用 `49150` 后缀，并已保存输出快照。未关闭用户进程。
