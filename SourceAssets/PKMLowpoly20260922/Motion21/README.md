# PKM Motion21 — 出链口盖板与提把惯性

日期：2026-09-23。当前修改范围仅为出链口盖板的独立铰链及提把摆动。保持弹量、射击/换弹时钟、手臂动画、几何外形和 Finish20 材质。

## 出链口盖板

原件 `PKM_Part_058`（盖板）和 `PKM_Part_101`（中央铰链套）原先整件跟随 `PKM_Cover`，没有独立开度。二者刚性绑定到新增叶骨 `PKM_ExitCover`，父骨仍为 `PKM_Cover`；外侧固定铰链件 102 保持原绑定，因而打开上盖时整组仍随上盖走。

铰心取中央铰链套的实际几何中心。当前板长约 2.54 cm，横向半宽约 3.29 cm；具体数据记录在 `authoring.json`，并由作者脚本生成 `Source/FPSGAME/Weapons/PKMOutletCoverGeometry.h`。

运行层 `FPKMExitCoverDynamics` 在已有出链姿态之后执行：采样当前引导片、空链节的几何包络，计算盖板不得侵入的最低开度，另叠加重力、移动惯性和带阻尼回弹。接触开度即时约束，不让弹簧延迟把板片带入弹链。弹链移出接触区域后回落，打开上盖时使用上盖的当前坐标系继续计算。

弹链是否参与接触直接读取实际 `__OldBelt` / `__NewBelt` 显示状态，包含末发进给延迟、普通换弹、空仓换弹和新弹链出现阶段。隐藏骨的零缩放同时排除。重复求值不重复积累时间。

这是一套游戏表现用的受限铰链与弹链包络约束，不是整枪 Chaos 刚体仿真，也不宣称完成所有姿态的精确三角面碰撞验收。

## 木制提把

沿用 CarryHandle18 的真实挂点、木柄和金属支架权重。回弹频率从 3.8 Hz 降至 2.15 Hz，阻尼比从 .62 降至 .30；摆幅由 -9°/+7.5° 改为 -15°/+18°。增加靠近限位的渐进缓冲和轻微回弹，保留线性/角加速度驱动，允许奔跑、换弹与急停后产生余振。没有添加持续循环的随机抖动。

## 提把灵敏度回调（2026-09-23，Motion21 后续）

用户实机反馈「灵敏度太高，稍微移动就来回晃动」：小幅瞄准或移动就换来一整段往复摆动，余振也停不下来。只改 `Source/FPSGAME/Weapons/PKMCarryHandleDynamics.cpp` 的驱动与阻尼，不动挂点、骨骼、木柄几何、动画和材质。

| 参数 | 原值 | 现值 | 作用 |
| --- | --- | --- | --- |
| 线性加速度耦合 | .95 | .38 | 枪身／镜头加速度的驱动量，灵敏度主开关 |
| 角加速度耦合 | .85 | .32 | 转身引起的驱动量 |
| 阻尼比 | .30 | .45 | 压掉来回余振 |
| 限位恢复系数 | .18 | .08 | 撞限位后不再被弹回 |
| 输入滤波系数 | 28 /s | 16 /s | 小幅抖动更不容易进入弹簧 |

弹簧频率 2.15 Hz、摆幅 -15°/+18°、软区 3°、重力项 .24 保持不变，仍按机械行程与静垂口径处理。降频率会让同一次枪身运动摆得更远，不作为「降灵敏度」的手段。

常规 Editor 目标构建成功（`build_handletune.log`，`Saved/BuildEditor/build-20260923-212543.log`，退出码 0），本轮 TU 为 `[1/11] Compile PKMCarryHandleDynamics.cpp`，`UnrealEditor-FPSGAME.dll` 时间戳 21:26:03 晚于源码 21:25:40。未运行游戏，灵敏度与余振的实际观感由用户测试。

### 第二轮回调（2026-09-23，用户复测后）

复测仍觉敏感、也仍有晃动，按第一轮同一口径继续压驱动与阻尼，并收窄摆幅上限：

| 参数 | 第一轮 | 第二轮 | 作用 |
| --- | --- | --- | --- |
| 线性加速度耦合 | .38 | .25 | 灵敏度主开关，继续降 |
| 输入滤波系数 | 16 /s | 12 /s | 小幅抖动更难进入弹簧 |
| 阻尼比 | .45 | .55 | 压掉来回余振 |
| 摆幅上限（上／下） | +18°／-15° | +12°／-10° | 收窄极端摆幅 |

角加速度耦合 .32、限位恢复 .08、弹簧频率 2.15 Hz、软区 3°、重力项 .24 不变。摆幅上限只在大力冲击时起作用：线性弹簧和软限位本来就把日常摆动压在几度以内，这一项主要限制急停、换弹撞击的极端行程。

构建过程与第一轮不同，留档：首次完整构建在 `[1]` 步被并行会话的未跟踪新文件 `Source/FPSGAME/UI/ColdSteelSmeltingWidget.cpp` 阻断（`error C2065: "FuelSlot": 未声明的标识符`，`build-20260923-214006.log`，退出码 6），没有链接。按并行工作规则没有改对方的文件；改用 UBT 记录的编译命令独立编译本次 TU：`cl.exe "@…/PKMCarryHandleDynamics.cpp.obj.rsp"`（工作目录 `Engine/Source`），cl 退出码 0，obj 21:41:23 晚于源码 21:40:04。对方 21:41:02 补齐声明后重跑完整构建：`build_handletune2.log`、`Saved/BuildEditor/build-20260923-214127.log`，`Result: Succeeded`，14 个动作后 `[13/14] Link UnrealEditor-FPSGAME.dll`，DLL 时间戳 21:41:43 晚于本次 obj。未运行游戏，实际手感仍由用户测试。

### 第三轮回调（2026-09-23，用户仍觉敏感）

前两轮只按比例压增益，输入到摆角仍是一条过零的线性通道：走路与奔跑时枪身摆动的加速度可达每秒数千米平方，乘上耦合后依旧把木柄推出几度。本轮改成「死区 + 更低增益」，并把重力耦合降一半：

| 参数 | 第二轮 | 第三轮 | 作用 |
| --- | --- | --- | --- |
| 线性加速度死区 | 无 | 250 cm/s² | 走路、瞄准、手抖下驱动严格为 0 |
| 线性加速度耦合 | .25 | .10 | 死区以上的驱动增益 |
| 角加速度死区 | 无 | 2.5 rad/s² | 缓慢转头不再驱动 |
| 角加速度耦合 | .32 | .15 | 同上 |
| 输入滤波系数 | 12 /s | 8 /s | 截止频率降到约 1.3 Hz，步行摆动再衰减一档 |
| 重力项 | .24 | .12 | 转枪时不再把木柄甩向行程两端 |

死区实现为「只把超出阈值的部分乘增益」（`LinearDrive`／`AngularDrive`），阈值以下驱动严格为 0，阈值以上线性接续、不产生跳变；两个阈值设 0 即退回普通增益。阻尼比 .55、摆幅 -10°／+12°、弹簧频率 2.15 Hz、软区 3°、限位恢复 .08 不变。

量级估计（不是实测）：步行摆幅从约 10° 量级降到 3° 以内，急停、落地、换弹撞击这类瞬时冲击仍能看到几度摆动。

常规 Editor 目标构建成功（`build_handletune3.log`、`Saved/BuildEditor/build-20260923-214829.log`，退出码 0）：`[1/4] Compile PKMCarryHandleDynamics.cpp`，obj 21:48:32 晚于源码 21:48:24，`[3/4] Link UnrealEditor-FPSGAME.dll`，DLL 时间戳 21:48:34。未运行游戏，实际手感由用户测试。

## 文件与接入

- 来源：`../OutgoingBelt19/PKM_OutgoingBelt_Editable.blend`。
- 新可编辑源：`PKM_HingedOutlet_Editable.blend`。
- 导出：`Exports/SK_PKM_Manny_Modular.fbx`。
- 作者入口：`author_hinge.py`，输入几何记录 `source_hinges.json`。
- 导入入口：`import_asset.py`，捕获当前材质绑定后重导入同一活动网格，恢复材质槽；不导入动画或新材质，不更新已有骨骼参考姿态。
- 游戏目标：`/Game/Weapons/PKMLowpoly20260922/Accessories14/SK_PKM_Manny_Modular` 及其 PKM 私有骨架。
- Finish20 的划痕、干湿材质和独立雨滴表保留。

制作、导出与后台导入已完成。首次编辑器桥请求未发现可连接节点，没有执行导入；用户保存关闭编辑器后，Python commandlet 已将活动网格与私有骨架保存。导入记录为 `imported.json`，后台导入日志为 `import_background_01.log`，退出码 0。导入捕获并保留了当前 Finish20 材质绑定。

动态逻辑的常规 Editor 目标构建已成功，退出码 0；记录为 `build_cpp.log`，完整构建日志为 `Saved/BuildEditor/build-20260923-092938.log`。盖板和提把逻辑已进入常规构建产物，没有启动或重启编辑器。

未运行游戏、测试或验收渲染，实际摆动幅度与换弹接触由用户测试。
