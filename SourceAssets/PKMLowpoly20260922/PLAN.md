# PKM 可动模型与换弹候选计划

用户范围：继续模型和动画制作，外观确认后才接游戏。原始 GLB、Refinement01 和其他武器保持不变。输出放在 `Mechanics02/`，后续手臂动作放在独立 `Animation03/`。

1. **内部可见结构**：依据已保存的 D&B 同一实物照片 DSC_0007-11、DSC_0008-08，补供弹区可见承托面、上盖内侧细节和机匣内侧导向结构。模型与照片存在差异；新增结构是适合动画镜头的外观近似，不提供真实机械制造模型，也不宣称完整内部还原。
2. **活动件与骨架**：按已有完整网格身份分配上盖、上盖附件、弹箱、箱盖、拉机柄、扳机与分段弹链。保留原网格和 UV，不按包围盒切皮。转轴和绑定参数可编辑；弹链逐节控制，不通过缩放隐藏。
3. **手臂与换弹**：收到/找到动作参考后，先读取并查看动作，记录帧率、普通/空仓时长、左右手接触、支撑交接和收势。复用现用 Manny 手模；按本枪握点制作，旧 PKM 废案不能作为成功动作母版。
4. **交付**：提供可编辑 Blend、独立模型 FBX/GLB、动作 FBX 和动作时序说明。默认不做验收渲染、游戏测试或 UE 接入，不改目录、存档、代码及其他武器。

## 当前状态

- 已读取并查看开盖实物照片；读取了当前源网格分件数据。
- 内部补模与活动件绑定：已输出 Mechanics02，保留 141 个源网格，新增 18 个可见内部结构网格，25 根机械控制骨。
- 换弹动作参考：用户指定 BV1jHeA6sEmj；已读取并实际查看关键帧，记录在 `References/motion_notes.md`。
- 手臂换弹：Animation03 已制作普通 6.5 秒、空仓 7.5 秒、待机 1 秒三条候选，复用 Manny 手模并进行本枪握点适配。
- 游戏接入、视觉验收：未进行；交由用户先确认候选。
