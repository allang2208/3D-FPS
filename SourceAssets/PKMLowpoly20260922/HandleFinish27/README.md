# PKM 提把螺钉与哑光金属修订

2026-09-23。用户反馈提把出现两块长突起，以及金属过亮、过新。

## 提把

从当前 `Motion21/PKM_HingedOutlet_Editable.blend` 继续制作。木柄 136 和支架 75 已绑定 `PKM_CarryHandle`，两颗贯穿螺钉 80、81 却仍绑定 `WPN_root`；因此木柄摆动时，螺钉不随动，螺杆从木柄露出。

两颗螺钉现以刚性权重 1 绑定原有 `PKM_CarryHandle`。按螺钉轴线与木柄表面的交点重新安置两端，外露高度各 0.45 mm，直径从 10 mm 缩为 8.4 mm，贯穿长度约从 25.5 mm 改为 24.9 mm。保留木柄、支架、原有转轴和提把惯性代码。未添加或更改骨架参考姿态、动画时序、手臂权重。

- 作者入口：`author_handle.py`。
- 可编辑源：`PKM_HandleFinish_Editable.blend`，后续主体建模以此为当前修订。
- 导出：`Exports/SK_PKM_Manny_Modular.fbx`。
- 几何输入和制作记录：`handle_geometry.json`、`authoring.json`。
- 目标：`/Game/Weapons/PKMLowpoly20260922/Accessories14/SK_PKM_Manny_Modular`。

## 金属表面

仅修改 PKM 私有 Finish20 材质中分类为 metal 的母材质，以及 OpticMount23 的两份连接座材质。干、湿版本中的基础涂层节点同步处理，湿膜和水珠继续使用既有连接。保持资产路径和天气映射，已使用这些材料的枪体、金属改造件、脚架和瞄具座随材质更新生效。

- 大面积干燥涂层粗糙度目标约 0.56–0.80，以现有粗糙度纹理保留局部变化；少量划痕略微磨亮。
- 增加低幅度细涂层纹理，亚像素细节通过导数淡出；细纹作用于粗糙度，不替换结构法线。
- 稍降底色亮度与白色划痕增量，保留原深色枪钢、漆面颜色和白色标记。
- 木材、聚合物、橡胶、内腔、弹药、手臂和光学材料保持各自原材质；混合设备外壳沿用既有区域遮罩。
- 调整代码：`MatteRoughness.hlsl`、`MatteColor.hlsl`。源微划痕函数来自 `Finish20/MicroFinish.hlsl`，在本轮导入脚本内增加细纹通道。

## 接入

`import_finish.py` 捕获现有槽绑定，再重导入当前骨骼网格，按导入槽身份恢复材料。保留私人骨架、既有全部动画、当前部件可见性命名和脚架改造独立性。随后修改既有 PKM 材质节点代码并完成必要材质编译与保存；不重建材质表达式表，也不修改跨枪共享材料。

已有编辑器时，通过项目 `mcp_call_codex.ps1 -PythonScript` 批次互斥执行；没有编辑器时可使用 Python commandlet。重导入要求 PIE 已结束。脚本保留本轮目标资产的磁盘备份，不保存其他未保存资产。

当前状态：源模型和 FBX 已导出，活动骨骼网格与私人骨架已保存，42 份 PKM 金属干/湿材质已完成必要编译并保存；独立雨滴映射和原材质槽绑定保留。本次无 C++ 改动，不需要原生构建。

首次桥接因 PIE 正在运行而在资产写入前退出。用户结束预览并关闭编辑器后，第二次桥接未发现节点，没有执行写入；随后 Python commandlet 在后台完成全部接入，退出码 0。`import_receipt.json` 为逐资产保存回执，`import_background.log` 和 `import_console.log` 为制作日志，`BeforeImport/` 保留修改前资源，`material_graphs_before.json` 保留修改前节点代码。

FBX 导入器报告导出装配的绑定矩阵不匹配，随后自动重建绑定姿态成功；没有更新已有骨架参考姿态或重导入动画。该导入提示保留在日志中，后台导入成功不代表实机动作已验收。未启动交互式编辑器、运行游戏测试或生成验收渲染。
