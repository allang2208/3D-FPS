# SVD 持匣、插匣段左臂修正（2026-09-23）

## 2026-09-23 整理后的当前入口

旧换弹导出已由 `SVDThumbUp20260923` 替代；当前模型与材质入口为 `SVDRefinedFinish20260923`。本目录的旧制作记录保留其历史日期与验收边界。已经移走的旧导出、自动备份及恢复快照位于 `trash/svd-superseded-20260923/SourceAssets/` 下的同名目录，原路径、散列及保留替代物见 [归档清单](../../Docs/AssetArchives/svd-superseded-20260923.json)。当前所需 Blend、脚本、参数与原素材仍保留；不要直接重跑旧导入覆盖用户已认可的抓握。新 checkout 的恢复方式见 [SVD 发布说明](../../Docs/Weapons/svd-publication-20260923.md)。

用户定位：左手握着弹匣、往枪里插时扭曲。

本轮按手臂技能的整臂支撑、掌宽轴扭转和完整辅助骨变换经验制作。旧动作固定了新的手掌抓握，但肩肘主要沿用旧路径；插匣段接近伸展极限，腕轴折角较大。本轮重做左肩支撑与肘部路径，使前臂随掌宽方向旋转，移除旧求解器对主值角乘 0.65 的部分扭转处理。手掌和手指的组件空间姿态保持原来的弹匣接触关系。

## 已落盘

- 普通、空仓换弹，以及 vertical、canted、prism、angled 四种握把版本，共 10 段动画，已通过无界面 UE commandlet 导入并保存到原运行路径。
- `Animations/` 为本轮 FBX；`SVD_*_Editable.blend` 为可继续制作的源文件。
- `import_receipt.json` 为保存回执，`Before/` 保留这 10 个动画资产的导入前副本。
- 只导入动画，模型与材质沿用当前资产；右手动作、武器机械轨迹、换弹长度与接触时钟保持原有数据。

## 制作参数

以当前 SVD 骨架静止姿态的腕轴为度量，插匣段 160–240 帧：普通换弹最大折角从约 53.81° 调整到 19.25°，空仓从 45.68° 调整到 19.24°。相应肩部最大位移约 11.82 cm / 8.30 cm。这些是源动画制作数据，不能替代实际蒙皮与游戏画面效果。

`support_authoring.json` 保留制作时的逐帧参数，`author_animations.py` 保留完整制作方式。辅助骨使用完整段变换；肩肘曲线平滑后重新求解骨段长度。

本轮未启动编辑器或游戏，未追加渲染、试玩和运行验收。最终观感由用户测试。
