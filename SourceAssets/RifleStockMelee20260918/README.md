# M4 枪托砸击（快速进战·步枪版，2026-09-18）

用户 2026-09-18 指定「只做 M4」。上一版（2026-09-17 V1/V2 程序化姿态层）实机否决后已退役，
本轮按废案记录建议的路线重做：**先在 Blender 里按参考视频做对位预览，动作本体走作者源 clip**
（与已接受的手枪 DW715 握把砸击 V9/V10 同一条路线），不再有运行时程序化姿态层。

2026-09-18 晚：用户实测第一版（A 版，抬枪口再往下扫）判定不符预期；改为 B 版（抬枪托）后用户指正
**"是横向枪托攻击"**——不是上下劈，是**整枪在水平面内横扫**。据此定为 **C 版（默认）**：
俯仰基本不动、偏航做大，整枪横躺在画面里自左向右扫过（见下方"动作表"）。

## F 版（2026-09-19 三次重做，默认）：直接看视频落参——左手轴大回旋挥击 + 前伸

用户判 E 版仍不达标并要求"参考参考视频的动作，不要自己创造；如实汇报能否看到参考视频"。
**如实汇报：此前三轮都没有直接看过视频**——做的是把抽帧 PNG/拼条送给视觉分析模型拿回文字
描述再综合，该通道屡次自相矛盾，等于按转述+自创几何在做。本轮改为直接观看：Read 工具把
`test/uzi_ref/uzi_ref.mp4` 的 64.3–67.8s 段作为视频输入（另制 0.2x 慢放提亮版
`test/uzi_ref/ak47_melee_slow5x.mp4` 逐段观察）。

**直接观察结论（每周期 ~1.05s）**：①蓄势——整枪逆时针滚平（抛壳窗朝上）、**大幅偏航**
（枪口甩到画面左侧、枪托甩到右侧）、**拉近**变大；②打击——**以左手为轴的大幅水平回旋**：
枪托从右横扫过画面中心到左、**枪口同时从左大幅弧线摆到右**、整枪前伸推出去，滚平贯穿；
③收势回滚回位。D 版错在支点（右手——枪托不走弧线、只有枪口在甩）；E 版语义对但幅度错
（偏航 22° 太小，成了"顶撞"而不是"挥击"）；参考是**大幅度回旋挥击 + 前伸**两者兼有。

F 表（0.90s/接触 0.435，比例常数不变）：yaw 0/10/30/**46**/**−16**/−22/−30/0（大回旋）、
roll 0/−12/−45/−62/**−72**/−66/−45/0（滚平贯穿）、pitch 0/3/4/6/**10**/8/4/0、
offset（右,前,上）(0,0,0)/(0,−0.01,0.02)/(0,−0.03,0.04)/**(0,−0.06,0.06)**拉近/
**(0,0.22,0.02)**前伸/(−0.01,0.23,0.01)/(−0.01,0.10,0)/(0,0,0)。
tune_motion 校核：枪口 u 0.21(左)→0.52(接触)→0.60(右) 大弧线；枪托接触后扫入画面中下；
枪口距离 0.48(拉近)→0.83(前伸)。Base 预览视觉过审后全量烘焙（不变量恒定）、远程导入 6/6。

## E 版（2026-09-19 二次重做，已被 F 取代）：左手支点小偏航前顶（幅度不足）

用户判定 D 版"方向不对、动作语义也不对"后二次重做。对打击瞬间（66.33–66.70s）三段独立逐帧
精读修正了理解：参考是**以左手护木为轴，枪托自右后向前划弧顶出（前顶/推撞）**——枪口基本停在
左前不回头，唯一的大位移端是枪托（右中→右下→穿过中心→左中）；D 版绕右腕做整枪偏航大回旋，
枪口从左甩到右，"旅行的端"错了，读感就成了横扫。几何事实：我们的相机坐在机匣位、枪托板天然在
相机身后（左手支点偏航 <45° 时枪托 y 在相机平面之后，"蓄势枪托右侧可见"不可达——参考能拍到是
它相机更靠后），所以按**本框架取语义**：前顶为主、横摆为辅。

E 表（总长 0.90s、接触 0.435s，时间轴与 D 相同，比例常数不变）：

- pitch 0/3/6/6/**8**/8/4/0（枪口微抬，接触时枪口画面正中）
- yaw 0/8/18/**22**/**−6**/−14/−22/0（小幅：枪口停在左中，不再大回旋）
- roll 0/−12/−50/−60/**−75**/−68/−42/0（滚平贯穿，接触时抛壳窗正朝上）
- offset（右,前,上）米：(0,0,0)/(0,0,0.02)/(0,−0.01,0.03)/(0,−0.02,0.04)/**
  (0,0.26,0.03)** 硬前突/(−0.01,0.27,0.02)/(−0.01,0.10,0.01)/(0,0,0)

作者源支点按变体切换（E=hand_l，其余=hand_r）。离线校核（tune_motion.py E）：接触帧枪托板
(0.42,0.96) 入画中下、枪口 (0.49,0.47) 正中、枪口距离 0.87 m（前伸到位）。Base 预览条视觉过审
（枪口稳、滚平、前顶、双手无异常）后烘全量六 profile（不变量恒定），远程通道导入 6/6 =0.9000。

## D 版（2026-09-19 重做，已被 E 版取代）：右腕支点整枪偏航回旋

用户判定 C 版仍不达标后重做。这轮先用视觉模型逐帧精读参考（cycle_2 大图 + zoom_cock + 旧 C 版
对比条），得到的独立结论：参考的三要素是 **①蓄势整枪拉近相机到最大占屏（失焦贴脸）②枪身滚转
压平（顶盖向左、抛壳窗朝上，接触时最平）③枪托自右向左斜向扫过画面中心（枪口左出画→回扫到
画面中右、命中时枪口斜指右上）**；C 版三样全缺（无拉近、滚转峰值仅 11°、俯仰 5°~-4° 基本水平、
0.72s 太赶）。A/B/C 三版另有一个共性错误：方向讨论一直在"哪头扫"上打转，没有先锁定滚转与拉近
这两个更主导的读感要素。

D 表（总长 0.90s、接触 0.435s，时间轴 0.00/0.073/0.22/0.305/**0.435**/0.47/0.639/0.90，
比例与运行时常数逐项一致：Release .081/Cock .339/Contact .483/Follow .710）：

- pitch 0/3/5/5/**16**/14/12/0（枪口整段上抬，命中时斜指右上）
- yaw 0/14/41/45/**−12**/−22/−40/0（枪口左→右，机匣尾右→左横扫）
- roll 0/−15/−50/−55/**−80**/−74/−58/0（顶盖向左压平，接触时最平）
- offset（右,前,上）米：(0,0,0)/(0.01,−0.04,0.02)/(0.02,−0.12,0.04)/**(0.04,−0.12,0.10)**蓄势拉近
  /(0.04,−0.02,−0.02)接触/(0.03,0,−0.03)/(−0.04,0.10,−0.02)/(0,0,0)

制作流程（这轮的可复制路径）：`tune_motion.py` 先离线核屏幕轨迹（锚点由 solve 按参考 30fps 帧
的枪托屏幕位置反解：butt (0.42,0.85)→(0.58,0.60)→(0.42,0.52)→(0.34,0.60)）→ 烘焙 Base →
`render_preview.py` + `make_preview_strip.py` 出对位条 → **视觉模型逐帧比对通过后才烘全量**。
六 profile 握把不变量全程恒定（dR 0.1251/dL 各自常值）。导入走编辑器远程通道（编辑器开着，
commandlet 保存会静默失败），6/6 落盘 length=0.9000（10:53）。运行时无代码改动。

## 参考读谱

参考：`D:\FPS3D\test\uzi_ref\uzi_ref.mp4`（B 站 BV13K421e7Rw，COD4 模组展示），1:05–1:07 的 AK47 枪托近战段。

- 抽帧：`Reference/dense_frames/f_001..f_044`（64.80–67.67 s，1/15 s 一帧，640×360）与
  `Reference/f30/f_0001..f_0165`（63.0–68.5 s，**1/30 s 一帧，1280×720 原生分辨率**）。
  3×3 读谱大图在 `Reference/Sheets/`；夜景提亮读图在 `Reference/Bright/`
  （口径 out=clip(2.4·in+40)，与旧读谱一致）。
- 两个完整周期（64.80–65.87 / 66.00–67.07），单周期约 0.9–1.07 s。读到的动作语言：
  常态低持 → 整枪横过来（枪身几乎横躺在画面里）向一侧蓄势 → **水平横扫过画面中心** → 回位。
  与用户口述的"横向枪托攻击"一致：不是上下劈，是左右横扫。
  双手全程持枪（右手握把、左手护木），不是手枪那种松左手。
- **只用动作语言，不取资产**：COD4 动画素材有版权，未提取任何帧数据进资产管线。

## 作者源 clip（本目录）

- `author_quickcombat.py`：按 profile 打开对应 M4 可编辑源 → 采样该 profile 的待机帧 →
  以 **整枪刚体搬运写在 `WPN_` 骨上、双手由握把关系带动** 的约定逐帧 120 Hz 烘焙
  `M4_QuickCombat_<profile>` → 导出 `Animations/A_M4_QuickCombat_<profile>.fbx`。
  关键不变量：`|WPN_root − hand_r|`、`|WPN_root − hand_l|` 全程恒定（作者源自检逐帧打印）。
- 动作表（`animation.json`，**C 版＝默认**）：总长 **0.72 s**、接触 **0.348 s**（0.348→0.383 为 3 帧接触顿帧）。
  起手 0–0.058（整枪前推 5 cm，开始偏航）→ 蓄势顶点 0.20–0.26（**yaw +46°**：枪口甩到画面左侧、
  整枪横躺）→ 横扫 0.26–0.348（yaw 从 +46° 扫到 **−12°**，枪身横过画面中心命中）→
  跟随 0.383/0.511（yaw −22°/−36°，继续向右侧带出）→ 回位 0.72。俯仰只有 5°~−4°，整段基本水平。
  参数可用 `tune_motion.py C` 离线核对屏幕轨迹（枪口 u：0.54→0.21 蓄势 →0.60 接触→0.75 跟随）。
  A/B 两个上下劈的候选仍保留在脚本里（`--variant A|B`）。
  参考单周期约 1.0 s（含镜头摆动），本工程取更干脆的一档；改节奏只改这张表，
  运行时常数按 **clip 总长比例**换算，不需要同步改代码。
- **写参数前先跑 `tune_motion.py`**：它把「枪根刚体搬运」换算成枪托/枪口在画面里的位置
  （相机按游戏口径：垂直 75° FOV、视模相对相机右 7 cm/下 7 cm），不开 Blender 也能看框景、
  避免又出现「枪托顶到镜头前把画面糊住」这类问题。`python tune_motion.py solve` 可按目标屏幕位置反解参数。
- 六个 profile（与战术冲刺同一套）：**Base / Drum / Angled / Vertical / Canted / Prism**。
  每个 profile 用自己待机源的握把关系，所以换握把不会脱手。Base/Drum 沿用战术冲刺的
  12 mm/32 mm 前移发布口径。
- `render_preview.py` + `make_preview_strip.py`：从视模视点渲染对位胶片，输出
  `Preview/<profile>_strip_vs_reference.png`（上=本工程 clip，下=参考帧）。相机口径与游戏一致：
  **垂直 75° FOV（13.2 mm/36 mm 传感器）**，位置 = 骨架原点「左 7 cm、上 7 cm」
  （对应 UE 里 `M4HipViewmodelLocation=(0,7,-7)`cm）。用错 FOV/位置会误判框景。

## 导入

- `import_quickcombat.py`：骨架取 `/Game/Weapons/M4HK416Replica/SK_M4_FoldingSights_HK416`，
  压缩沿用 `BC_M4Viewmodel`，落到 `/Game/Weapons/M4StockMelee20260918/<profile>/`。
  编辑器在跑时用远程执行（`Tools/AssetPipeline/ue_python_exec.py --script ...`），
  编辑器关闭时用 commandlet（`-run=pythonscript`，不起第二个进程）。
  B 版重导读回 **6/6、length=0.7167**。
- **变体切换**：`author_quickcombat.py -- Base --variant A|B|C`（默认 **C＝横向横扫**；B＝抬枪托的上下版；A＝抬枪口的旧版）。

## 运行时接线

- `AFPSGAMECharacter::TriggerRifleStockMelee()`：M4 在手且非忙碌时按 profile 选 clip 播放，
  状态走既有 `EAKMWeaponState::QuickCombat`；`ResolveRifleGripProfile()` 与战术冲刺共用同一解析。
- `UFPSQuickCombatComponent`：新增步枪样式（`ConfigureForRifle`）——同一套时钟/冷却/修炼/
  击退眩晕合同，只换命中探针（`GetRifleStockMeleeProbe`：枪口沿枪轴回撤 12 cm 的枪身前段，
  读当前动画姿态而不是入场快照）与镜头常数（`QuickCombatRifleMotion.h`，动作镜头 ×1.6、冲量同级偏重）。
- `UColdSteelStatusModel::TriggerQuickCombat()`：剑 → 配重锤 / 手枪 → 握把砸击 / 步枪 → 枪托砸击。
- 修炼走 `rifleMastery`（`Shot.bRifle=true`）；音效沿用快速进战同一枚起手音与钝器命中音。

## 未做

## 状态：暂停待办（2026-09-18 收尾）

用户实机后判定 C 版仍不符预期，要求暂停并记为待办（见 `Docs/Backlog.md` M5 行与
`Docs/Weapons/m4-stock-melee-20260918.md` 的「恢复入口」节）。

- A/B 两版只存在于 `author_quickcombat.py` 的参数表里，**没有产生需要移到 trash 的文件**；
  同名 FBX/blend 是 C 版覆盖写的。
- 引擎侧 uasset 按仓库规则只在本机保留（`.uasset` 在 .gitignore 内）。
- 未实机判读的项：横向幅度/节奏、命中探针手感、六个握把的遮挡与穿模。

## H 版（2026-09-19 五次重做，默认）：实机审计闭环标定的大回旋版

用户实测 G 判"枪托没往前扫、像枪口往前捅"。本轮打通**实机审计闭环**并暴露两个测量陷阱：

1. **新增实机审计夹具**（`Source/FPSGAME/FPSGAMEQuickCombatAudit.cpp`，`-QuickCombatAudit`）：
   `-game` 进程内装 M4→触发砸击→逐帧记录枪托/枪口屏幕投影、相机与枪根世界变换，30Hz 截图。
   跑法：`UnrealEditor.exe <uproject> /Game/Weapons/M4InfimaRigV4/Preview/L_M4RigValidation -game
   -windowed -RenderOffscreen -ResX=960 -ResY=540 -unattended -nosound -GunplayAudit -QuickCombatAudit
   -ColdSteelProfile=QuickCombatAudit-<label> -GunplayLabel=<label> -FixedSeed -d3d11 -UseFixedTimeStep
   -FPS=60 -GunplayCaptureFrames`（与用户编辑器并存无冲突；产物在 `Saved/QuickCombatAudit/<label>/`）。
2. **陷阱一：探针方向**。M4 臂架 +Y 是**前**，枪托在局部 −Y（g4 用 +0.26 量到的是机匣前段）。
3. **陷阱二：离线相机模型失真**。Blender 预览/tune_motion 的相机 ≠ 游戏相机（视模偏移+运行时
   镜头层），G 在预览里"枪托穿中央"、实机里"下沉出画"——**屏幕级验收只认实机审计**。

G 实机数据（真枪托）：入画 (0.79,0.90)→接触 (0.46,0.61) 即止，回旋仅 ~31°——读感=前捅。
H 按实测方向性加倍回旋（表 yaw 蓄势 +60 → 接触 −62；实测表 yaw −45 时枪仍偏左，横过画面需
−60 级）+ 滚平贯穿 + 前伸。**H 实机审计（h1）**：枪口 u −0.3（出画左）→0.76（右侧）大弧；
枪托 (0.83,1.24) 入画→接触 (0.41,0.74)→持续到 0.38——真正扫过中心；枪口距离 47→96cm。
实机画面（`m4_H_ingame.mp4`）确认"整枪压平横扫+枪托穿过中央"读感与参考一致。
六 profile 不变量恒定，远程通道导入 6/6（12:08）。
