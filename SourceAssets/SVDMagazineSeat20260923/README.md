# SVD 装匣左手同步调整（2026-09-23）

## 2026-09-23 整理后的当前入口

旧换弹导出已由 `SVDThumbUp20260923` 替代；当前模型与材质入口为 `SVDRefinedFinish20260923`。本目录的旧制作记录保留其历史日期与验收边界。已经移走的旧导出、自动备份及恢复快照位于 `trash/svd-superseded-20260923/SourceAssets/` 下的同名目录，原路径、散列及保留替代物见 [归档清单](../../Docs/AssetArchives/svd-superseded-20260923.json)。当前所需 Blend、脚本、参数与原素材仍保留；不要直接重跑旧导入覆盖用户已认可的抓握。新 checkout 的恢复方式见 [SVD 发布说明](../../Docs/Weapons/svd-publication-20260923.md)。

沿用 `SVDReloadArm20260923` 的完整动作源，在用户指出的装匣左手阶段继续制作。保持既有持匣、插入的手掌锚点和上一轮整臂支撑，修改压实之后的松指、外侧退让和回握。

## 动作衔接

120 Hz 源动画：240 帧弹匣压实，左手继续保持抓握至 244 帧；244–260 帧依次松开拇指和四指；254–268 帧沿弹匣外侧退开；268–302 帧回到各版本原有护木或握把位置，284 帧起收指恢复握姿。腕肘与肩部跟随新手腕轨迹求解，支撑过渡在 336 帧结束。保持骨段长度和手指局部平移、缩放；辅助骨使用完整骨段变换。

普通与空仓、原厂及四种握把共 10 条动画，均沿用原资产路径。弹匣轨迹、240 帧压实事件、右手拉栓和整段时长保持原数据。

## 文件

- `author_animations.py`、`authoring.json`：制作脚本和阶段信息。
- `SVD_*_Editable.blend`、`Animations/`：五份可编辑源及十条 FBX。
- `import_animations.py`、`run_job.ps1`：后台导入；已有编辑器时使用当前桥的批次互斥。
- `import_receipt.json`：每条动画的实际保存回执；`Before/`：本次替换前的对应资产。

仅更新动画。本轮不启动游戏，不追加测试、截图或渲染；实际画面效果由用户测试。
