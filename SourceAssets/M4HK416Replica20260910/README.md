# M4：HK416 动画适配与音效同步

2026-09-10。保留用户当前 M4、Manny 手套手臂、机械瞄具／全息镜和弹鼓配件。

## 已接入

- 从 Godot 实际运行时逐帧读取 HK416 普通换弹、空仓换弹、装备动作，包含 AnimationTree 和手部姿态修正。源数据为 `Reference/runtime_poses.json`，原版实录为 `Reference/HK416_actual.avi`。
- 保留原 98 根骨骼及层级，新增 `WPN_ChargingHandle` 和 `WPN_BoltCatch`。分别绑定现有 M4 的 332 个拉机柄／卡榫顶点与 162 个释放钮顶点；没有替换枪体或手臂。
- 完整迁移枪体、弹匣、手腕和指节运动，再以 M4 接触位置适配。空仓使用原版翻腕释放动作；右手食指维持自然弯曲。装备时拉机柄行程约 6.41 cm，空仓枪机闭合行程 3.5 cm。
- 普通换弹 2.1 s，空仓 2.7 s，装备 0.62 s。弹鼓倍率仍为 1.75，改装台和运行时均从同一目录数据计算：3.675 / 4.725 s。
- 普通换弹声音接触点：29、76、95 帧；空仓：21、54、80、130 帧（60 Hz）。声音与动画共享时钟和弹鼓时间映射。切换／卸下枪械停止旧机械声音与待播放队列。
- 补充原版 HK416 装备录音。开火和机械声保持原素材、自然音高；实际声音由 UE 音频组件播放。
- 当前资产位于 `/Game/Weapons/M4HK416Replica`。复制独立 Skeleton 并声明原 Skeleton 兼容，原动画与源模型资产保留。

## 验证证据

- `compile3.log` / `compile4.log`：编译成功。最终模块 `UnrealEditor-FPSGAME-2026091204.dll`；后一次仅更新弹鼓审计的期望时长。
- `import_report.json`：5 个动画在 120 Hz 回读，压缩位置误差最大约 0.0041 cm。旧待机与新骨架兼容。
- `surface_validation.json`：空仓释放接近段 2.0–2.2833 s，以 120 Hz 采样实际变形手套顶点并向 M4 左侧机匣表面投射射线。最低侧面间隙约 0.783 mm；第 130 帧最近手套接触点距离目标约 0.322 mm。这是指定接触段检查，不是整个模型在所有动作中的无穿模证明。
- `Saved/GunplayUpgrade/m4-hk416-final-av60/result.json`：50 项游戏检查通过，进程退出 0。
- `Saved/GunplayUpgrade/m4-hk416-equip-final/result.json`：20 项装备、连续切枪及声音中断检查通过，进程退出 0。
- `drum_runtime.log`：35 项弹鼓、瞄具配合、换弹和弹药事务检查通过。
- `audio_validation_m4-hk416-final-av60.json`：真实混音录音非静音、无削波；11 个机械音效均匹配源波形，相关性 0.802–0.995；枪声攻击段约 0.999。该次声音触发时间误差最大约 23.8 ms，另有混音缓冲延迟。
- `equip_audio_validation.json`：两次完整装备录音与 HK416 源录音相关性约 0.998；中断场景由声音组件／队列检查覆盖。

## 预览与可编辑源

- `Delivery/M4_换弹与开火_实际录音.mp4`
- `Delivery/M4_装备拉栓与切枪_实际录音.mp4`
- `Delivery/HK416_M4_实际画面对照.png`
- `M4_HK416_Adapted_Editable.blend`、`M4_HK416_Drum_Editable.blend` 和各动作 FBX。

视频为每秒 20 张实际游戏截图与同一次运行的真实混音输出；缺失截图保持前一帧，未逐个移动或替换声音。它不是游戏性能基准。两边枪体、手套尺寸与镜头不同，结果为动作适配，不宣称像素或骨骼坐标 100% 复刻。

未关闭用户原有编辑器。已经打开的 UE 进程需要重新加载本次 C++ 模块或重启编辑器，才能使用新逻辑。

## 源与备份

Godot 生产源文件只读。使用已有 HK416 动画和音效、用户现有 M4 与手臂资产；继承各原资产既有授权，本次不引入新外部资产或授予新许可。

修改前的相关 C++ 和配置保存在 `D:/FPS3D/FPSGAME/trash/M4HK416Replica20260910-before`。并行开发目录的其他修改保持原样。资产构建与回读脚本均保留在本目录。

导入日志中的既有 GameFeatureData 目录配置错误、弹鼓测试场景多方向光提示与本次动画／音效检查分开记录，未修改这些无关系统。
