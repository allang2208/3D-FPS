# 幻影后握把接入（2026-09-13）

用户选定三视图生成的 seed_91627，作为 M4A1、AKM、QBZ-191 的后握把改造件。

- 枪匠入口：后握把 → 幻影后握把，选项 ID `phantom_reargrip`。
- ADS 瞄准速度 -15%：速度乘 0.85；现有系统使用耗时，配置 `ads_percent=0.17647058823529416`。
- 后坐力控制 +10%：实际后坐力乘 0.9。
- 腰射随机散布 +10%：散布乘 1.1。
- 安装隐藏原厂握把材质分区，拆卸恢复；接入枪匠草稿预览、武器图标、掉落武器和既有改造存档流程。

## 资产与制作

原模型：`../PhantomRearGripMultiview_20260913/seed_91627/textured_master_00001_.glb`。

`build_assets.py` 保留原始模型，制作约 5 万面候选、PBR 贴图及三枪拟合静态 FBX。各枪文件夹保留可编辑 Blender 源文件。AKM/QBZ-191 从既有枪械源文件拆出原厂握把材质分区，导出新路径；M4 使用已有独立握把分区。拟合参数见 `authoring.json`。

`import_assets.py` 导入握把、材质和 AKM/QBZ-191 枪械分区版本，完成回执见 `import_results.json`。模型在 `/Game/Weapons/PhantomRearGrip`，新枪械路径见回执。未修改既有动画。

## 交付状态

完成源代码、配置、资产制作与导入。Editor 必要编译成功，模块后缀 913743。重新打开编辑器以加载新模块和资产。

未启动游戏、未执行测试或预览验收，由用户测试。导入器对两把骨骼模型报告绑定姿势警告并使用零时姿势重新绑定，因此手部接触、动画表现与装配外观尚未确认。导入脚本已完成并保存资产；命令行进程因项目现有 GameFeatureData 配置错误以非零状态退出，不代表该项目错误已解决。
