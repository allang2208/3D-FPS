# SVD 材质与衔接件细化 — 2026-09-23

## 2026-09-23 整理后的当前入口

旧换弹导出已由 `SVDThumbUp20260923` 替代；当前模型与材质入口为 `SVDRefinedFinish20260923`。本目录的旧制作记录保留其历史日期与验收边界。已经移走的旧导出、自动备份及恢复快照位于 `trash/svd-superseded-20260923/SourceAssets/` 下的同名目录，原路径、散列及保留替代物见 [归档清单](../../Docs/AssetArchives/svd-superseded-20260923.json)。当前所需 Blend、脚本、参数与原素材仍保留；不要直接重跑旧导入覆盖用户已认可的抓握。新 checkout 的恢复方式见 [SVD 发布说明](../../Docs/Weapons/svd-publication-20260923.md)。

用户确认最终换弹抓握成功后，要求记录异形弹匣标准，并参考 A762 升级 SVD 材质、前握把和瞄准镜衔接件。

## 已保存的交付

- `finish_receipt.json`：37 组 SVD 私有表面材质及对应雨湿材质、4 个紧固件／凹槽材质实例，绑定至 22 个现有枪体和配件网格；同步现有湿润映射表。无界面制作进程退出码 0。
- `connectors_receipt.json`：23:46 完成 7 个实际 UE 模型资产的覆盖导入与保存，包含 5 类前握把、通用瞄具支架和带 PSO 细节的现有 SVD 骨骼网格。无界面导入进程退出码 0。
- `Exports/` 中保留对应 7 份 FBX；同名 `.blend` 为可编辑制作源。
- 本次没有导出或导入动画；继续使用用户已认可的 `SourceAssets/SVDThumbUp20260923` 动画。
- 未启动 GUI 编辑器、预览、渲染或游戏测试。这里的完成指制作、导入及资产保存，游戏视觉效果由用户确认。

## 材质

参考工程内 `/Game/Weapons/A762/Refinement03/Materials/M_A762_UpperReceiver03` 及 `T_A762_RebuiltFinish`。读取到 A762 的金属度 0.83、粗糙度中心 0.34；原 SVD 钢件哑光处理下限约 0.56，容易压平表面层次。

SVD 新涂层使用稍偏冷的深色缎面，按零件区分粗糙度：枪机 0.30、拉机柄 0.33、开关／接口 0.35、主体及一般配件 0.37、弹匣 0.39。螺钉为 0.29，凹槽为 0.43。使用 A762 细纹纹理的私有副本，5 cm 尺度的三向投影、2.4×1 方向比例，轻微影响颜色和粗糙度，不覆盖原始结构法线。

保留原有金属／聚合物区域、白色标记、镜片和光学遮罩，保持 UV0 结构法线及 AO；不修改 A762 资产、共享手模材质、光学玻璃或已有枪托聚合物材质。沿用原有湿润图，替换其中旧哑光层，避免重复叠加粗糙度。新资产位于：

`/Game/Weapons/SVDDragunov20260922/RefinedFinish20260923/`

## 衔接件

- `vertical / tactical_vertical / canted / prism / angled`：保留握把本体、UV、法线和安装原点；将分散小垫块改成连续鞍座，加局部夹持边和有实际内六角凹槽的紧固件。维持原连接高度，不挪动握持区域。
- `optic_bridge`：保留接触点、导轨长度／间距和 Z=0.086 m 的安装顶面；改成倒角侧板、连续渐变支撑肋及燕尾截面导轨齿，细化凹槽紧固件。
- 原厂 PSO：保留镜体、镜片、先前的接缝修复以及支架结构，在四处原有固定螺钉位置细化螺帽和凹槽。新增材质槽含 `ScopeMount` 身份，继续随原厂镜体显隐，不影响换装通用瞄具。
- 现有配件路径不变，当前 SVD 网格仍为 `/Game/Weapons/SVDDragunov20260922/StockAdapter20260923/SK_SVD_ModularStock`。不修改配件安装变换、瞄具定位、骨架参考姿势、手部权重、动画或 C++。

## 抓握经验已同步

个人 SKILL 和工程镜像均添加 `ue5-fps-arms-animation/references/irregular-magazine-grip.md`，并从技能入口和 `pose-contact.md` 链接：

1. 宽长／异形弹匣先选自然可包住的局部区域；SVD 抓前半段，不强迫拇指跨越全宽。
2. 拇指自然向上延展、轻屈，不拉伸虎口，也不收入掌心。
3. 已认可的掌心、四指和腕臂冻结；局部问题只改对应手指，不重新拟合整手。
4. 保持骨长、位移、缩放和蒙皮，取匣、持匣、插入、压实及松手连续衔接。数值接触误差不能代替自然手型与用户确认。

## 制作与备份

- `read_inputs.py` / `inputs.json`：当时实际 UE 材质、槽位及 A762 参数输入。
- `install_finish.py`：新建私有涂层与雨湿版本，保留继承参数并更新现有绑定。
- `read_geometry.py` / `geometry_inputs.json`：制作所需的原厂 PSO 零件位置。
- `author_connectors.py`：背景 Blender 模型制作与导出。
- `install_connectors.py`：保留当前材质、插槽、骨架、物理／后处理属性后替换模型。
- `Before/`：材质绑定前的现有网格及湿润映射表备份；`BeforeGeometry/`：本轮几何替换前的网格备份。
- `run_job.ps1`：遵循现有 UE MCP 批次互斥；已有编辑器时走桥，无编辑器时走无界面 commandlet，不主动打开编辑器。

导入曾因 Python 不能直接读受保护插槽数组而中止，改用临时静态网格组件读取插槽名称；一次重复保存已完成的紧固件材质遇到文件占用，后续仅在用途标志需改变时保存，最终全部模型保存成功。回执记录每个实际目标和保存结果。
