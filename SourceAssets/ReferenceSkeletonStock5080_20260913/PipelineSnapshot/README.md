# 本次生成节点记录

`nodes.py` 从当前远端 `D:/开发文件/ComfyUI/custom_nodes/ComfyUI-Trellis2/nodes.py` 保存，供此次工作流来源追踪。

读取 Git 提交号时，SSH 默认命令环境返回找不到 `git`，原始回显保留于 `node_revision.txt`。本次没有获取到节点仓库提交号，也未为了记录版本而安装或更改远端环境。实际服务与节点接口见上一级目录的 JSON。
