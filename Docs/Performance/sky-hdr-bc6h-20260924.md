# 天空 HDR 压缩：2026-09-24

本批执行用户同意的第一阶段：七张 PWL 天空立方贴图使用 BC6H HDR 压缩，保留原有分辨率与 mip 设置。运行时天空材质、昼夜切换、曝光、SkyLight、体积云、Lumen、硬件光追以及纹理池配置均不在本批修改范围。

完成状态：七张资产已在原路径完成压缩构建并保存。使用已有 UE 编辑器会话执行，未新开引擎进程。构建日志使用 `TFO_BC6H`；桥返回 `success=True` 与七条保存记录。

本批回执：`SourceAssets/HDRSkyBC6H20260924/Runs/20260924-223349-456610/authoring.json`；执行输出：`Saved/HDRSkyBC6H20260924/apply-20260924-03.txt`。

## 资产与制作

路径前缀：`/Game/PWL_Light_Manager/Textures/HDR/`。

| 资产 | 既有每面分辨率 | 原 FloatRGBA 数据 | BC6H 数据估算 |
| --- | --- | --- | --- |
| T_HDR_Night_00 | 4096 × 4096 | 768 MiB | 96 MiB |
| T_HDR_Sunrise | 4096 × 4096 | 768 MiB | 96 MiB |
| T_HDR_Sunset | 4096 × 4096 | 768 MiB | 96 MiB |
| T_HDR_Sunset_02 | 4096 × 4096 | 768 MiB | 96 MiB |
| T_HDR_Sunshine | 4096 × 4096 | 768 MiB | 96 MiB |
| T_HDR_Sunshine_02 | 4096 × 4096 | 768 MiB | 96 MiB |
| T_HDR_Overcast_High | 2048 × 2048 | 192 MiB | 24 MiB |
| 合计 | 六张 4K、一张 2K | 4800 MiB | 600 MiB |

只将 `compression_settings` 从 `TC_HDR` 改为 `TC_HDR_COMPRESSED`，关闭编辑器的延迟压缩（原值已为 false）。沿用线性 HDR（sRGB=false）、`TMGS_NO_MIPMAPS`、原有 MaxTextureSize、LODBias、亮度、饱和度和 Oodle 项目编码策略。原始贴图源数据保留在资产内；不重导入、降分辨率或改成普通 LDR 图片。

制作脚本：`Tools/Weather/compress_sky_hdr_bc6h.py`。脚本拒绝修改已有未保存编辑的目标包，在修改前备份七张资产，逐张等待纹理构建完成，只保存对应纹理。正在运行的编辑器通过 `Tools/AssetPipeline/mcp_call_codex.ps1 -PythonScript ... -OutputFile <唯一新路径> -MaxOutputChars 3000` 接入；编辑器关闭时才使用无界面的 PythonScript commandlet。脚本可以再次用于后续源素材重导后的同范围压缩制作。

逐张制作状态、修改前后属性与备份路径记录于 `SourceAssets/HDRSkyBC6H20260924/Runs/<批次>/authoring.json`。需要恢复时，在编辑器关闭且资产没有后续编辑的条件下，用对应批次 `Backup/PWL_Light_Manager/Textures/HDR/` 中的原始包恢复精确目标；编辑器打开时不要直接覆盖已加载的包。

## 收益口径与未测范围

原占用来自 `Saved/Diagnostics/M4Texture20260924/runtime-streaming-snapshot.log` 的既有 PIE 快照。本次继续采用一个 mip 层级：FloatRGBA 每像素 8 字节，BC6H 每像素等效 1 字节。理论纹理数据由 **4.6875 GiB 降至 0.58594 GiB**，减少 **4.10156 GiB（87.5%）**。这不是整卡显存或 FPS 的复测结果，不包含捕获贴图、临时资源和分配开销。

BC6H 为有损 HDR 压缩；星点、太阳高亮边缘、霞光渐变及金属反射的实际观感交由用户查看。本批没有主动启动或重启 UE、启动 PIE、运行性能采样、截图、视觉验收或回归测试。资产保存时引擎自身生成的内容浏览器缩略图属于保存流程。

本改动不能视作已确认修复 M4 的间歇性低清；此前尚未捕获其故障时的完整流送状态。

## 源码发布边界

2026-09-25 发布制作脚本与本说明。七张纹理属于 PWL 内容依赖，原包、修改后的 uasset、成功批次及 Backup 原始包均留在本机；公开 Git 不替代这些资产的完整备份。两次修改前失败的属性读取及对应桥输出已移入本轮 trash，成功保存回执继续保留。
