# 枪械配件与预览任务归档

2026-09-13，按用户要求整理本任务已确认废案，更新技能并发布 Git。范围是本任务的枪托／AKM 共振二代握把、已撤回的 UI 升级和最近的预览清晰度优化；没有整理其他并行任务。

## 已归档

共 40 个文件、2,349,605,198 字节（约 2.35 GB），移动至本机 `trash/gunsmith-session-cleanup-20260913/`，保留原相对目录：

- 23 份 `.blend1` 自动备份：骨架枪托、参考图 5080 枪托、QR 高性能后托和 AKM 共振二代握把。对应 `.blend` 正式可编辑文件都在原位置，范围内制作脚本未引用 `.blend1`。
- 17 份用户明确撤回的 UI 升级文件：原 `Saved/UIStyleAudit20260913/Withdrawn/` 中的设计草案、源码快照与回退记录。现行 UI 源码和正式设计规则未被替换。

逐文件记录见 [归档清单](gunsmith-session-archive-20260913.json)，含原路径、trash 路径、大小、SHA-256、原因与保留替代物。移动前核对绝对路径与来源散列，移动后逐项比对 SHA-256；此次是可恢复移动，没有删除文件或释放磁盘空间。

## 保留内容

原参考图、三视图、5080 原始模型、有效作者脚本、正式 Blend／FBX／uasset、材质及许可、制作／导入记录继续保留。`Before` 目录承担可重建输入或回退基线，不按日期归为废案。预览优化的两份共享源码基线、材质生成器、构建日志和已发布增量补丁也保留。

旧 `M_WeaponPreview` 和工作台导入器保留作既有背景／环境恢复与回退资料；新合成材质由独立 `build_gunsmith_preview_resolved.py` 生成，不因运行路径已更换就整批移走共享恢复资源。

## 技能与发布

为 [UE UI 技能](../../skills/ue5-ui-umg-slate/SKILL.md) 增加 [预览渲染参考](../../skills/ue5-ui-umg-slate/references/preview-rendering.md)，覆盖贴图驻留、原始 HDR 与最终显示色、透明覆盖率、空间／时序抗锯齿选择、DPI 分辨率、材质槽更新及释放生命周期。已同步个人技能和工程镜像；案例中的采样／刷新参数及未实机测试状态与通用方法分开。

Git 仅发布本次技能、归档清单和说明；trash、商用素材、源模型及构建产物继续留在本机。预览运行代码此前已直接接入当前宿主，其相对于共享基线的补丁发布于 `076b3c9`，边界见 [预览改动说明](gunsmith-preview-quality-20260913.md)。本次不夹带其他已有暂存或未提交修改。

执行仓库发布相关的散列、差异、链接与推送检查；没有重新构建、启动游戏、截图或运行功能测试。
