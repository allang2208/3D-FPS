# 强化工作台实时预览

强化面板以改造栏的 `UM4GunsmithWidget` 为展示宿主，复用 `InitializePreview / SyncStudioPreview / TickCapture / RotatePreview` 和 `SM4PreviewSurface`，不再放大背包图标。两处使用相同的工作台背景、灯光、材质、投影、按可见部件包围盒计算留白、鼠标旋转灵敏度及俯仰限制。强化预览区域边距改为与改造栏相同的 8/8/8/20。

新增独立展示模式：预览世界不 BeginPlay，不连接玩家档案，不改变实际装备。只复制选择实例的枪型和已安装配件；切换装备或配件变化时重建展示姿态，强化等级和材料数量变化不会重置旋转。默认水平侧视；左键拖动旋转、双击或水平复位按钮重置；瞄准按钮使用该枪的 Aim 动画与现有 ADS 校准。

静止每秒捕获一次，拖动后短时间提高到 30 Hz；渲染目标按界面尺寸调整。关闭时释放鼠标捕获、场景捕获、展示世界、模型副本和渲染目标。强化业务、材料发放规则和伤害公式保持现有实现。

这项更新取代 `enhancement-materials-20260910.md` 中“强化面板仍为静态图”的历史检查结论。

2026-09-10 实测批次 `20260910225331`：1280×720 与 960×540 分别通过 46 项操作/业务检查及 3 项新进程读档检查，共 98 项零失败。实机鼠标拖动、按钮复位、瞄准切换、背包枪械独立预览及关闭释放均通过。已查看默认、旋转、瞄准和切换枪械截图；随后修正 AKM 侧视中的手臂材质显示，仅瞄准时保留手臂。

最终构建 2026091906：独立预览关闭时立即释放渲染目标资源，不等待对象 GC。最终复验 `20260910230117-960-write.log`（46 项）与 `20260910230117-960-reload.log`（3 项）全部通过，实际查看最终 `backpack-live-960.png` 与 `enhance-960.png`，侧视无手臂，重复打开后的截图未再出现先前约 10 MB 的纹理池提示。此观察不代表全游戏纹理预算验收。

共享代码回归：`Saved/GunsmithWorkbenchAudit/20260910225921-1280.log` 的原改造栏 34 项检查全部通过，并检查实际旋转截图。最终追加的资源释放仅在独立展示模式生效。当前已打开的编辑器需要重新启动以加载新模块。
