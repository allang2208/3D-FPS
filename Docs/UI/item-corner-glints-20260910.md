# 加工三角角标与闪光

按用户本次指定，替代上一版强化/改造/附魔竖条：强化在右上角使用金色三角形，改造在右下角使用青色三角形，附魔在左下角使用蓝色三角形。只显示实际存在的加工状态。按后续要求，背包与装备格移除稀有度竖条及其左侧预留空间；稀有度与强化等级仍由详情浮窗显示。

背包与装备格共用 Slate 绘制。三角形按格高适配到 7–18px，使用同色渐变和局部亮带；每 3.2 秒扫光一次，各种加工及不同实例错峰。光限制在三角形内，拖动时随原物品淡出。物品名称、数量避开对应角标，移除竖条释放模型空间。没有新增材质资产或修改库存/加工数据。

表示数据仍由模型变更事件更新；可见背包中有加工物品时，只请求 Paint 刷新来播放闪光，没有持续材质捕获、异步任务或新增定时器。审计的帧录制定时器仅在 `-InventoryCornerGlintAudit` 下启动，录完 32 帧后清除。

UE 5.8.2 Editor Development 编译通过，模块后缀 `2026092032`。使用 `run_inventory_visual_acceptance.ps1 -Widths 1280 -CaptureGlints` 录制真实游戏画面，并验证装备格与背包格的三种状态组合、72 格命中、合法/非法预览及物品数据不变。另检查 960×540、1920×1080。每档 8 项断言。

`build_corner_glint_preview.py` 将 32 张游戏截图的背包区域裁成原速 GIF（120ms/帧），输出 `Saved/InventoryVisual/corner-glints-1280.gif`。1280 实机角内固定像素的金/青/蓝三处分别有 8/11/9 种亮度颜色，确认三种闪光都在实际渲染中变化。输出图片与 GIF 留在本机 Saved 中。原生更新由重新启动的编辑器/游戏进程加载。

移除稀有度竖条后，以模块后缀 `2026092041` 重新编译通过；1280×720 真实游戏验收 8 项通过，日志 `Saved/InventoryVisual/20260910215121-1280.log`。本次更新 overview/selected/allowed/rejected 截图，确认背包和装备格无稀有度竖条、三色角标保留；此前 GIF 为移除竖条前的闪光验证记录。
