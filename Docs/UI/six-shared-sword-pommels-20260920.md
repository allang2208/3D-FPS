# 六款通用配重栏

将三款共用扩展为六款共用，保留两个原系列的不同造型和属性。配重栏顺序：原装、硬化配重、符文配重、魔力球配重、陨星锤首、凝碧星核、疾星配重。

两把当前可改造剑都读取同一份六款定义，不保留武器专属选项过滤。沿用当前选项列表/网格滚动、选中详情、属性总览、预览和应用/撤销行为。原装不占六款改造计数。

新三款使用 `pommel_hardened`、`pommel_runic`、`pommel_mana_orb`，避免与现有存档和星系三款 ID 冲突。每把剑采用相应尺寸、材质和真实连接件的透明实物图标；共同来源记录及共享图标回退保留。

当前三款星系配重的存档 ID 不变。六款均通过现有 `GunsmithSystem` 计算、保存并用于手持/预览/掉落外观；不新增专用数值分支。

六款目录、宿主适配、材质与图标已写入实际工程，2026-09-20 用户确认“达标了”。已有普通 Editor 构建成功记录为 `Saved/BuildEditor/build-20260920-132848.log`。本轮只进行整理与发布，没有追加游戏测试。
