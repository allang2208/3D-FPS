# 右下角武器名称与备弹栏扩展

2026-09-14 用户澄清：本次「武器详情栏」指右下角 HUD 的武器名称、弹药口径、弹匣及备弹，不是改造台。直接调整并接入，默认由用户测试。

## 布局与主题

- 从旧 184×38px 横排改成标准宽 360px、最小高 124px 的上下分区；长武器名和说明按整栏可用宽度换行，实际高度随内容增加，不使用 54px 名称列或省略号。
- 上方依次显示完整武器名 16px、口径／近战说明 12px；细分隔线下方为弹匣与备弹两列，标签 12px、数字 24px，保留精确数值与无限备弹符号。
- 使用现行冷钢 GlassTint、Border、Noto Sans SC 与 JetBrains Mono。外壳圆角 8px，内边距横向 14px、纵向 12px。
- 常规在右下角，距右侧与底部各 20px。宽度服从视口；与居中快捷栏横向冲突时，整体上移到快捷栏和体力条上方，保持至少 12px 间隙。位置由 HUD 使用同一坐标系计算，文字保持实际字号。

## 数据、输入与状态

只读现有装备实例名称、弹药类型，以及 Character 的当前弹数、备弹、容量、换弹和无限备弹状态。保留低弹、空弹匣、无装备和符文剑说明，打开背包仍隐藏。没有新操作、按键、模型或存档字段。

`ColdSteelAmmoReadout` 用 UMG 容器构建；现有 HUD 刷新负责数据与位置。没有新增事件绑定或定时器，控件仍为 HitTestInvisible，不接管焦点或鼠标。

## 文件和交付

修改 `ColdSteelAmmoReadout.h/.cpp`、`ColdSteelHUDWidget.cpp` 的创建与刷新入口，更新正式规则和历史文档入口。无需新增图片或更改改造台。完成必要 Editor 原生构建；未要求检查、截图或运行测试，交由用户测试。

## 当前交付状态

布局与数据展示代码已完成，正式 UI 规则已更新为 2.6。用户保存并关闭编辑器后完成必要 Editor 原生构建，结果 `Succeeded`，日志 `Saved/BuildEditor/build-20260914-144711.log`。未进行运行或视觉测试，由用户测试。
