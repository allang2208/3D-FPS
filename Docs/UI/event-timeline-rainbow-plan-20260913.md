# 顶部事件进度条彩虹配色

用户范围：仅替换顶部事件进度条的灰色渐变，接入原 game-dev 彩虹配色，其他 UI 不动。

来源：`E:/无尽轮回/长期备份/2026-7-13-1/game-dev/game-style.css` 的 `.world-invasion-bar`，以及 `src/ui/game-ui-manager.js` 的渐变首尾赋值。左到右为 #E5413E（0%）、#F1C13F（33%）、#418BE7（66%）、#3DC45B（100%），按 sRGB 通道插值。

实现范围：`Source/FPSGAME/UI/ColdSteelEventTimeline.cpp` 的现有 128×1 临时纹理生成。沿用 UMG 控件、纹理持有与释放方式，无新外部资产。宽窄窗口布局、展开与折叠尺寸、轨道、标记、字体、外壳及动画保持现状。

数据与状态：继续使用现有世界时间和天气数据刷新；无事件、筛选、详情弹层、焦点与输入处理保持现状，无玩法或存档写入。

交付：完成源码接入并进行必要 Editor 构建。未要求预览或测试，不启动应用，由用户自行测试。
