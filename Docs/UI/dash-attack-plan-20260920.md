# 冲刺攻击被动卡片

范围：现有技能抽屉的全部／被动页新增冲刺攻击，沿用 ColdSteelSkillPage 单列卡片、详情和返回焦点；窄窗用既有 440px 身份换行，内容归原滚动区，外壳与固定筛选不变。无新增弹窗。按用户要求接入，不自动预览或测试。

主题：ColdSteelUIStyle；名称、正文沿用原卡的 Noto Sans SC，数值用 JetBrains Mono。图标复用原 game-dev/assets/skills/冲刺攻击.png，迁入 ColdSteelData/Skills/dash_attack.png；缺图时保留名称与数值。

数据：DashAttackStats 提供当前／下级倍率、伤害、准备秒数、体力、60°扇区、范围与击退；MasteryProgress 提供等级、经验，统一档案事务升级与保存。未持近战时伤害显示“需装备近战武器”，不显示假伤害；体力、准备与增量仍可查看。

输入：被动卡仅点击详情，不参加快捷栏拖放。连续有效前进奔跑就绪后按左键触发；就绪提示复用战斗 HUD。命中/多目标/击杀奖励使用定义值，满级显示既有完成状态。返回恢复卡片焦点。开发面板技能目录加入同一 ID。

2026-09-20反馈修订：移除突进距离行，卡片类型由突进改为下劈；使用原有下劈起手，说明命中正前方60°（左右各30°），不再显示前冲／回弹能力。
