# 武器详情扩展与 gamedev 快捷栏效果迁移

2026-09-14：直接实现并接入 UE5。采用冷钢 UI 正式规则，默认由用户测试。

## 布局与内容

- 改造台右栏宽屏 560px、紧凑 500px；三栏布局要求实际主体宽至少 1240px、高至少 520px。更窄或更矮时上下排列，保留固定顶栏和底部操作。
- 当前配件详情正文至少 280px，并随说明及实值自然增高。整枪总览至少 540px，按完整表体期望高度继续伸展；右栏整体滚动，比较按钮、四列表头、全部属性与颜色图例保留。上下排列时右栏至少 820px。
- HUD 保留 Q／E／X／1–4 七个 48px 方槽、8px 间距和当前主题。技能图标在 48px 区域等比居中，物品在 40px 区域等比居中；按键位于右下角，数量位于顶部中央，状态及 CD 数字居中。

## 原项目依据与动画

只读参考 `E:/无尽轮回/长期备份/2026-7-13-1/game-dev`：`game-style.css:60–74,611–624`、`ui/panel-theme-backpack.css:1513–1555`、`src/ui/quick-bar.js` 的槽位构造和冷却呈现。

- 按键 1.2 秒循环，透明度 1 → 0.4 → 1，采用原 CSS ease-in-out 曲线。
- 冷却遮罩为 65% 黑色，顶部固定，高度为真实剩余／总 CD；用 50ms 线性高度过渡，与原项目一致。保留图标可辨识度，不再叠加原来的 35% 图标透明度和底部进度条。
- 同一绑定的真实 CD 从正数归零时闪白，起始透明度 0.8，按原 CSS ease-out 曲线在 0.6 秒内淡出；按键、数量位于闪光上层。
- 绑定变更、初次显示只建立当前 CD 基线，不伪造冷却完成。图标缺失显示中性问号，空槽只显示键位；缺蓝、体力不足、物品耗尽使用现有可用性状态。

## 数据与输入

继续使用 `ColdSteelStatusModel::QuickBinding/ResolveQuickItem`、技能定义图标、物品 `ue_icon`、`FPSFireballComponent` 与物品实例的真实冷却。冷却秒数显示一位小数。视觉只插值遮罩，不创建业务冷却计时器，不改变消耗、保存、技能释放或 E 键交互优先级。现有快捷槽交换、解绑及拖动图标沿用原入口。

## 文件与交付

修改 `M4GunsmithLayout`、`M4GunsmithSelectedDetails`、`M4GunsmithWidget.h`、`ColdSteelQuickSlot`、`ColdSteelFireballHUD`、`ColdSteelHUDWidget::MakeHotbarSlot`。新增专用冷却遮罩和动画曲线实现；复用现有图标及工程字体，不新增图片资产。

完成必要 Editor 原生构建；未要求检查、测试、截图或启动编辑器，由用户自行测试。

## 本次交付

上述布局、图标及效果已接入源码，正式 UI 规则更新为 2.5。必要 Editor 构建完成：`Saved/BuildEditor/build-20260914-130804.log`，结果 `Succeeded`。未进行运行、画面、拖放或冷却测试，由用户测试。
