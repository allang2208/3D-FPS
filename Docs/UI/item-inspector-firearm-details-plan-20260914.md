# 枪械浮窗核心属性、攻击公式与改造分组

用户于 2026-09-14 要求直接调整现有装备浮窗。

- 结构：枪械核心区两列三行，依次为枪械伤害、攻击间隔、弹容量、有效射程、正常换弹、空仓换弹；窄窗沿用单列。其他物品维持原摘要数量。固定头部、页脚、正文滚动和浮窗限位沿用当前冷白鉴定卡。
- 详细参数：用“攻击力计算公式”和“强化后攻击力公式”替换枪械“武器基础伤害”，显示真实系数、当前属性、强化等级和公式代入结果；解释公式结果与包含改造、精通、附魔的最终枪械伤害的区别。系数保留足够精度，不把 0.045 显示成 0.05。
- 数据：核心数值沿用 `ColdSteelWeaponStats` 和枪匠组合参数；公式读取强化系统当前选用的配置，属性取值与实战共用只读入口，计算使用 `CoreCombatFormula::Weapon`。不改变配置、计算顺序、伤害平衡、保存或装备状态。
- 改造与附魔：分区标题、改造件名称、逐条效果建立层级；每件改造末尾绘制浅灰虚线，合计效果独立显示。长公式和说明采用全宽纵排，效果保留收益／代价颜色。
- 实现：UMG 原浮窗管理生命周期，静态 Slate 虚线由 `UNativeWidgetHost` 托管；沿用模型刷新、结构签名和文本更新，不新增轮询。鼠标、键盘、展开、焦点和滚动状态沿用原逻辑。
- 范围：Tooltip 数据／摘要／布局／结构签名、强化系统只读公式访问器和相关规则；无新图像资产。完成必要原生构建，未要求运行游戏或测试，由用户测试。

实施状态：六项核心数值、两种攻击公式及当前属性代入、改造件虚线和分组排版已完成，正式规则更新为 2.10。用户关闭编辑器并要求继续后，`Build-Editor.ps1` 完成 `FPSGAMEEditor Win64 Development` 构建，结果 `Succeeded`；日志 `Saved/BuildEditor/build-20260914-225808.log`。本次浮窗及公式访问器编译完成，其他既有文件仍有废弃 API 和浮点转换警告。本轮未运行游戏或测试，实际排版与交互由用户测试。
