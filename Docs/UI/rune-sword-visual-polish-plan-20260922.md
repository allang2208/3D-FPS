# 符文长剑改造显示修订

范围：修补护手附近原生符文遗漏、改善符文长剑改造预览照明、重制该武器的 24 张选项图及 5 张分类图。使用当前五栏顺序、卡片、选择/草稿/应用/取消事务及窗口响应布局；不增加栏目、不改属性或存档。

预览继续使用既有 Slate 图像与 UMG 宿主、独立 FPreviewScene、颜色/覆盖率双捕获和纹理驻留流程。符文长剑使用中性棚拍补光，随工作台预览场景一起释放；拖动旋转、滚轮缩放、关闭焦点归还均保留。其他武器沿用当前照明。

图标读取现行 melee-gunsmith、rune-sword-modules 和共享配重目录，按武器专属 PNG 优先规则接入。24 项含原装与现有选项；剑身Ⅰ三个数值选项继续复用实装原刃外形。图标从当前部件作者源渲染，剑尖/朝刃安装端在左、原剑宽轴朝上，正交水平侧视，不做二维镜像。1024 RGBA、透明背景、无字框、单件、中性棚光、约 83% 占幅，保留当前材质分区。

新增制作目录：SourceAssets/RuneSwordVisualPolish20260922。该目录保留目标文件基线、替换前副本、模型/方向/材质来源、可编辑场景、最终 PNG 和部署/导入回执。更新 PNG 与同名 UE Texture；图标缓存沿用当前关闭清理逻辑。

用户请求的符文排查和图标制作在本轮范围内；不追加启动游戏、全项目回归或自动实机验收。
