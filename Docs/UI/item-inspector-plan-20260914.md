# 装备浮窗：摘要、详情和实值对比

2026-09-14 用户授权按本次六项审计建议直接修改。保留白色鉴定卡主题，替换旧三卡横排和所有文本禁止换行的规则。

## 布局、状态和输入

- 悬停同一物品 0.2 秒显示约 400px 宽核心摘要，锚点固定在首次停留位置；离开、起拖、关闭或切页取消。点击／F1 立即固定为约 520px 宽详情。
- 同一白色外壳包含固定头部（动态图标、完整名称、稀有度／等级／强化、装备位置）、纵向滚动主体、固定操作提示。名称和说明按可用宽度换行；宽高均受视口留边限制，不缩小整张画布。
- 摘要优先伤害、防御、攻击间隔、换弹、容量、开镜；详情包含同槽对比及分组展开的附魔、改造、物品资料和复杂参数。仅固定状态显示关闭按钮，标明 Esc 关闭／方向键滚动；保留原双击使用、右键穿戴及拖放。

## 数据合同

- 提取共享伤害／攻击间隔／换弹修正函数，角色原生计算与浮窗共用，覆盖强化、附魔、步枪／手枪精通、敏捷和巧手；保留现有武器基础值及计算顺序。
- 明确区分武器基础值与当前角色实值；对比优先当前武器组的相容槽，否则按真实可穿戴槽选择装备，并显示目标槽和名称。空槽、自身、不可比较字段不伪造差值；消耗品不做装备对比。
- 枪械图标复用 `ColdSteelWeaponIcons` 缓存和完成事件，目录图回退，失败显示中性占位。弹药名称使用公共映射。
- 模型更新时计算新展示快照，结构不变只更新文字与颜色；保留展开和滚动状态。图标单独更新；窗口 DPI／尺寸改变重新排版并保留阅读位置。未改变消耗、保存、容量或装备事务。

## 实现及交付

范围：`ColdSteelItemTooltip*`、背包悬停入口、共享武器数值修正、角色数值调用和弹药 HUD 映射；更新正式 UI 规则和技能引用。无需新增图片资产。完成必要 Editor 构建；不运行测试或截图，由用户测试。

已完成：六项修改接入共享浮窗，正式规则升级为 2.7，个人技能引用与工程镜像同步。饰品对比显示物品标注属性及缺失维度的增减；不会把这些字段称为完整角色换装预测。

必要构建：`Tools/Build/Build-Editor.ps1` 构建 `FPSGAMEEditor Win64 Development` 成功，最终日志为 `Saved/BuildEditor/build-20260914-160205.log`。没有启动编辑器／游戏、运行回归、静态检查、截图或渲染；显示效果、键鼠操作和实际游戏数值由用户测试。

## 三角入口与脉冲调整

用户反馈单独给原箭头染色不够明显，要求未展开使用实心三角并增加脉冲闪光。保留 UMG 展开区的整行点击和原焦点行为，以只绘制、不命中输入的 Slate 三角替换默认箭头；固定 24px 图标区域，实心三角约 14px 高。收起时向右，每 1.8 秒一次亮峰和柔和外晕；展开后向下且停止脉冲，不改变标题／内容透明度或布局。展开状态通过原事件同步，隐藏后停止绘制，重建释放旧 Slate 资源。颜色备选为深琥珀金、深青蓝、铜橙。

用户选择深青蓝：基础 `#176C86`，闪光 `#57BCD5`，轮廓 `#123D4B`。实现位于 `ColdSteelDisclosureIndicator`，由浮窗章节创建和展开事件驱动；色值来自共享冷钢主题。此调整仍由用户测试。

本次必要 Editor 构建成功，日志 `Saved/BuildEditor/build-20260914-210456.log`；未运行游戏、截图或测试。
