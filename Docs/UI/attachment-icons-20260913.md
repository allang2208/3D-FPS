# 改造卡片独立图标

2026-09-13：使用内置 image_gen 逐件生成 21 张图标，覆盖当前六个配件槽位的改造选项与原厂／无配件选项。未通过 CLI 指定模型，不将本轮生成描述为显式指定 GPT Image 2。

图片目录：`Content/ColdSteelData/AttachmentIcons20260913`。原始生成路径、主题和对应文件名记录于同目录 `provenance.json`，保留原图；深色背景不是透明 alpha。

沿用银灰拉丝、石墨凹槽、左上柔光风格。按槽位和配件 ID 使用独立图标，同 ID 的跨枪配件共用该配件示意图；不是各枪型模型的实机截图。原厂图表示对应配件类型，无前握把使用空导轨示意。

卡片继续使用 264×142 布局和 64×64 图标区。加载后在控件内缓存画刷与纹理，UObject 持有纹理；UFS 打包配置随图标目录接入。只有实际已安装改造件显示荧光绿状态与呼吸光效，待应用状态仍明确区分。

执行必要 Editor 构建；未运行游戏、截图或视觉验收，由用户测试。
