# 冷白装备鉴定卡实施

用户确认 概念预览（本机 `Docs/UI/References/item-inspector-cold-white-concept-20260914.png`，图片未随源码发布），授权按建议优化实际浮窗。图片中的属性和背包背景仅作排版示意；实现仍读取真实物品、角色和装备对比。

- 外壳冷白 #EEF0F2、标题区浅灰 #E2E6E9、石墨文字 #252A2E、次字 #697278、细分隔 #CDD2D5；沿用 Noto Sans SC／JetBrains Mono、8px 圆角和深青蓝箭头。
- 固定头部：枪械／长武器使用横向图片区，其他物品方形；名称 20px，类型／品质／强化和位置 12px。主体独立滚动，页脚固定。
- 首屏核心属性最多四项，常规两列，极窄窗口一列；标签 14px、值 24px。正文仍为 14px，仅数字与单位使用等宽字体。常规详情宽 520px，摘要宽 400px。
- 同槽对比默认显示三项相关指标的差值，完整前后值保留在详细参数中；空槽和自身装备使用明确状态文字。
- 折叠栏目合并为改造与附魔、详细参数、物品说明，保留全部原始详情、说明和来源。各分区使用细线与统一间距，不堆叠卡片。
- 首次打开固定详情，仅第一个收起栏目提示两轮脉冲，随后悬停／键盘焦点所在栏目脉冲；展开后静止。保留实心三角，减小光晕。
- 固定详情首次确定位置后保持左上角；展开内容在剩余视口内滚动，视口变化时限位。读取状态、展开状态、滚动和关闭焦点沿用现有流程。

范围为浮窗展示、专用主题和章节指示器；库存、伤害计算、保存和背景背包布局沿用现有代码。完成必要构建，不主动测试、截图或运行游戏，由用户测试。

## 交付记录

布局、真实数据展示、章节脉冲与固定位置已接入，正式规则升级为 2.8，个人技能引用与工程镜像同步。必要构建成功：`Saved/BuildEditor/build-20260914-212642.log`。构建期间对 `Skills/ColdSteelSkillModel.cpp` 的通知定义集合补充了明确的 const 指针数组类型，解决混合 const／非 const 指针的推导错误，保留原有技能条目和通知逻辑。

未运行游戏测试、截图或视觉验收；实际效果由用户确认。概念图是选定设计依据，不作为本次实机截图。
