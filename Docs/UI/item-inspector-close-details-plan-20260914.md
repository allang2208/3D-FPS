# 浮窗关闭按钮对齐与完整参数

用户授权修正关闭 X 与背景偏心，并在详细参数中完整列出核心区数值，按实际用途补充参数。

- 关闭按钮：沿用 24px 方形按钮和冷白悬停／按下颜色，清除样式及内容槽内边距；X 以两条对称矢量线在同一按钮几何中心绘制，避免字形基线造成偏心。继续使用原 UButton 点击、Esc、焦点归还和销毁入口。
- 信息结构：“详细参数”首先列出“核心参数”，按核心区同样顺序、标签、单位和数据展示全部数值；再显示物品信息、两条紧凑攻击公式及其他武器参数，最后完整装备对比。重复的同值行仅在后续列表省略，核心参数块始终完整。
- 补充数据：枪械增加理论射速（按实际攻击间隔折算每分钟发数）；剑增加普通挥砍距离、每次格挡体力，保留突刺时间并统一秒单位。最大攻击距离仍含突刺。保留此前删除操作教程的决定，不重新加入连击／按键／重击操作说明。
- 实现：UMG 管理布局和交互，NativeWidgetHost 只承载静态 X 绘制；详细核心行直接绑定已有 Summary，普通数据刷新继续更新文本并保留展开／滚动位置。固定头尾、视口限位、窄窗自然换行和主题保持现有规则；无新资源或存档变更。
- 范围：Tooltip Layout、Data、Summary 和对应规则文档；完成必要 Editor 构建。未要求游戏测试或截图，由用户自行测试。

实施状态：源码和规则 2.13 已修改。最初构建因编辑器运行被阻止；后续近战右下角数据任务中已完成必要构建，`Build-Editor.ps1` 返回 `Target is up to date` 和 `Result: Succeeded`，日志 `Saved/BuildEditor/build-20260914-234631.log`。未运行游戏或测试，由用户测试。
