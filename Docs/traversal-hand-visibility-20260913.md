# 攀爬与翻越的手臂显示交接

用户反馈攀爬、翻越时仍能看到左手并产生视觉穿模。本次读取现有动作配置、作者时序和历史第一人称帧后，修改两套手臂的显示交接；没有制作新动作或改动源骨骼关键帧。

原逻辑在源时间 0.06 秒显示攀爬手臂，但持枪手臂到 0.12 秒才完成收起；结束时又按收枪插值阈值提前恢复武器。两套手臂在交接阶段可能同时可见。此外，IsWeaponBusy 包含攀爬，误触发了用于换弹的居中构图。

- 收枪在攀爬手臂出现前完成；攀爬手臂收回后，武器才从镜头下方恢复。可见时段使用同一源时间边界，翻越的 2 倍播放倍率继续统一作用于整个过程。
- 武器与其附件在攀爬手臂显示期间使用临时 HiddenInGame 控制，避免附件自己的 SetVisibility 更新重新显示部件。记录原隐藏状态，正常结束和取消时恢复。
- 收枪位移使用相机坐标，避免各枪网格朝向改变退出方向。
- 攀爬不再触发换弹用的持枪居中构图。保留原抓墙左手、接触 IK、胶囊路线、镜头限制以及 Vault / Mantle / Climb 时长。

本次未运行测试、验收、截图或新预览。视觉效果由用户实机测试；历史截图仅作为原动作参考，不能代表本次修改效果。

必要构建：Editor Development 编译完成，模块为 `UnrealEditor-FPSGAME-9132237.dll`，日志位于 `Saved/TraversalHandVisibility-ModuleBuild-20260913.log`。普通构建因打开的编辑器占用旧模块而停止，随后使用独立编号完成游戏模块及相关插件的构建；没有关闭或重新启动用户的编辑器。用户保存工作并重启编辑器后再试本次修改。

## 用户截图补充：爬墙收尾的袖口截面

用户随后明确指出，异常是爬墙完成阶段左下角出现攀爬手臂的内侧／截面。前述持枪手臂互斥修正只处理两套手臂重叠，没有覆盖这次截图里的问题。

原收尾同时执行源动画 Release 至 ClipDuration 的加速采样、整套手臂向终点的 EndCorrection、镜头回归以及世界 -Z 下移。第三人称起身尾段可能把肩膀带入第一人称视野；下看时，世界向下也不等于退出画面下缘。

针对这一表现，运行时改为在原 Release 姿态结束源动作采样；接触 IK 在 Release 时归零，随后使用独立的 0.12 秒源时间收手段。已松手的姿态随镜头回归的平移携带，再沿镜头前方 12 cm、下方 50 cm 平滑退出。移除 EndCorrection 对手臂的终点搬移，镜头独立回到原站立目标。收手位移不反向影响 head 参考镜头和观察目标，自由观察与持枪仍在收手之后恢复。

保持原动作的抓墙、撑起、Release 时间、执行总时长和胶囊路线；没有永久隐藏左手或修改全局近裁剪面。此次以用户截图和代码定位问题，未启动游戏、生成截图、渲染或运行测试，视觉修正效果交由用户确认。

本次截图修正的必要构建已完成：Editor Development 模块 `UnrealEditor-FPSGAME-9132238.dll`，退出码 0；日志 `Saved/TraversalSleeveExit-ModuleBuild-20260913.log`。这只表示编译完成，未进行实机视觉确认。用户保存工作并重启编辑器后可试用。
