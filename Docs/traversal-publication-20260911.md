# 攀爬源码整理与发布（2026-09-11）

发布根为 `D:/FPS3D/FPSGAME`，目标为 `https://github.com/allang2208/3D-FPS.git` 的 `main`。依据 AGENTS、WORKFLOW 第 8 节与 AssetSetup，发布本任务攀爬执行器、角色输入/武器占用接入、双向滚轮切枪及攀爬期间的切换限制、测试/作者工具、参数记录和技能。角色、配置等共享文件仅暂存必要片段，保留其他任务的工作区改动。

## 归档

无效的未注册组件编辑器探测脚本由真实输入的 CourseAudit 取代，三份旧 Blender 自动备份由已验证的同名 .blend 取代。四文件共 146049947 字节移入本机 `trash/traversal-publication-20260911`；路径、替代物和 SHA-256 见 [归档清单](traversal-archive-20260911.json)。保留正式作者源、官方参考、旧失败日志和最终验收记录，它们仍有恢复或追溯用途。

## 发布边界

二进制动画、模型、地图和参考图保持本机，不公开上传；[恢复说明](AssetSetup.md) 列出内容与作者输入依赖。运动参考采样及官方资产完整盘点也留在本机。Git 中的作者摘要不等于完整资源备份。

本机包含独立 AKM 集成；发布核心没有把该并行功能一起上传。运行测试缺少 AKM 目录定义时使用第二把 M4，测试双栏切换/攀爬合同；完整宿主既有 AKM 测试记录不冒充精确发布快照的素材验收。

## 技能

新增第一人称攀爬接触与镜头交接参考，个人技能与工程镜像同步。手臂技能提供入口，C++ 与调试技能链接到同一参考，记录墙面坐标、法线距离、阶段交接、控制器角度约束、有体积的表面检测、保持骨长的 IK 和有效验证范围。

## 验证

此前完整宿主：94 项规则/碰撞检查、10 组运行用例通过；这是运行功能证据。此次另在 Saved 下导出精确暂存源码做独立编译与回归，验证其不依赖并行未提交代码；该目录仅是短期验证快照，提交和推送仍在主仓库完成。

精确发布快照验证完成：Editor Development 全量编译成功；`Saved/TraversalPublication20260911/rules.log` 规则 39 项、物理场景 55 项零失败；`runtime.log` 六组零失败、退出码 0，使用已发布目录中的两把 M4。Python 作者工具语法、三个技能入口格式、暂存空白差异和凭据模式扫描通过。源码/配置与被验证快照逐文件比对一致。

临时快照的源码、配置、工具和说明共 537 文件已移到 `trash/traversal-publication-20260911/validation-source`，逐文件 SHA-256 读回一致，清单位于该目录的 `archive-manifest.json`。不删除内容的归档方式保留了 Saved 下的生成缓存和素材链接；那里不再留有可开发的项目源码副本。
