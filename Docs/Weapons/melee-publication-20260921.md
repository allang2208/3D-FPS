# 近战旋风、冲刺攻击与战术持剑整理

2026-09-21。按用户授权整理本次近战迭代、更新技能并发布至 `allang2208/3D-FPS` 的 `main`；不包含其他在途的枪械、怪物、魔法、库存和建筑改动。

## 当前版本

- 大旋风：V5 的旋转发力与接触反馈，V6 在既有后摇窗内外展武器再回待机。运行路径仍为两种握柄的 `A_RuneSword_WhirlwindV5`，V6 写回该对象；当前后摇由元数据与导入记录识别。
- 黄褐色边框：背景模糊只在实际 shader map 完整且强度非零时加入镜头，避免默认后处理替代材质绘制边框。UV边缘保护继续保留。细节见 [边框定位](../../SourceAssets/Whirlwind20260920/BorderFix/README.md)。
- 冲刺技能：有效奔跑就绪后左键，0.25秒前摇后进入前方60°扇区，无技能附加前冲或回弹。准备、成本、伤害、修炼、存档、详情和HUD接入；群怪查询复用可穿过Pawn的遮挡忽略列表。
- 战术持剑：准备满格后0.5秒进入右后高位持剑，低左肘避让；剑身与镜头共用脚步相位。V4保留两种握柄的进入／循环／退出共六条，V5负责两条 `SprintOverhead`。V4重新制作／导入已限定六条移动动画，避免覆盖V5前摇。

## 归档与保留

90个文件、1,085,487,984字节移入本机 `trash/melee-sprint-retired-20260921`：V1/V2/V3被替代的动画输出、V4旧瞬发攻击输出、临时编译产物和修改前源码快照。逐文件记录原路径、目标、大小、SHA-256、原因与替代物，移动后散列读回一致，见 [归档清单](melee-publication-20260921-archive.json)。目录中的研究、作者方法、输入与历史回执保留，旧输出位置已补迁移说明。

保留以下实际依赖，不按版本号清理：

- 旋风 `ImpactV5/Whirlwind_Manny_Editable.blend` 与 `whirlwind_motion.py`，RecoverV6直接读取它们；V4为已接受基线，V2/V3仍提供背景／前景材质制作。
- 当前冲刺 V4/V5 的 Blend、FBX和密集骨轨道；V4 Blend含历史攻击 action，但同文件内三条移动动作仍是有效可编辑源。
- Manny握持源、ChargedErgoV43、共享双臂求解器、RuneSwordElbowRepair坐标标定、PickaxeOverhead母版与标准／长柄输入。
- UE中的实际运行对象、V6制作对象及其旋转前缀备份。此次没有移动UE包或调整运行资产路径。

## 公开与本机恢复边界

公开原创C++、技能数据、作者／导入脚本、动作参数、开发说明、归档散列及SKILL方法。作者脚本引用本机授权素材，克隆源码不能恢复完整可运行外观。

UE包、Manny与授权剑模型、Blend/FBX、密集逐帧姿态、音频、图标、参考视频和抽帧继续保留在本机。B站指定片段只作动作研究，不进入游戏或公开仓库；带时效令牌的视频播放地址JSON也不发布。图标继续从用户合法原项目恢复。

恢复顺序：先恢复原近战骨架／材质／声音和现有下劈，再接入旋风V5、应用V6尾段，恢复V2/V3后处理及边框修订；冲刺导入V4六条移动动画，再导入V5两条前摇攻击动画。作者入口分别在对应 `SourceAssets` README，保存结果留在本机回执中。

## 技能与构建边界

手臂技能新增 [战术冲刺](../../skills/ue5-fps-arms-animation/references/tactical-sprint-melee.md)，旋转章节补充接触时钟、外展收势及默认后处理边框；技能／魔法新增 [冲刺攻击](../../skills/ue5-skill-magic-workflow/references/dash-attack.md)。个人技能和工程镜像的对应内容同步维护。

V4此前完成正式Editor构建；V5前摇此前通过Live Coding应用于当前编辑器，两种动画已保存。本次整理更新了技能详情中的0.25秒文字，未重新构建或启动游戏。发布前仅执行本次仓库范围、暂存差异、文本／脚本、许可／敏感信息和归档完整性检查，不据此宣称运行效果已通过。
