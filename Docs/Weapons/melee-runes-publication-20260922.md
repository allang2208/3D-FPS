# 近战武器与符文源码发布 · 2026-09-22

本次发布范围：符文长剑原生金色符文与剑根补色、改造预览灯光与语义图标、寒晶自带侵蚀及专属精神迸发、两把旧剑的剑身Ⅰ外观、高地双手剑及阔锋重刃 V2／蛮荒符文 V2／裂角护手，以及冲刺攻击释放前进 100 cm。最新材质为平滑明灭版本；金色强化只允许符文长剑，旧存档与预览一并归一化。

## 发布与本机资源边界

这是源码、目录数据和作者配方发布，不是完整可运行资源包。Meshy 原始下载、付费／第三方武器和手臂、派生 Blend／FBX／UE 资产、贴图、密集几何与动画采样、账户及任务响应、构建和安装回执都留在本机。Git 不提供这些资源的再分发许可。

高地双手剑来源为用户选定的概念图，通过用户明确指定的 Meshy 管线生成；选定参考保存在 `SourceAssets/HighlandClaymoreMeshy20260922/References/user-selected.png`。概念阶段的造型参考来源记录在该批次概念 brief 中；此引用不表示 Meshy 或其他源资产获得公开再分发授权。图标及蛮荒纹样由内置 imagegen 制作，提示词随作者配方保存。恢复时须使用具备相应授权的本机源文件。

共享代码采用逐块提交。怪物命中形式／抗性重构、枪械与配重锤平衡、自动存档、背包、地下城和性能修改保留在工作区。本次削韧倍率接入仓库现有的 Poise 累积入口，物理穿透使用现有伤害接口；不把其他任务的新怪物体系作为发布依赖。

## 当前资源与恢复顺序

1. 先恢复已有符文长剑、寒晶及其模块、PBR、共享近战手臂和动作来源。保留实际源授权及对应本机目录。
2. 原生金色链：`NativeRuneGold20260922` → `RuneSwordVisualPolish20260922`。后者 `Before/native_ink_mask.png` 仍是重建输入，没有归档。剑根属于护手模块，必须恢复两张 UV 遮罩；不要运行已退役的 RuneGoldMaterialCommandlet 文本注入方案。
3. 剑身Ⅱ图标最终使用 `RuneSwordSemanticIcons20260922`；其他部位保留 VisualPolish 实物图标。材质基础配色来自 `RuneSwordSemanticGlow20260922`。
4. 寒晶链：`FrostSpiritBurst20260922` → `FrostSpiritBlend20260922`。自带侵蚀为智力×10%＋精神×10%；精神迸发只把此自带附伤翻倍，不放大所有装备附魔。旧侵蚀改造迁移为精神迸发。
5. 剑身Ⅰ六款旧剑变体来自 `SwordBladeVariants20260922`。
6. 高地基础模型与模块按 `HighlandClaymoreMeshy20260922/Integration/README.md` 恢复。阔锋重刃 V2 依赖 `Broadblade20260922` 的可编辑 V1 输入，再运行 `BroadbladeThicknessV2_20260922`；不能因版本旧就删去 V1。
7. 蛮荒初版目录 `WildRune20260922` 只作为材质引导与图标输入；随后必须应用 `WildRuneTotemV2_20260922`。最终为非重复兽颚、错落爪痕与骨矛赤红战纹，攻击韧性伤害×1.30、物理防御穿透＋20%。
8. 裂角护手来自 `ClovenGuard20260922`：格挡体力消耗－10%；成功弹反后 4 秒内下一次普通攻击替换为瞬发重击，物理伤害×1.25、韧性伤害×1.40。最多一次、弹反刷新、发起消耗，空挥也消耗。
9. 最后应用 `MeleeRuneFade20260922/install_materials.py`，防止旧作者配方覆盖最新渐隐。最终材质为 `M_SilverRuneSurfaceV2`、`M_SilverRuneSurface_FrostSpirit`、`M_AzureRunesword_NativeGold`；蛮荒继续使用 V2 专属材质。

所有 UE 导入与保存仍通过项目现有互斥桥执行，不主动结束他人编辑器。作者脚本中的宿主路径为本机制作位置；其他机器恢复时先配置相应源路径。历史 `Before` 回执指向已归档快照，按下面清单找回，不当作当前资源入口。

## 归档与经验

152 个已退役文件移入本机 `trash/melee-runes-publication-20260922`，逐项记录原路径、目标、大小、SHA-256、理由和替代物，移动后已读回散列。包含被否决的字形主体图标、未选概念、旧回滚副本、可替代 Blender 自动备份及初代金色注入命令行工具。详见 [归档清单](melee-runes-archive-20260922.json)。

可复用规则沉淀在 [剑身符文 SKILL 参考](../../skills/ue5-weapon-workflow/references/rune-surfaces.md)，包括真实 UV 定位、模块边界相位、保色渐隐、专属选项与旧存档、附伤与削韧通道、语义图标和预览照明；个人技能与仓库镜像同步。

## 状态边界

既有安装与构建结果属于各制作阶段的历史记录。用户认可蛮荒 V2 的明灭方向，不能据此推断本轮全部材质获得验收。最近一次渐隐后原生构建曾被无关的 `AWitchRebuiltMonster::ConfigureDistanceLODs` 链接缺失阻塞（本机 `Saved/BuildEditor/build-20260922-182842.log`）。本次仅执行用户要求的归档和推送范围检查，不启动 PIE、不重跑原生构建或视觉验收；游戏效果由用户测试。

推送前已检查精确暂存范围、敏感字符串、文件大小、52 份 Python 源码语法、26 份 JSON、文档本地链接、技能双份一致性与 152 项归档散列；`git diff --cached --check` 无错误。以上是发布检查，不是游戏或构建验收。
