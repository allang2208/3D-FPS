# 寒晶·双手剑接入

用户指定 Meshy ZIP，确认使用名称「寒晶·双手剑」，沿用现有双手剑规则。

## 已完成

- 独立物品 `ue_frost_crystal_sword`，新增模型、原始四张 PBR 材质，复用项目当前 Manny 手臂和双手剑动作。
- 剑柄按既有双手接触区域适配；保留模型来源、完整可编辑 Blend、静态和骨骼 FBX。
- 起始仓库一次性发放；现有背包、装备主手组/副手占用、快捷栏、仓库转移、丢弃拾取及保存继续使用统一物品实例。
- 将原先只识别符文剑的运行组件、近战体力显示、物品参数、图标预览和掉落模型入口扩展为近战/双手剑类型识别，近战物品不写入枪械弹药状态。
- 使用与原双手剑相同的伤害公式、属性成长、攻击时序、连击、蓄力、突刺、格挡、剑气和声音；加入资源烘焙目录。

## 使用

重新启动工程并进入游戏，在仓库取出「寒晶·双手剑」并装备至主手。空间不足时先腾出一块 2 × 4 区域，再重新进入游戏领取。

## 状态与作者入口

UE 资源导入、FPSGAMEEditor 原生构建完成。导入器自动重建了 FBX bind pose，详细信息见制作日志；未运行游戏测试、静态检查或验收渲染，由用户自行测试。

[完整作者源、导入入口、来源与限制](../../SourceAssets/MeshyMelee20260915/README.md)

## 模块化接入（2026-09-15）

当前寒晶剑使用 `Modules20260915` 下的独立剑刃、护手、握把、配重锤，第一人称使用保留原骨架的 `SK_FrostSword_Arms` 作为动画载体。护手四款按原改造 ID 替换；剑身Ⅱ符文只附着独立剑刃。改造台、图标、掉落共用模块目录，存档仍保留原物品实例与改造 ID。

原整剑资源保留为作者来源。新部件按共用接口制作，不再需要为每种组合重导完整手臂和整剑。详见 [模块源文件、安装接口与新增部件流程](../../SourceAssets/FrostSwordModules20260915/README.md)。本轮导入与构建完成，未运行游戏或视觉验收，由用户测试。
