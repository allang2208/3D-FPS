# 攀爬输入与审计修复发布（2026-09-12）

实际 Git 根目录为 `D:/FPS3D/FPSGAME`，目标为 `https://github.com/allang2208/3D-FPS.git` 的 `main`。发布前已读取当前 AGENTS、WORKFLOW 第 8 节及 AssetSetup，核对远端 URL/默认分支、fetch 和待推送历史；起始 HEAD 与 origin/main 均为 `80e7ecf06dbf9bdda6914d3737c4800b20959566`。

## 范围与归档

发布点按空格、空中接墙、镜头选墙、两倍翻越、村庄几何适配及五项审计修复；包括配置、C++、运行测试、规则说明和技能。角色源文件只暂存跳跃与镜头交接片段；配置只暂存 TraversalSettings，技能入口只暂存攀爬路由。其他枪械、滑铲、台阶与 UI 任务继续保留在工作区。完整宿主的 40 cm 自定义台阶组件不随此次发布；主线角色的既有台阶参数保持不变。

31 份已被验证实现替代的 Air/View/Fix 修复前源码备份，以及一份被实际 VillageAudit 替代的编辑器初探脚本，已移入本机 `trash/traversal-publication-20260912/`。精确路径、替代物、字节数及 SHA-256 见 [32 文件归档清单](traversal-archive-20260912.json)，移动后已读回校验。源动画、可编辑 Blend/FBX、官方参考、有效失败复现和最终日志保留；不能仅凭旧日期判为废案。

不上传原始/派生模型、动画、地图、截图、日志或 trash。[AssetSetup](AssetSetup.md) 中的第一人称攀爬恢复边界继续有效。本轮没有新增运行二进制资产依赖。

## 技能沉淀

个人与工程的 `ue5-fps-arms-animation/references/traversal-contact.md` 保持同步。除玩法与接触合同外，补入真正旋转斜台并读回角度、独立双手高度/法线、同条件比较取消镜头、多帧率速度与收敛、末帧故障注入及证据归档方法。旧 0.25 s 门槛明确标为历史，入口可按点按、空中接墙、镜头选墙和强控中断查找。

## 验证依据

完整宿主昨天的玩法验证见 [修复记录](traversal-audit-fixes-20260911.md)：47 项规则、80 项碰撞断言通过；30/60/120 Hz 各 7 组边界、11 组空中、6 组真实斜台/地面和 2 处村庄石墙通过。命令行规则进程原有配置错误单独记录，不称为干净启动。

本次另在 Saved 下导出精确暂存树，独立编译与验证，提交和推送仍在主仓库完成。验证使用临时资源目录链接；源码、配置及物品 JSON 来自暂存树，不复制未提交源码。攀爬验收入口只保留其独立运行模式；历史 Slide 便捷入口依赖另一任务的验收实现，已从本工具移除。

发布快照实测：Editor Development 全量编译成功（127 个构建动作）；规则 47 项、碰撞 80 项全部通过，规则进程仍因既有 GameFeatureData 配置错误退出 1。Boundary 7 组、Air 11 组、Surface 6 组均通过，三个游戏进程退出 0，包含真正倾斜的接触面、角色输入以及两把主线 M4 的切换回归。

整理期间其他任务已发布天气及握姿标准。最终基于 `f8b14bc23508c60596533faad90ad5f73d874476` 使用独立索引提交这 29 个攀爬文件；共享暂存区的其他任务内容保持原状。逐文件比对确认发布源码/配置与已测快照一致，且没有额外未跟踪源码参与编译；最新主线增量构建复用相同对象，仅重新链接成功。相关记录位于本机 `Saved/TraversalPublication20260912/{build.log,build-latest-main.log,preflight.json}`。

完成验证后，临时源码/配置/工具及三份临时暂存输入共 1133 文件、22794065 字节已移入 `trash/traversal-publication-20260912/{validation-source,partial-stage-inputs}`，逐文件 SHA-256 读回一致。完整清单保留在该目录的 `validation-source-manifest.json`；Saved 下只留下验证日志、生成缓存和资源链接，不留另一套可直接开发的项目源码。历史源码归档与主仓库的工作文件分开，原工程 Content 未移动。
