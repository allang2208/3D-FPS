# 快速进战技能接入（2026-09-17）

## 需求与范围

- 占位接入：新增技能「快速进战」，触发快捷键 F；原 F 键的武器检视改绑 L（第二轮已按 UI 分卷规则补齐卡片与拖动绑定）。
- 正式效果（同日用户定义）：对前方 2m 的单个单位造成伤害，伤害 = 25 + 技能等级×5 + 力量×（5 + 技能等级×0.1）（同日二次调整，初版 50 + 等级×10 被用户判定过高），击退 1m 并眩晕（2.5 + 技能等级×0.1）秒。按 F 且装备剑类武器时触发，动作取第四连击的配重锤打击；其他武器版本后续设计。

## 数值与公式口径

- 公式按用户原文「×技能等级」字面结算（伤害项 2026-09-17 二次下调为 25+5×等级）：1 级基础 30 伤害 / 2.6 秒眩晕，20 级基础 125 伤害 / 4.5 秒眩晕（力量项另计）。
- 力量取 `UColdSteelStatusModel::Attribute("str")` 当前总值（基础属性 + 装备 + 重击/机枪精通的力量加成合并）。
- 参数入口统一在 `Content/ColdSteelData/skills.json` 的 `quickCombat`：`damageBase/damagePerLevel/strengthFactorBase/strengthFactorPerLevel/knockbackCM/rangeCM/stunBase/stunPerLevel/cooldown`。

## 冷却（基础 12 秒，2026-09-17 第二次补充）

- 完全沿用火球/冰锥的「预留-结束起跳」合同：动作实际开始时 `CommitQuickCombatCast` 预留全额冷却（`bQuickCombatReserved`，预留期间不走表）；挥击结束 `FinishQuickCombatCast` 解除预留后 12 秒才开始走表；取消（死亡/换装/不可用走 `CancelAction`）与存档重载（abandoned 清理）同样解除预留并**保留**已提交的冷却。
- 门禁：`TriggerQuickCombat` 在 reserved 或剩余冷却 >0 时拒绝；开发面板「无能力冷却」调项照常生效。
- 存档字段 `QuickCombatCooldown/Duration/bQuickCombatReserved` 并入版本 11 合同（该版本今天才引入，未发布，无需再 bump）；`Validate` 校验有限性、时长 ≥ 剩余、≤300。
- 快捷槽接入冷却遮罩与数字读秒（`ColdSteelQuickSlot` quickCombat 分支）。

## 修炼（参考其他技能定值，2026-09-17 第二次补充）

- 释放技能 +1（`useExperience`，动作实际开始记，挥空也算；排队补发算一次）——对齐重击/闪避的使用口径。
- 使用技能击杀目标 +15（`killExperience`）：可修炼目标（有 MonsterCombatComponent、非尸体、非召唤/NoSkillTraining）被本次打击直接击杀时，挥击结束统一提交。参考系：冰锥每杀 12、重击首档（同次 2 杀）12、火球每杀 24；本技能为 12s 冷却的单目标必中打击+硬控，取 15 居中。
- 无命中/多目标档（单目标技能）；命中继续按剑类命中修炼剑精通与暴击（既有快照路径）。

## 已接入内容

- **数据与存档**：skills.json 正式数值；存档版本 10→11 自动补键；`FQuickCombatTuning`（ColdSteelSkillTypes.h）由 `ColdSteelSkills::LoadDefinition` 解析。
- **触发链**：F（`QuickCombat` ActionMapping）或快捷栏绑定 → `UColdSteelStatusModel::TriggerQuickCombat` → 限剑类（当前符文剑 `IsEquipped`）→ `URuneSwordComponent::BeginQuickCombatStrike`。
- **动作**：复用第四连击的 `PommelStrike` 剪辑与全部节奏/镜头/音效/ rift 表现（`bPommelAttack` 路径，天然单目标、前方窄走廊）。不推进普通连击计数（直接调 `StartSwing`，不经过 `BeginAttack`）。守卫/装备/收手等占用与普通攻击一致；施法占用左手时拒绝；挥击接触段结束后按 F 排队补一记（`bQueuedQuickCombat`，挥击结束处消费）。消耗一次普通攻击体力（沿用 `StartSwing` 既有扣减，与重击同口径）。
- **结算**：命中后 `SwingDamage` = 技能公式结果，经既有 `ColdSteelSkills::ApplyHit` 面板管线（同重击：金额按武器伤害面板比例分摊）；命中即 `UMonsterCombatComponent::ReceiveStun(Pawn, StunSeconds, 100cm)` 一次提交击退+眩晕，普通推退在该打击内关闭。范围 `SwingReach = max(武器 reach, 200cm)`。
- **眩晕**：新增 `ReceiveStun`（MonsterMeleeKnockback.cpp），镜像格挡反制流程（打断攻击 → Stagger 状态 → BeginReaction 控制闸 → 停止移动 → 立即推 20% + 0.16s 内推余下 80%），不带招架表现标记。覆盖 Wolf/Maggot/Nurse/HandBrain 四类 AI 怪；FatZombie/Mutant3 走独立 FPSCombatHealthComponent，与格挡/击退一致不受此组件管。
- **修炼**：动作实际开始时记 useExperience=1（挥空也算施放；排队补发算一次）；命中经既有快照继续修炼剑精通与暴击。升级提示由 `QueueProgressNotices` 自动发布（Detail 显示新等级打击伤害与眩晕秒数）。
- **UI**：技能页详情换正式效果行（触发键 F / 打击伤害含当前力量 / 力量系数 / 眩晕时间 / 击退距离，当前/下一级对照）；标签「剑技 / 打击 / 主动」；快捷栏拖动绑定保留。开发面板可升/满级。
- **图标**：1254×1254 六边银框系列占位图（以暴击图为基底，仅换中央双箭头符号），`SourceAssets/QuickCombat20260917/`，恢复脚本已登记。正式图标后续按同规则重制。

## 决策点（可再调）

- 体力：用户未指定，暂按近战动作合同消耗一次普通攻击体力；如需无消耗，改 `StartQuickCombatStrike` 即可。
- 公式字面按 ×等级（伤害项现为 25+5×等级，1 级基础 30 伤）；若再调公式只改 `QuickCombatStats` 与 skills.json 的 damageBase/damagePerLevel。
- 连发由 12 秒冷却与动作节奏限制；冷却中按键无效（快捷槽显示读秒遮罩）。

## 交付前自查修正（2026-09-17 第三轮）

- 快捷槽可用性变暗极性写反（原 `Dim=!Fraction` 会在就绪时变暗）：改为「无剑或冷却中才变暗」，并在未装备剑类时显示「需剑类」而非读秒。
- `EndPlay` 补冷却预留释放（与 `FinishHeavyTraining` 同口径），避免销毁路径把预留标志留给下次会话；存档加载的 abandoned 清理仍作为兜底。
- `StartSwing` 防御性复位 `bQuickCombatStrike`，确保任何进入普通挥击的路径都不会残留技能标记。
- `Migrate` 清理上一轮留下的不可达行（`P.SkillProgressVersion=10`）。
- 交叉核对 skills.json 键名与 `LoadDefinition` 解析键（damageBase/damagePerLevel/strengthFactorBase/strengthFactorPerLevel/knockbackCM/rangeCM/stunBase/stunPerLevel/cooldown/killExperience）逐项一致。

## 快捷栏 F 专属槽（2026-09-17 第三次补充）

底部快捷栏在数字 1–4 组后用同一细分隔线隔开，追加一枚 **F 固定槽**：与七槽混放槽共用 `UColdSteelQuickSlot`（同尺寸、同框、同图标路径、同冷却遮罩/读秒/就绪白光/键位呼吸闪动），格式完全统一。

- 槽位新增「固定技能模式」：`Configure(Owner, Index, FixedSkill)` 传入 `quickCombat` 后只读显示该技能——不读混放绑定、不可拖出、不收投放（四处拖放入口 + `NativeOnDrop/DragOver` 全部早退）；键位标注给定为 F。
- HUD 侧刻意**不**把该槽注册进 `QuickSlotSurfaces`（`QuickBarDropIndex`/`HighlightQuickBar` 遍历它解析投放目标）也不进 `HotbarDropSlots`，拖动高亮与投放不会落到它上；`RefreshQuickBar` 的 0.05s 轮询仍覆盖它，冷却读秒/遮罩持续刷新。
- 槽内状态沿用既有语义：无剑类装备时变暗并显示「需剑类」；冷却中显示数字读秒 + 黑色遮罩；冷却结束触发就绪白光。悬停提示「F · 快速进战 / 按 F 直接触发；冷却结束后可用」。

## 手枪版握把砸击（2026-09-17 第四次补充）

单持手枪按 F：松开左手、右手单独持枪、以**握把部分向前猛砸**。动作按 [施法与完整骨段](../../skills/ue5-fps-arms-animation/references/casting-arm-volume.md) 与 [手枪动作适配](../../skills/ue5-fps-arms-animation/references/pistol-adaptation.md) 的既定路线做**程序化姿态层**（火球 V3 同一机制家族），不做 FBX 烘焙。

- **节奏分段**（`QuickCombatPistolMotion.h`，对齐配重锤的起势—接触—收势）：松左手 0–0.16s → 右手上抬竖枪 0.16–0.40s（握把底甩向目标，枪口上仰 38°）→ 前砸 0.40–0.58s（接触点 0.54s，回正到 12°）→ 收势 0.58–1.02s（左手回握闭指）。全部目标相对入场快照，适配任意待机姿态。
- **姿态层**：`FPSCastingMeshComponent::ApplyQuickCombatPose` ——双臂两骨 IK（恒骨长、93% 肩带支撑、前臂继承掌侧 roll 的闭式解，与火球/符文剑同一修复）；左手手指张开度借用火球手型配置与逐指延迟曲线；锁骨保持动画驱动；右手手指保持动画握持并随被接管的手腕刚体跟随；`WPN_root` 挂右手链下时自然跟随，否则按右手入场差值整组刚体搬运（骨架挂接关系运行时检测）。
- **状态机**：`UFPSQuickCombatComponent`（`FPSQuickCombatComponent.h/.cpp`）——Trigger 门禁在角色侧 `AFPSGAMECharacter::TriggerPistolQuickCombat`（单持、非 busy、非施法占用、存活）；动作实际开始提交冷却与使用修炼；接触点沿准星 2m 球形单目标结算（同剑版伤害/击退/眩晕公式，`ReceiveStun` 一次提交），可修炼目标击杀在收势统一提交 +15；收势结束冷却起跳。
- **仲裁**：砸击占用计入 `IsCastingWithLeftHand`（装备换装锁）、`IsCastBlockingLeftHandAction`（新左手工序拒绝）、`IsWeaponBusy`（ADS 自动压制）；`FirePressed` 在砸击期间不接开火；双持左手被副枪占用不触发；换枪/双持/死亡在 Tick 立即收手，冷却照常保留。
- **V1 已知边界**：无前踏步（猛砸距离感靠手臂+肩带前送，待反馈再议突进）；砸击期间普通攻击恢复后的衔接、滑动/翻越中的表现未调；双手 IK 为 V1 参数（起抬 38°、前送 +20cm），形变与手感以用户实机反馈为准（火球 V1→V3 同样迭代）。

## V2：按 COD4 UZI 近战参考重调（2026-09-17 第五次补充）

用户指出 V1 完全不合格，给出 B 站参考 BV13K421e7Rw（COD4 模组展示）45–47s 的 UZI 近战段。下载该视频逐帧看过，提炼的动作语言：

- **枪向右肩方向快速上提、枪口朝上**（不是 V1 的小幅水平后收）；
- **从高处斜向下前方一记短促前捅**，机匣尾/握把端领先——高低差是力度来源，不是水平推；
- **左手同步张开、向左前方开掌推挡**（V1 的"垂到低守位"方向就错了）；
- **全程约 0.5 秒**，干脆利落，收势极短。

据此重写 `QuickCombatPistolMotion.h`：总时长 1.02s→0.85s（接触点 0.54s→0.42s）；上抬角 38°→55° 且终点改到右肩高度（-12,+4,+9）；前捅终点改为斜下前方（+18,+2,−14，高低差 23cm）；左手从低守位改为与砸击同步的左前推掌（+6,−22,−2）。姿态层与组件逻辑不变，只换曲线常量。

参考声明：只借鉴动作语言（节奏/方向/左右手职责），COD4 动画资产有版权未提取；本实现仍为程序化原创。V2 待实机反馈的迭代点：接触瞬间的镜头下压（camera kick）尚未做；上抬与前捅的幅度在玩家视点下的观感；左手推掌与回握的衔接。参考视频存 `test/uzi_ref/uzi_ref.mp4` 本机留档。
- 快捷槽无剑时提示由「需剑类」改为「需剑/枪」；技能页描述同步双武器版本。

## V2 实测结论与挂起（2026-09-17 最终）

V2 实测被用户否决：「根本不合格，跟视频参考一点都不像，枪械模型飞出去了」。定位到**武器搬运变换 bug**：武器组刚体搬运的乘法顺序写反、且对组件空间变换重复做了一次世界→组件转换，每帧给枪乘上垃圾变换直接飞出视模——该 bug 自手枪版首版即存在，导致历次实测看到的都是枪飞，动作本体从未被有效评估。bug 已修（相机空间求「目标手×入场手⁻¹」差值再一次性转回网格空间），修复后未经用户确认。

**用户决定：手枪砸击留到下次开发。** 当前状态：剑版快速进战（配重锤）功能完整可用；手枪砸击代码保留但视为挂起候选，重做入口与教训沉淀见 [手枪握把砸击尝试](../../skills/ue5-fps-arms-animation/references/pistol-grip-bash-attempt.md)。本次无确认退役文件，未走 trash 归档。

## V3：旋转空间修复 + 镜头语言接入（2026-09-18，待实测）

继上次挂起后重开手枪砸击。参考视频留档仍在（`test/uzi_ref/uzi_ref.mp4`），重看核对：V2 节奏（上提→斜下前捅→收势，接触 0.42s，全程 0.85s）与参考 45–47s 段的动作语言一致，数值保留不动。本轮改三处：

- **手部倾斜旋转的前/后乘 bug（`ApplyQuickCombatPose`）**：注释写「绕相机右轴负转」但代码是后乘 `EntryHand.GetRotation()*FQuat(Y,θ)`，实际绕的是手骨局部 Y 轴——竖枪上仰会呈现为腕部扭断而非整枪上扬，是 V1/V2「不像」的隐性成因之一（与枪飞 bug 叠加、从未被看到）。两手均改为前乘 `FQuat(Y,θ)*EntryHand.GetRotation()`（相机空间轴）。
- **武器搬运修复段复核**：逐项重推了 `HandDeltaCam=GoalHandRCam.GetRelativeTransform(EntryHandR)`（=目标手×入场手⁻¹）、`WeaponGoalCam=HandDeltaCam*EntryWeapon`、`CameraToMesh` 与 `ToMesh` 换算——上次的修复数学正确，保持不动。
- **镜头语言接入（教训「打击感约一半来自镜头」的补课）**：`QuickCombatPistolMotion.h` 新增镜头常量段；`UFPSQuickCombatComponent` 新增 `ImpactAge/ImpactStrength` 与 `GetCameraMotion`——上抬段镜头随枪微抬（Z+1.6/Pitch+1.4/Roll−1.2），前捅段快速切到压头前送（Pitch−3.4），命中叠加配重锤同款 exp·sin 阻尼冲量但方向改为**下压**（Pitch−7.5、后坐 13cm 下沉 3cm、0.30s 包络；挥空只有动作语言无冲量，与配重锤「命中才震」合同一致）。角色相机链（`UpdateFirstPersonCamera` 内剑版同一叠加点）并排接入。

编译：Game 与 Editor 双目标构建通过（2026-09-18 08:50/08:52，编辑器当时未运行）。待实机验收：竖枪上仰方向是否正确、枪不再飞、镜头下压的打击感、以及动作整体与参考的相似度——动作本体在枪飞 bug 修复+旋转修复后是**第一次被真正看见**，观感参数（`QuickCombatPistolMotion.h`）预期要按反馈再调。

## V4：棘轮根因修复 + 左手按用户定义重做（2026-09-18，待实测）

V3 实测反馈：模型仍消失；左手食指向上伸开不符合预期。用户定义左手行为：**松握 → 自然下垂移出镜头 → 攻击结束后恢复正常握姿**；先把动作搞清楚，打击感后议。

- **「模型不见了」的真正根因（V1 起贯穿历次实测）**：`FinalizeBoneTransform` 里火球与砸击两个分支各写各的快照清零——砸击进行时火球分支每帧执行 `EntrySerial=0`，砸击分支随即发现序号不匹配**每帧重捕入场快照**。目标点变成相对一个逐帧移动的基线，手与枪以每帧一个满偏移量的速度棘轮式漂移，几帧内飞出视模/撑爆包围盒被剔除。上次修的搬运乘序数学本身是对的，但基线在动所以仍然飞；V3 修的旋转前乘也救不了它。修复：快照只在**两个动作都不占用**时清零（火球路径行为不变——它激活时本来就独占）。
- **左手重做**：删除 V2 按参考视频做的开掌前推与翻掌，删除借用的火球手型开指逻辑（枪模 rigs 上呈现为「食指向上伸开」，被否）。改为：Release 段松握让手（走向下垂位 35%）→ Cock 段垂到 `LeftHangOffset(-10,-8,-38)` 出镜 → Smash 保持 → Recover 段回握入场位。手指保持动画局部随 IK 腕位刚体跟随，不再做张开度驱动；手腕保持入场旋转不翻掌。
- 右臂/武器搬运/节奏参数与镜头语言不动。

编译：Game 与 Editor 双目标通过（2026-09-18 09:09）。待实机验收：模型不再消失（棘轮修复第一次真正生效）、左手下垂出镜与回握、右臂竖枪上扬方向、斜下前捅幅度。

## V5：关键帧化重做（2026-09-18，待实测）

用户指定实测枪为 **Dan Wesson 715**，并确认 V4 仍不达标。按审计结论把 V1–V4 的「标量常量 + 分段公式」换成关键帧表，四处结构改动（R0–R4）：

- **R1 动作结构（`QuickCombatPistolMotion.h` 全表重写）**：总时长 0.85s → **0.60s**，五段 → 六段（松握起手 0–0.06 / 上提蓄势 0.06–0.18 / 蓄势保持 0.18–0.21 / 斜下砸 0.21–0.30 / 跟随 0.30–0.42 / 回握 0.42–0.60），接触点 0.42s → **0.30s**，回握不再比挥击更长。删掉 V4 前 0.12s 右臂完全静止的空转段。参数集中在 7 键表：右腕/右肘极/右手朝向/左腕/左肘极/左手朝向/松握量/肩带位移/肩线偏航/相机位/相机转，每段缓动（Smooth/Accel/Decel/Linear）写在 `EaseToNext`。调参只改表，不再改公式。
- **R1 力度来源改为肘与肩**：腕只作 IK 目标，新增**肘极向量**关键帧（蓄势外后上 → 下砸前下）与**肩线偏航**（0→7°→10°→8°→0，正 = 右肩前送）。肩带位移 + 锁骨偏航后由 `RefUpperHeadInClav`（上臂根在锁骨空间的固定点）解出上臂根；够不到臂长时锁骨与上臂根**一起**前送，替代 V4 只推上臂根导致左肩脱节的写法。
- **R2 左手按用户定义做实**：新增 `LeftRelax` 关键帧，手指子骨向参考姿势最多混合 30%（不再借火球开指，避免「食指上伸」）；下垂位由 `(-10,-8,-38)` 收到 `(-8,-7,-26)`，并给左手**朝向**关键帧（不再冻结入场旋转）；下垂与回握都有肘极，回握跟随 Recover 权重交还动画。
- **R3 命中与画面同源**：新增 `UFPSCastingMeshComponent::GetQuickCombatStrikeProbe()`——接触判定改为**从握把底**（手骨世界位置 + 相机空间握把偏移）出发，方向取「玩家瞄准 65% + 斜下砸走向 35%」加权，长度仍用技能 `rangeCM`。V4 前是从眼位沿视线前扫，枪和手从没碰到过判定线。
- **R0 诊断日志**：动作入口打印枪型（DanWesson715 / M1911 / 其它手枪）；入场快照打印武器是否挂右手链 + 右腕/左腕/右肩的相机空间基准；接触时打印射线来源（握把底或眼位回退）、起点、方向、距离与命中目标。实机反馈可直接对上参数。
- **顺带修掉的隐患**：`EntrySerial` 由火球与砸击两个分支共用，砸击序列号可能与火球序列号撞值导致首帧沿用旧快照；砸击改用 `0x40000000` 序号段隔离。

编译：Game 与 Editor 双目标均通过（`Binaries/Win64/FPSGAME.exe`、`Binaries/Win64/UnrealEditor-FPSGAME.dll`，2026-09-18）。Editor 首次链接因编辑器/Live Coding 占用失败，关闭编辑器后重链成功。

未实测：本条全部数值（0.60s 节奏、82°/58° 枪口上仰、肘极与肩线幅度、左手下垂位与松握量、命中加权 65/35）都是按审计推导的起点值，等实机观感与打击感反馈再改表。

## V6：自激振荡 + 视模剔除（2026-09-18，待实测）

V5 实测（715）：**右手动作对了**（用户确认），但枪模完全看不到、左手看起来没变化。加入姿态自检日志后（`Saved/Logs/FPSGAME.log` 的 `[QuickCombat] 姿态`，每 0.12s 一行），定位到两个彼此独立的问题：

- **自激振荡（数值爆炸）**：同一动作内 `t=0.39` 正常（右手 46.9,4.3,−36.5 / 左手 22.1,−8.3,−53.3 / 武器距右手 17.0cm），`t=0.44` 起爆到 162→347→1150cm，武器距右手最大 1300cm——手与枪都被推出视模。根因是姿态层**把自己的输出当成下一帧输入**：肘极取「当前肘位」、收势混合取「当前局部姿态」，而这两处上一帧刚被本层写过，于是每帧放大一次。修复：整层只用「入场快照 + 关键帧目标」，任何通道都不再回读当前组件空间姿态（肘极基准改为入场肘位 `EntryLowerL/R`；组装改为「入场绝对姿态 → 目标绝对姿态」按层权重混合）。
- **视模剔除（武器完全看不见）**：修复自激后姿态成立（骨骼位置日志正常、武器距右手 12–17cm），但画面里看不到武器。原因是 UE 按**为旧动目标定的导入包围盒**剔除视模——先例见 `Docs/Weapons/m1911-development-audit-20260913.md` 第 1 行（「导入参考包围盒未覆盖动画后的蒙皮顶点 → 枪身与双手消失，只留静态配件」，M1911 因此设 `BoundsScale=4.17`，并注明「此值属于当前资产，不能套给所有手枪」）。715 现用 `BoundsScale=5`，是按旧动作标定的包络。修复：砸击期间把包围盒 ×3 并改为跟随当前姿态骨（`bComponentUseFixedSkelBounds=false`），动作结束原样还原；日志新增「视模包围盒」行。
- **左手**：日志证明左手一直在被驱动（入场 `30.0,−1.3,−29.3` → 0.26s `22.1,−8.3,−55.1`，正是关键帧给的下垂位）；V6 另把手臂蒙皮占比最大的 twist/掌骨辅助骨改成「静止局部随段刚体」（避免残留持枪 roll），五指（含掌骨）保持入场握持并向参考姿势混合 40% 作为松握。

编译：Editor 目标编译 + 链接通过（PIE 用）。Game 目标被并行会话的 `Building/VoxelBuildAudit.cpp` 挡住——该文件的匿名命名空间变量 `Expected` 与 `Atomic.h` 的 `CompareExchange(T& Expected,…)` 撞名触发 C4459（当错误），与本次改动无关。

未实测：以上都等 715 实机确认——枪是否重新可见、左手外观是否跟着动、收势是否不再崩、以及动作观感与打击感（打击感用户明示后议）。

## V7：真正的根因——武器搬运丢了 100 倍骨缩放（2026-09-18，已实机复现并修复）

用户要求我自己开编辑器验证，于是按「编辑器 → PIE → 发送 F → 连拍截图」自查（截图存 `Saved/QuickCombatProbe/`，属本机产物不入库）。结论：

- **现象（截图对照）**：动作期间枪体与右手都看不到，只剩一截细长条（激光射线）与自然下垂的左手；按 F 前与动作结束后枪都正常。日志同步显示 `武器缩放=1.00`，而入场是 `武器缩放=100.00/参考100.00`。
- **根因**：这把枪的网格导入后，**手骨/武器骨在运行时是 100 倍缩放**（`右腕缩放=100.00/参考100.00`）。武器搬运的中间变换写成 `FTransform(HandRCam,WristCam)`（纯旋转+位置 ⇒ 缩放 1），于是 `HandDeltaCam` 的缩放变成 1/100、`WeaponGoalCam` 最终缩放=1，**枪体被缩到 1/100**，等效消失；激光是射线、不受骨缩放影响，所以只剩一条细线。
- **修复**：`GoalHandRCam` 带上入场手骨缩放（`EntryHandR.GetScale3D()`）；武器目标保留 `WeaponGoalCam.GetScale3D()`；手臂链骨显式沿用入场姿态缩放（`EntryAbs[...].GetScale3D()`）。修复后日志全程 `武器缩放=100.00`，截图里枪完整可见、左手自然下垂、结束后回到常态。
- **撤销的错误方向**：先前怀疑的「视模包围盒剔除」不成立——实测把包围盒放大到 13 米包络后枪依旧不可见；已把那段防御性改动（`BoundsScale ×3` + `bComponentUseFixedSkelBounds=false`）**整段删除**并复测（枪仍完整可见），只在诊断日志里保留包络字段。`m1911-development-audit` 的包围盒先例仍有效，但与本症状无关。
- **顺带解封**：`Building/VoxelBuildAudit.cpp` 匿名命名空间的数组 `Expected` 与 `Core/Templates/Atomic.h` 的 `CompareExchange(T& Expected, …)` 撞名，unity 分组变化后触发 C4459（本工程当错误）→ 改名 `ExpectedMaterials`（两处，无行为变化）。

实机记录（2026-09-18，编辑器 PIE，DayNight_Lighting，DanWesson715）：按 F → 蓄势（枪口上仰、左手松握下垂）→ 斜下砸 → 回握，**全程枪在右手里**。待调项：前砸最低点落在画面右下角、接触瞬间枪身贴屏幕下缘，属于观感/打击感范围，用户已明确后议。

## V8：武器不跟手/方向错位——搬运不是刚体变换（2026-09-18，已编译，待 715 实机确认）

用户反馈：V7 后枪看得见了，但**枪不跟手、运动方向错位**。

- **根因（引擎语义）**：`Core/Templates/../Math/TransformNonVectorized.h` 的 `Multiply` 写明
  `C=A*B` ⇒ `C.T = B.T + B.Q*(B.S*A.T)`、`C.Q = B.Q*A.Q`（**右操作数才是父级/外层**），
  而 `GetRelativeTransform(Other) = this*Other⁻¹`。因此：
  - 上下文里 `Child_abs = Child_local * Parent_abs`、`Local = Child_abs * Parent_abs⁻¹` 都成立（现有代码一致）；
  - 刚体搬运要求**「武器在手坐标系里的局部关系」不变**：`Rel = Weapon_entry * Hand_entry⁻¹`，
    `Weapon_goal = Rel * Hand_goal`；
  - V7 之前写的是 `GoalHand * EntryHand⁻¹ * Weapon_entry`（把"手的自身帧差值"套在武器上）——
    这不是刚体变换：枪会相对手漂移，朝向也会错位。
- **修复**：`ApplyQuickCombatPose` 的武器搬运改为 `WeaponRelHandCam = EntryWeapon.GetRelativeTransform(EntryHandR)`，
  `WeaponGoalCam = WeaponRelHandCam * GoalHandRCam`（同时保留 V7 的入场缩放）。手臂链骨/手指/辅助骨的写法本就用
  「局部 × 父级」形式，未受影响。
- **自测情况（我自己开编辑器 + PIE + 发 F + 连拍截图）**：V7 的缩放修复已用截图证实（枪完整可见、左手自然下垂、收势回位）；
  V8 已编译通过（Game + Editor）并一度用编辑器内 Live Coding 生效，但**本轮无法独立复现 715 在手的会话**——
  本机存档的当前武器是 M4A1（715 在"主手武器2"槽），而项目没有武器切换的按键绑定（`BindAction` 只有
  Aim/Fire/InspectWeapon/Jump/QuickCombat/Reload/Slide/Sprint），背包面板点击与数字键都不切换。
  故 V8 的**画面**结论留待用户以 715 按 F 确认。
- **可判定的客观校验**：日志 `[QuickCombat] 姿态` 行的 `武器距右手` 必须**全程恒定 ≈13.0cm**
  （入场值即 13.0）。修复前它是 13.0→12.1→7.6→16.9→12.2（漂移 ⇒ 不跟手）；修复后若恒定即为刚体搬运成立。

## V9：改走作者源 clip（2026-09-18，已制作/导入/接入，待实机）

用户 2026-09-18 决定：手枪快速进战的**动作本体改走作者源路线**，不再用运行时程序化姿态层
（沿这把枪换弹的约定：把持枪运动烘焙到 `WPN_root`、由枪带动右手）。理由见「路线」一节：
V6/V7/V8 修的全是程序化路线自带的坑（自激、缩放、刚体搬运），作者源路线从构造上消除它们。

- **作者源**：`SourceAssets/DanWesson715QuickCombat20260918/author_actions.py`（基座为掌面避让版可编辑源），
  逐帧 120 Hz 烘焙 `DW715_quickcombat`，导出 `Animations/A_DW715_quickcombat.fbx`，并存
  `DanWesson715_QuickCombat_Editable.blend`。动作表见该目录 `animation.json` / `README.md`：
  0.60s 六段（起手/蓄势 82°/保持/下砸 58°/跟随/回握），**接触 0.30s**；左手锚点 0.06s 后固定、下垂 26cm 出镜、收势回握。
- **关键不变量（作者源自检）**：`|WPN_root − hand_r| = 0.133 m` 全程恒定——枪始终在手里，不跟手/漂移在作者源层面不可能出现。
- **导入**：`import_assets.py` → `/Game/Weapons/DanWesson715/QuickCombat20260918/Animations`，
  `DW715_QUICKCOMBAT_IMPORT_COMPLETE`，读回 `length=0.6000`。
- **运行时接线**：新增 `EAKMWeaponState::QuickCombat` + `QuickCombatAnimation`，
  `TriggerPistolQuickCombat()` 里播放；结束复用既有 `FinishWeaponAction()`。
  `FPSQuickCombatComponent` 只保留命中射线（起点=握把底、方向=玩家瞄准）、冷却、修炼与命中镜头冲量。
- **退役代码**：`FPSCastingMeshComponent::ApplyQuickCombatPose` 及其**全部**诊断/姿态层成员已删除
  （V5–V8 期间的关键帧表、入场快照、IK、辅助骨、刚体搬运与包围盒逻辑一并移除）；
  `QuickCombatPistolMotion.h` 只留下组件仍在用的分段时钟、命中球半径、握把偏移与冲量常数。

编译：Game 与 Editor 双目标通过（2026-09-18 16:50 重链）。

未实测：本机存档当前活动武器是 M4A1（715 在"主手武器2"），项目没有武器切换按键绑定，
自动化抓图拿不到 715 在手（且用户当时在另一窗口工作，我停止抢占前台）。请切到 715 按 F 判读：
枪是否全程在右手、蓄势/下砸方向是否合理、左手松握→下垂→回握是否自然；打击感按用户要求后议。

## V10：打击感通道补齐（2026-09-18，已编译/导入，待手感判读）

用户 2026-09-18 确认 V9 的作者源 clip 成立，随后要求优化打击感。先按项目里**已获接受的近战基准**
（符文剑配重锤）逐通道对齐，发现并补齐了缺口：

| 通道 | 配重锤（已接受） | 手枪砸击（本轮前） | 本轮 |
| --- | --- | --- | --- |
| 动作本体 | 复用第四连击 | 程序化层（已退役） | 作者源 clip（0.60s，含接触顿帧） |
| 命中确认（准星命中标记 + 钝器确认音 + 怪物血条反馈） | 有（`NotifyConfirmedWeaponHit`） | **缺失** | **已补**（`bFirearmHit=false`） |
| 命中音 | `S_MeleeHit_Quick` | 同一枚 | 不变 |
| 起手挥动音 | `S_Sword_Attack` 作攻击层 | 无 | 已加（复用同一挥动层，音量 0.6，起手一次） |
| 动作镜头（蓄势/前送/回位） | 有（含 1.5 强度） | V9 起只剩命中冲量 | 已加（随枪微抬 → 下砸压头前送 → 跟随回位，整体 ×1.4） |
| 命中镜头冲量 | 位置 18/2.8、旋转 10.5/1.6（有效 ×1.5） | 13/3、7.5/1.8（明显偏弱） | 提到同级下压版：位置 24/3.6、旋转 12/2.2；仍「命中才震」 |
| 敌方反应 | 击退 + 眩晕 | 同（`ReceiveStun`） | 不变（不动数值） |
| 顿帧 | 无（项目此前无 hitstop） | 无 | **做在 clip 里**：0.30→0.33 接触保持（3 帧），不动引擎时钟 |

- 顿帧选择写在作者源而不是运行时：`author_actions.py` 的 `MOTION` 增加 0.30→0.33 的接触保持段，
  重新导出导入（`DW715_QUICKCOMBAT_IMPORTED length=0.6000`）。命中结算仍在 0.30s，二者不冲突。
- 镜头冲量量级按「配重锤基础值 × 其 SwordCameraStrength(1.5)」折算，避免再出现"比已接受的动作弱"的问题；
  数值集中在 `QuickCombatPistolMotion.h`（`PistolBashCameraStrength` / `ImpactSpan/Decay/Freq/Location/Rotation`），
  改手感只改这几个数。

编译：Game 与 Editor 双目标通过（2026-09-18）。

未实测：手感由用户判读。可选下一步（未做，等用户定）：命中时的短促 hitstop（局部放慢动作播放 2–3 帧）、
专用金属刮擦命中音（当前复用配重锤钝器音）、接触点火花/碎屑（项目近战此前没有该通道，需要先定标准）。

### V10 二次调参（用户 2026-09-18：镜头冲击加强、右手抬高、下砸更快）

- **镜头冲击抖动加强**：`ImpactLocation` 24/3.6 → **32/4.8**，`ImpactRotation` 12/2.2 → **16/3.0**，
  衰减 13 → **12**、频率 48 → **52**（更长更硬）；动作镜头强度仍 1.4。
- **右手蓄势抬高**：枪根蓄势偏移 Z `0.12 → 0.19`（同时更靠右后 X −0.07 / Y 0.11），肘极同步抬高（0.06→0.08）。
  作者源读数：蓄势帧右手高度 −0.085 → **+0.002**（抬高约 8.7cm）。
- **下砸与整体提速**：下砸段 0.21→0.30 改为 **0.21→0.26**（约 1.8× 快），接触点 0.30 → **0.26**，
  全程 0.60 → **0.55s**（顿帧仍在接触后 3 帧：0.26→0.29）。
- **时钟改成按比例推导**：组件新增 `ConfigureForClipLength(Length)`，接触/阶段时间 = clip 长度 × 比例
  （`ReleaseFraction/HoldFraction/CockFraction/ContactFraction/FollowFraction`），由角色在起手时传入
  `QuickCombatAnimation->GetPlayLength()`。**以后改作者源节奏不会再和结算脱节**。
- 重新导出/导入：`DW715_QUICKCOMBAT_IMPORTED length=0.5500`；Game + Editor 双目标编译通过。
  注：编辑器打开时 uasset 被占用（`MoveFile ... Error Code 32`），需关闭编辑器再导入；已打开的编辑器要重载资产/重启才会看到新 clip。

### V10 三次调参（用户 2026-09-18：手再抬高、前砸手臂再伸出）

- **蓄势再抬高**：枪根蓄势 Z `0.19 → 0.26`（Y 0.11 → 0.12），肘极再抬高（0.08 → 0.11）。
  作者源读数：蓄势帧右手高度 `+0.002 → +0.090`（相对待机 −0.166，累计抬高约 25.6cm）；枪口到 Z=+0.445。
- **前砸把手臂送出更远**：接触枪根 Y `−0.14 → −0.22`、跟随 `−0.19 → −0.28`，下压幅度略减（Z −0.09→−0.07 / −0.13→−0.10）。
  作者源读数：接触帧右手 `Y −0.546 → −0.644`（**前送多 9.8cm**，Z 从 −0.355 抬到 −0.330），跟随帧 `−0.590 → −0.702`。
  达到这段距离靠"上臂根（肩带）沿目标方向前送"，即**整条手臂伸出去**；`|WPN_root − hand_r| = 0.133` 仍全程恒定。
- 时长/时钟不变（0.55s / 接触 0.26s），所以只需重导资产、无需重编译。导入：`length=0.5500`。

### V10 起手/释放音替换（用户 2026-09-18 指定 quickhit2.mp3；先误接到命中音，已按澄清改正）

- 首次理解成"命中音"，已按用户澄清改正为**起手/释放音**（起手那一下的挥动层）。
- 按项目既有音频口径处理：`D:\FPS3D\资产\音效\quickhit2.mp3` → `imageio_ffmpeg` 转
  **44.1 kHz / 立体声 / 16-bit PCM WAV** → `AssetImportTask` 导入为 SoundWave，
  `LoadingBehavior=FORCE_INLINE`，写回执。
  资产：`/Game/Audio/QuickCombat20260918/S_QuickCombatSwing`（**0.3840 s**）。
  入口与记录：`SourceAssets/QuickCombatSwingAudio20260918/`（`convert_source.py`、
  `import_quick_combat_swing.py`、`run_import.ps1`、`import_receipt.json`、README）、
  `Docs/Audio/quick-combat-swing-20260918.md`。
- 接线：`FPSQuickCombatComponent::BeginPlay()` 的 `SwingSound` 从复用剑的 `S_Sword_Attack`
  改为 `S_QuickCombatSwing`（起手触发一次、2D、音量 0.8）；**命中音回退到与剑柄尾共用的
  `S_MeleeHit_Quick`**（本轮只改起手音）。误建的 `/Game/Audio/QuickCombatHit20260918/S_QuickCombatHit`
  已删除（无引用）。
- **复核（2026-09-18）**：`quickhit2.mp3` 与 `quickhit.mp3` 时长同为 0.3840 s、转换 WAV 同为 67,844 字节，
  但 SHA-256 不同、PCM 有 62,203/67,800 字节不同（最大差值 255）——**两段不同素材**（用户确认）。
  教训：音频"同长度同体积"不等于同一素材，别用文件大小当重复判据；要判重复就比哈希或比 PCM。
- **顺手修掉的静默回归（我自己跑 PIE 时读日志发现）**：V9 删掉程序化姿态层后，
  `CacheCastSkeleton()` 只剩火球施法路径调用，于是不打火球时 `BashHandR` 恒为 `INDEX_NONE`，
  打击探针静默退化成"眼位起点"（实机日志 `射线=眼位回退`）——"起点=握把底"这条修复等于没生效。
  修复：探针内惰性初始化（`if(PoseMesh != GetSkeletalMeshAsset() || BashHandR==INDEX_NONE) CacheCastSkeleton()`）。
  教训：删除一条路径后，要检查它**副作用初始化**（缓存、骨骼索引、诊断）是否还有别的调用者；否则不会报错，只会悄悄退化。
- **"声音没替换"排查结论（用户 2026-09-18 实测日志）**：日志显示 `射线=握把底`（探针修复已生效）、
  `枪型=DanWesson715`（走的是手枪路径），但三次接触全是 **`目标=无`** ——按"命中才出声"的合同
  （与剑柄尾一致），命中音不会播；用户听到的是**起手挥动音** `S_Sword_Attack`（沿用剑的挥动层）。
  为免再靠听感猜，组件加了三行判定日志：
  `[QuickCombat] 音效资产 命中音=… 挥动音=…`（BeginPlay）、`挥动音播放=…`（起手）、
  `命中音播放=… 目标=… 伤害=…`（确认命中）。**要听命中音必须先真的打中一个目标**；
  测试图当前没有怪物，需要用 F6 开发面板刷一个。

## 取消武器类型锁（2026-09-18，用户要求）

用户要求先拆掉“只有手枪/近战武器能按 F”的限制。改动四处，全部在判定/显示层，不碰数值与冷却合同：

| 位置 | 之前 | 现在 |
| --- | --- | --- |
| `UColdSteelStatusModel::TriggerQuickCombat()` | 剑/单持手枪/M4 之外直接拒绝并打印“当前武器不支持” | **不判定武器类型**：剑（已装备）→ 配重锤；手枪 → 握把砸击；其余枪械 → 步枪枪托砸击。只有角色没有动作组件时才拒绝 |
| `AFPSGAMECharacter::TriggerRifleStockMelee()` | 额外要求 `bUsingM4Infima`（“当前不是 M4”） | 取消该条；非手枪的任何枪械都可触发，非 M4 复用 M4 的整枪 clip（各枪共用同一套 Manny 手臂骨架 + `WPN_` 骨），日志标 `clip=本枪/M4 回退` |
| `UColdSteelQuickSlot`（F 专属槽） | 不是剑/单持手枪时变暗并提示“需剑/枪” | 只在冷却中变暗；不再有武器类型提示 |
| 技能页/规则默认文案 + `skills.json` 描述 | “限剑类或单持手枪…” | “不限武器类型。按 F 按当前手里的武器选动作…” |

保留的判定（不是武器类型锁）：冷却中拒绝；双持手枪时手枪版拒绝（左手被副枪占用）；
移动动作 / 施法占用 / 武器忙碌 / 已死亡照常拒绝。

顺带修掉的动作组件缺口：没有作者源 clip 的手枪（M1911 等）此前会“提交冷却但什么都不播”，
现在改为明确拒绝并打印原因。

## 未测试范围

未运行游戏与任何验收；未新增测试命令或验收脚本（项目规则：检查/验收由用户发起）。F 专属槽轮：Game 目标编译链接通过（含 UE5.8 格式串编译期校验修正：`FString::Printf` 的 `%s` 实参必须解引用为 `TCHAR*`）；Editor 目标因用户编辑器开启（Live Coding 占用）未重建，当前编辑器 DLL 缺 F 槽与正式伤害文案，重启编辑器前后由 UBT 补建。待实机确认：F 槽显示与冷却读秒、无剑「需剑类」变暗、拖动高亮不落 F 槽、F 触发与第四击动作观感、伤害数值与面板分摊、击退/眩晕表现、连击排队、非剑武器按 F 的忽略路径、击杀修炼与升级提示、旧档迁移。

## M1911 补齐作者源 clip（2026-09-19）

1911 此前在 `TriggerPistolQuickCombat()` 的"该枪没有快速进战 clip"门槛被明确拒绝（动画只有
715 一份）。本轮按 715 的作者管线（`SourceAssets/DanWesson715QuickCombat20260918`）为 1911
出同语言 clip，作者源记录见 `SourceAssets/M1911QuickCombat20260919/README.md`：

- 基座 `M1911_RearFinish_Editable.blend`（与运行网格 `RearFinish20260913/SK_M1911_Manny` 同源），
  以 `M1911_idle` 为基准，动作表与 715 **逐值相同**（0.55s / 接触 0.26s，`QuickCombatPistolMotion.h`
  的分段比例对两枪同时成立）；`WPN_root` 驱动右手（RIGHT_GRIP 取 1911 自己的 idle），
  左手松握→下垂出镜→回握。自检：`|WPN_root − hand_r| = 0.125 m` 全程恒定，末帧精确回 idle。
- 导入 `/Game/Weapons/M1911/QuickCombat20260919/Animations/A_M1911_quickcombat`（0.55s，
  `BC_M4Viewmodel` 压缩）；骨架从运行网格动态解析——实际用的是
  `P9Retarget20260913/SK_M1911_Manny_Skeleton`（同命名骨架全项目有三份，硬编码会指错）。
- C++ 接线：`M1911WeaponAssets.h::QuickCombatAnimationPath`；`bUseM1911` 分支加载；
  **`InitializeWeaponVisuals()` 重置段补 `QuickCombatAnimation=nullptr`，修复换枪残留指针**
  ——此前先用过 715 再切 1911，残留指针会让 1911 把 715 的 clip 播出来（从未适配验证过）。
- Editor 目标 2026-09-19 10:04 构建成功（obj/DLL 时间戳已核）。未实机验收，重点看 1911 砸击
  观感、接触命中反馈、与 715 动作语言一致性。

### 修订（2026-09-19 下午）：1911 首版动作语言不一致 → 装配朝向共轭修复

用户实测两枪后判 1911"动作语言不一致"。定位：**1911 的 Manny 装配臂架系与 715 差 180°**
（枪口 +Y/右手 +X vs 枪口 -Y/右手 -X；`WPN_root` 欧拉 (0,4.9,-180) vs (0,0,0)）。动作表里
枪局部系合成的量（gun/tilt）身体相对方向天然一致，但**臂架空间分量直用的三个量全部镜像**：
左手下垂偏移（甩向枪口方向而非左后出镜）、肘极（蓄势抬肘方向反）、躯干平移（前后反）。

修法与验证详见 `SourceAssets/M1911QuickCombat20260919/README.md` 修订段：ALIGN 纯 Z 共轭
（实测 -179.2°，带右手侧断言），修复后正则系逐帧增量差——左手 ≤3mm、枪 ≤5cm（1911 自带
4.9° idle 上仰所致，Z 分量≈0）、肘 ≤9.6cm（IK 非线性）。资产已重导入，C++ 未动。
**通用教训：跨枪复用动作表，凡臂架空间直用的偏移必须按装配朝向共轭；判别法=比对两枪
idle 枪口方向与右手所在侧。**

### 终版（2026-09-19 第三轮）：真根因=基准取错 idle 代，"直接复用"成立

用户第二轮反馈后重查：**可编辑源 blend 里堆着历代 action，前两版的基准 `M1911_idle` 是老一代**
——装配朝向差 180° 且枪收胸口；而运行时 idle 经 `M1911WeaponAssets::AnimationPath("idle")` 指向
Contact20260913 的 `M1911_Contact_idle`，它**与 715 的 idle 臂架空间逐位一致**（含握把距离 0.133m）。
所以第一版镜像、第二版方向对但 54cm 起手跳变，都是同一个错基准的两个症状；"装配 180°"是老一代
装配的真实属性，不是当前运行时的。

终版=换 Contact idle 基准，ALIGN 自动退化为 0°，715 动作表原样直用（即"直接复用"）；与 715 clip
逐帧增量差全 0.000。715 自身 QC 基座（PalmClearance 源）与运行时（Upgrade 源）两代 idle 逐位一致，
从未中此坑。**教训：作者源基准必须取运行时实际加载的那一代（先查 AnimationPath 指向再开 blend）；
blend 里同名前缀的历代 action 不能按"名字最朴素"挑。**

## 剑类判定切到手枪同款合同 + 手枪挥击音改到命中确认（2026-09-19）

用户排查发现剑类快速进战命中判定有问题。核对结论：两边**判定语义不一致**——手枪 V5 是
接触时刻一次确定性扫射（握把底沿瞄准 200cm、Ø32 球、ECC_Pawn 最近者）；剑仍走普通挥击的
动画路径采样（Ø6cm 打击头沿配重弧线、Ø40cm 前向走廊、眼位 Visibility 遮挡复查、
`SwingReach=Max(刀长×2=360, 200)` 的混乱门控）。后果：剑的横向容差窄一半以上、草丛/栏杆/
前置怪物被遮挡复查静默拒判、末端物理覆盖只有 ~180cm（手枪等效 ~240cm）、贴脸低于弧线的
目标漏判。

**修复**（对齐合同，动作只负责观感）：
- `URuneSwordComponent::QuickCombatContractHit()`：配重接触帧（`ExtensionEnd`）做一次与手枪
  完全相同的判定——起点=配重打击端（`ReadBlade` 的 pommel 面，读不到退眼位）、方向=瞄准、
  长度=技能 `rangeCM`、球 `QuickCombatPistolMotion::QueryRadiusCM`（Ø32）、`ECC_Pawn` 最近
  阻挡者。结算沿用剑侧既有口径（ApplyHitWithReactionScale、ReceiveStun 一次提交、击杀修炼
  Pending、符文易伤、剑术命中修炼、NotifyConfirmedWeaponHit、附魔 OnHit）。
- Tick 命中块：`bQuickCombatStrike` 时**跳过**动画路径采样，只打一次合同判定
  （`bQuickCombatContactDone` 防重，四处重置：StartSwing/StartQuickCombatStrike 经由
  StartSwing、CancelAction、攻击结束块）。
- `StartQuickCombatStrike` 删 `SwingReach=Max(...)`，改存 `QuickCombatRangeCM=Cast.RangeCM`
  ——快速进战不再借用普通挥击的双倍刀刃 reach。

**手枪音效**（用户 2026-09-19 指定）：挥击音（quickhit2.mp3，0.8 音量）从「松握即播
（0.06s，挥空也响）」改到 `ContactHit` 伤害确认分支——挥空整段只有动作没有声音，与
「命中才震」的镜头冲量同一合同；命中音 `S_MeleeHit_Quick` 仍在命中点播放。删除
`bSwingPlayed` 成员。步枪版共用本组件，行为同步。

Editor 构建（完整依赖追踪，未带 `-NoUBTMakefiles`——两个头文件有布局变化）2026-09-19
10:41 成功。未实机测试：重点=剑贴脸/侧向/2m 距离的命中、草丛与前置怪物遮挡场景、
挥空不出声、命中双音（挥击音+命中音）叠加听感；剑的配重打击音（`PommelHitSound`，
ContactStart 无条件播）本轮未动，要对齐可再改。

## 步枪枪托砸击 D 版重做（2026-09-19）

用户判定 C 版（纯水平横扫）不达标后重做。这轮改了方法：先用视觉模型逐帧精读参考帧
（cycle 大图 + 蓄势放大 + 旧版对比条），锁定参考三要素——**拉近（蓄势贴脸）、滚平（抛壳窗
朝上，接触时 −80°）、斜向横扫（枪口左出画→回扫中右、命中时斜指右上，机匣尾扫过中心）**；
C 版三样全缺。D 表总长放宽到 0.90s（参考周期 ~1.0s），接触 0.435s，分段比例与
`QuickCombatRifleMotion.h` 常数逐项一致（无代码改动）。制作顺序固化为：
tune_motion 离线核轨迹（solve 锚点=参考枪托屏幕位置）→ 烘 Base → 渲染对位条 → 视觉比对
通过 → 烘全量六 profile（握把不变量恒定）→ 编辑器远程通道导入 6/6（0.9000s）。
待用户实机验收。A/B/C 保留为 `--variant` 对照候选。

### E 版二次重做（2026-09-19）：左手支点 + 枪托前顶

用户判 D 版"方向不对、动作语义也不对"。对打击瞬间三段逐帧精读修正理解：参考是**以左手护木
为轴、枪托自右后向前划弧顶出**——枪口停在左前不回头，唯一大位移端是枪托（右中→穿中心→左中），
且带明显前顶分量；D 绕右腕整枪偏航大回旋（枪口左→右），"旅行的端"错了。几何事实：本工程相机
坐在机匣位、枪托板天然在相机身后，参考"蓄势枪托右侧可见"不可达——按本框架取语义（前顶为主）。
E 表：小偏航（枪口左中）+滚平贯穿（接触 −75°）+硬前突 0.26m；接触帧枪托板入画中下、枪口正中、
前伸 0.87m。Base 视觉过审后烘全量，远程导入 6/6（0.9000s，11:04）。待用户实机验收。
**教训：双手持枪的整枪动作，先问"哪一端是旅行者、哪只手是轴"，再谈方向——支点错=语义错。**

### F 版三次重做（2026-09-19）：直接观看参考视频后落参

用户判 E 版仍不达标："按你的思路做的动作匹配你的想法，但要参考视频里枪托挥击的动作，不要
自创；如实汇报能否看到参考视频。"**如实汇报：前三轮（含 09-17/18 两轮）都没直接看过视频**，
一直走"抽帧→视觉模型→文字描述→综合"的间接通道，转述自相矛盾导致按自创几何做。
本轮 Read 工具直接以视频输入观看 64.3–67.8s（原速 + 0.2x 慢放提亮自制
`test/uzi_ref/ak47_melee_slow5x.mp4`）。**直接观察**：蓄势=滚平+大幅偏航（枪口左/枪托右）+拉近；
打击=左手轴大幅水平回旋（枪托右→穿过中心→左，枪口同时左→右大幅弧线）+整枪前伸，滚平贯穿；
收势回滚。F 表：yaw 46→−16 大回旋 + roll −72 贯穿 + offset 拉近 −0.06→前伸 +0.22。
校核（枪口 u 0.21→0.52→0.60）+ Base 预览视觉过审 + 全量六 profile 不变量恒定 + 远程导入 6/6。
**教训：动作复刻任务的参考素材必须以第一手形式看（视频直读/慢放），文字转述通道的矛盾
要在第一轮就暴露，而不是三轮之后再暴露。**待用户实机验收。

### H 版五次重做（2026-09-19）：实机审计闭环——屏幕级验收只认真机

用户实测 G："枪托根本没往前扫，枪口往前捅了一下"，并要求检查游戏内实际情况。本轮新增
`QuickCombatAudit` 实机夹具（-game 进程触发砸击+逐帧截图+枪托/枪口/相机/枪根世界变换 CSV），
暴露两个测量陷阱：①枪托探针方向反（臂架 +Y 为前，枪托在局部 −Y，g4 量的是机匣前段）；
②**Blender 预览相机 ≠ 游戏相机**（视模偏移+运行时镜头层），G 预览"枪托穿中央"、实机"下沉出画"，
离线屏幕预测全部失真。G 实机真枪托入画后停在 (0.46,0.61)、回旋仅 31°；H 按实测方向性把表 yaw
加到 +60→−62（大回旋），实机复验：枪口 −0.3→0.76 横穿、枪托 0.83→0.41→0.38 扫过中心、
前伸 96cm——读感与参考（整枪压平横扫、枪托穿中央）一致。六 profile 导入 6/6。
**教训：视模动作的验收必须闭环到实机画面；离线预览只做穿模/糊脸级检查，屏幕轨迹预测在
相机模型标定前一律不可信。**
