# 全水体击水与回落细节 — 2026-09-24

用户确认自然水花的方块修复成功后，要求推广到全部水体并继续下一步。本轮范围为全河道、罗马喷泉三层水盘及地牢四处浅积水；Foundation 的 Epic Water 蓝图仍是未投放的素材入口，不将其称为已迁移场景。

## 接入

- 河流不再限于出生点附近 120 m。材质升级层覆盖所有加载的河段，击水沿用 64 m 空间桶和实际河面三角形，填高后的干地仍排除。
- 喷泉和浅积水轮廓从当前保存网格的 source LOD0 导出，保留曲线轮廓，运行时只用紧凑多边形。三层水盘按高度分别判定，涟漪有高度限制，避免同一击中同时显示在上下层。
- 水面到达关卡时注册一次；建造喷泉在 BeginPlay/EndPlay 注册和移除，地牢生成器在创建网格后注册。复用原有生命周期，没有逐帧全场寻找水体。
- 静态水体保留原材质、水色、WPO、环境声和其他效果，只叠加命中法线与短暂泡沫。材质实例继承既有参数。
- 统一选择实体阻挡之前最近的入水面；弹道、伤害和穿透规则保持原实现。一发子弹仅触发一次入水表现。

## 下一步细节

- 每次击中增加两个错开的回落小涟漪，位置、延迟随该次命中变化，约 0.72 秒内消退。
- 这是与飞溅时段匹配的解析表现，不是对每一颗 Niagara 水滴做碰撞回调。没有增加粒子碰撞或实时流体求解。
- 碎泡沫短暂出现并破开；河流复用已有泡沫纹理采样，静态水面用解析破碎形状。
- 浅积水强度为普通水体的 0.28，水花主体约四分之一尺寸，近中景四颗水滴，远景两颗。普通河流和喷泉保留已认可的水花尺寸与粒子档位。

## 预算及修复保留

- 所有水体共用 12 个水花组件，不按水体增设水花池。
- 全世界最多 8 条活动命中记录，每条最多两个回落小波纹；新记录会清掉同槽旧水面的记录。
- 沿用每秒 30 次、突发 6 次的效果预算，以及已有距离渐退、裁剪。
- 水面 shader 对无效时间和远离命中的像素提前退出；没有新 Tick、渲染目标或水面碰撞体。
- **不重烘、不重导、不修改已认可的水花 atlas 和 M_RiverCrown**。保留 43 个空帧透明修复、有效帧截断及这张小图的 mip 驻留设置。只调整 Niagara 的浅水分支。

## 制作入口与交付边界

- `Tools/Fluids/export_water_footprints.py` 导出已有水面几何；`generate_water_footprints.py` 生成原生轮廓。网格造型改变后须重新导出并构建。
- `Tools/Fluids/author_water_impacts_all.py` 保存河流、喷泉、积水材质及共享水花系统；原始喷泉/积水制作器也保留叠加层。
- 保存回执：`SourceAssets/WaterImpactAll20260924/assets-saved.json`。目标资产原件放在该目录 `Before-*`。
- 原生部分需要常规 Editor 构建；不使用 Live Coding 交付新增成员布局。
- 没有启动游戏、截图、运行测试或测量帧率。源码、资产保存与构建状态分别报告，观感和性能由用户测试。

## 本轮落盘状态

后台资产制作于 10:40 完成，三个水面材质与共享 Niagara 系统已保存。制作器在完整图表连接后执行必要编译并成功保存；组装新增引脚过程中的临时断连警告不代表最终图表失败。

本轮水体相关源文件与最终 Editor 模块已完成常规构建，`UnrealEditor-FPSGAME.dll` 于 10:43 链接成功。构建日志为 `Saved/BuildEditor/build-20260924-104320.log`。此前并行 UI 修改造成的临时构建阻塞已解除；本任务没有覆盖 UI 源码、替换旧对象文件或绕过构建。下次启动项目使用该模块，不自动启动编辑器或游戏。
