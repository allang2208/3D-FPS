# 玩家第三人称动作复审修复 — 2026-09-22

对应 `player-body-reaudit-20260922.md` 中的六项问题。仅调整世界身体的表现层及一个翻越只读接口，沿用第一人称动作资产、伤害、移动、弹药和存档规则。

## 修复内容

1. 阴影：相机只更新 owner 可见性，不再预先打开阴影。身体、服装和装备统一计算最终阴影状态；第三人称保留投影，第一人称按诊断开关控制，隐藏整个身体或收起装备时同步关闭普通／隐藏投影。修正身体 HiddenInGame 的差异判断，避免恢复显示时仍保持隐藏。
2. 双持施法：世界武器和所有复制配件记录所属手。施法时仅隐藏副手枪及其附件、投影；主手保持独立表现。收手或中断后保留约 0.1 s 隐藏缓冲，与手部返回过渡衔接。
3. 双持近战：两手分别计算持握与击打姿势，按实际出手侧同时施加位移和转腕。左手出击不再给右手添加击打旋转。
4. 施法恢复：进入 Recover 时捕获上一帧实际第三人称左手变换，在剩余恢复进度内直接返回当前持握姿势。不再从完整 Gather／Push 姿势重新开始，也不叠加通用 0.1 s 过渡而改变恢复路径。
5. 翻越松手：采集翻越执行器的实际支撑权重，使用相同的接触／释放窗口，Release 时支撑权重归零。中断后停止追踪旧障碍物接触点，使用已有手部过渡返回持械；世界武器在返回缓冲后再显示。
6. 双持装备：新增逐手 `bEquipping` 状态，各自沿源 equip 动作进度从低位举起并恢复持握。一只手的装备或换弹不再混入该手的奔跑抬枪偏移。

## 实现边界

- 新增的逐手装备状态及翻越支撑权重沿既有身体状态结构复制；没有新增客户端玩法 RPC，也不代表完整多人玩法已经接通。
- 可编辑姿势仍位于 `FPSPlayerBodyAnimInstance.cpp` 与 `FPSPlayerBodyPoses.h`；没有新增或替换动画／材质资产。
- 本次不扩展地形脚部 IK、第三人称枪械机械动画或新的玩法动作。

## 构建与测试

源码修复已保存。用户关闭编辑器后，`Tools/Build/Build-Editor.ps1` 经编译互斥等待进入构建；`Saved/BuildEditor/build-20260922-182826.log` 被三处 C4458 局部变量遮蔽成员错误阻断。已仅重命名 `DevelopmentPerformancePanel.cpp` 的 `Status` 为 `GenerationStatus`，以及 `AuthoredDungeonGenerator.cpp` 的局部 `Role`／`LightModules` 为 `RoomRole`／`StagedLightModules`，未改变逻辑。

再次构建前，脚本检测到 18:31 新启动的 FPSGAME 编辑器并停止。用户关闭窗口后，后台 PID 96096 仍存活；等待 45 s 未退出，没有主窗口。经既有桥尝试只读查询未保存状态时，Python 客户端未发现可连接节点，未能获得保存状态。用户随后明确授权结束 PID 96096；核对进程名称、项目路径和创建时间后，仅结束了该进程。

最终常规 Editor 构建成功：`Saved/BuildEditor/build-20260922-183745.log` 返回 `Result: Succeeded`、`Target is up to date`，本次最终调用执行 0 个构建动作。采用工程当前完整目标的增量构建结果，没有使用 Live Coding 或仅替换带后缀的游戏 DLL。编辑器可重新打开。

交付时 `Binaries/Win64/UnrealEditor-FPSGAME.dll` 更新时间为 2026-09-22 18:36:40，大小 11,599,360 字节。

按用户默认规则，不主动运行测试、PIE 或视觉验收；由用户实机测试。
