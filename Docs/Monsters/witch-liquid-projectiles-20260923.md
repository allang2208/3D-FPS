# 巫婆普通毒弹液体表现（LiquidProjectile13，2026-09-23）

用户要求巫婆普通攻击投射物参照毒蛆优化。本次复用现有毒蛆液体材质和 `UPoisonMaggotVenomFX`，修改 `WitchProjectile.cpp/.h`。

普通毒弹由湿润外层和不透明浑浊内核组成，沿飞行方向轻微拉长。飞行中增加小幅体积补偿形变、内核起伏和转动；外层使用 `M_VenomLiquid`，内核使用 `M_VenomCore`。相位采用独立视觉随机流。

每 0.065 秒沿实际飞行线段生成液滴，每三次加入薄雾；单帧最多补发四次。撞击时在接触点生成飞溅，墙地表面使用共享系统的短暂湿痕，角色命中只生成飞溅。所有附属视觉均复用世界内固定容量的池：192 个液滴、48 个薄雾、40 个湿痕贴花。没有为每个液滴新增 Actor、伤害或碰撞查询；这属于成本控制设计，未测量实际帧率。

保留普通攻击三发扇形、速度、距离、伤害、碰撞筛选与释放时刻。只在普通毒弹分支启用内核和尾迹；投瓶继续沿用 FlightDistance12 的释放变换、真实接触落地和毒区逻辑。本次不改角色网格、服装、动画，也不重建毒蛆共享材质。

源码修改前备份在 `SourceAssets/WitchRebuilt20260921/Revision13/Before`。本轮构建与接入状态写入同级 `delivery.json`。2026-09-23 常规 Editor 构建成功，日志为 `Saved/BuildEditor/build-20260923-072738.log`；未使用 Live Coding。已发起编辑器重开，恢复 `/Game/GameMaps/L_Dungeon_Randomized`，启动记录见 `Revision13/editor-reopen.json`。材质直接引用已有资产，不需要重导入。

按用户规则，本轮不启动 PIE，不进行截图、渲染、运行回归或性能测试。游戏内外观与体验交由用户测试。
