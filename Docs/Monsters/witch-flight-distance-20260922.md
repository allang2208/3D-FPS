# 巫婆投瓶与距离预算修复（2026-09-22）

接续 [专项检查](witch-review-20260922.md)，用户已要求修复。范围仅为 Witch 飞行弹体、布料距离切换和中远景 LOD；沿用 Fabric09 材质修复及现有手指、手臂动作。

## 投瓶交接

- 弹体增加独立场景根节点，飞行位置与扫掠中心使用瓶子网格的本地包围盒中心。
- 从手中瓶子复制离手瞬间的世界旋转和缩放；可见网格相对中心反向偏移，使释放前后完整网格变换连续。
- 自转绕投掷方向的横向轴叠加在离手姿态上，不再每帧覆盖成世界固定朝向。
- 落池时清除瓶子模型的相对偏移，确保液面与真实碰撞点一致。
- 0.75 s 释放时刻、1.5 s 原始弧线阶段、原伤害和毒池脉冲合同保持。

## 无地面落点

- 删除到达目标点就直接 `Land(Goal, UpVector)` 的分支。
- 1.5 s 弧线结束后若仍未发生碰撞，使用弧线终点速度与世界重力继续下落，位置和速度连续。
- 只有真实扫掠/地面查询命中才生成毒池；始终没有命中则由既有 8 s 飞行寿命回收。墙面等非承接面仍不生成毒池。

## 距离切换与 LOD

- 20 m 内恢复布料、24 m 外请求暂停；中间距离保持上次状态。
- 0.35 s 混合到蒙皮姿态后才暂停模拟；恢复时重置模拟并渐入。
- 布料模拟/过渡期间固定使用原 LOD0，避免精简网格与近景布料绑定混用。
- LOD1/LOD2 以原网格为源，目标三角面比例为 50%/20%，屏幕尺寸阈值 0.09/0.035。保留骨骼边界、几何开口和顶点颜色边界，不精简 LOD0。
- 接入脚本 `Tools/WitchRebuilt/install_distance12.py` 只修改 `/Game/Monsters/WitchRebuilt/SK_WitchRebuilt`。生成时使用 UE 的布料解绑/恢复流程；已有 `UWitchRebuiltClothingAsset` 在恢复 LOD0 时重新应用有界映射。
- 实际生成顶点数、保存状态以 `SourceAssets/WitchRebuilt20260921/Revision12/distance_lods.json` 为准；没有用目标比例冒充实际减面量或帧率收益。

## 构建与接入记录

- 源码备份：`Revision12/Before/Source`，附 SHA-256 清单。
- 常规 Editor 构建成功：`Saved/BuildEditor/build-20260922-184404.log`。此前构建被正在使用的编辑器拦截；用户随后允许在无未保存资产、无试玩时正常关闭并继续。重试时已没有 FPSGAME 编辑器进程，没有强制结束进程。
- LOD 接入完成并保存：原 LOD0 为 193,755 个渲染顶点，中景 LOD1 为 138,291，远景 LOD2 为 65,111，均为 6 个渲染分区；保留 `LowerDrape07` / `UpperDrape07`。记录见 `Revision12/distance_lods.json`，原网格备份在 `Revision12/Before/SK_WitchRebuilt.uasset`。
- 减面器报告源网格约 1.65% 边由两个以上三角面共享，构建也记录了零长度法线提示；本轮没有据此改动近景拓扑。中远景轮廓与阴影仍需用户实际画面确认，减面保存成功不等于视觉验收。
- 后续重导入显示网格后，运行 `install_distance12.py` 恢复这两级距离 LOD；不要对 LOD0 开启自动减面。
- 本轮没有启动 PIE、运行战斗/布娃娃回归或测量帧率；实际画面与试玩结果由用户确认。
