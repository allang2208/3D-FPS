# 巫婆达标后的专项检查（2026-09-22）

**后续修复已接入：[FlightDistance12 修复记录](witch-flight-distance-20260922.md)**。两处投瓶问题、布料距离渐变和两级中远景 LOD 已完成常规编译及资产保存；下表保留检查当时的发现与优先级。

范围：当前唯一 `WitchRebuilt`，检查资产引用、统一布料、动画时钟、手持物到弹体的交接、落地毒池、死亡与布料距离策略。保留用户已认可的身体比例、手指动画和服装几何。

## 发现

| 优先级 | 问题与触发条件 | 证据 | 处理建议 |
| --- | --- | --- | --- |
| P1，已修 | Fabric09 布料主材质缺少 Clamp 输入，SM6 编译失败并回退默认材质。上袍、下袍和内衬共享这一主材质，因此“看起来统一”不能证明预期布纹正常生效。 | `Saved/Logs/FPSGAME.log` 本次进程启动的 `2026.09.22-09.52.28:354`，明确报告 `Missing Clamp input`；实际输入引脚名是 `None`，原脚本硬写 `Input` 并忽略了连接/编译失败。 | 已连接原有 `MaterialExpressionAdd_1` 到 Clamp 首个输入，编译返回零错误并成功保存。制作脚本同步使用实际引脚名，并处理连接/编译失败。 |
| P2 | 药瓶离手时变换不连续。手中瓶子的局部 Z=9 cm 点被用作弹体原点，但飞行模型未抵消这段原点偏移；生成旋转为零，随后改用世界固定俯仰自转。持瓶朝向越倾斜，交接越明显。 | `WitchMonster.cpp::ReleaseSpell` 与 `WitchProjectile.cpp::Launch/Tick`，仅复制网格和材质，没有复制释放姿态。 | 飞行弹体以瓶体中心运动，可见模型相对中心偏移；继承离手旋转，再围绕投掷方向叠加自转。保留 0.75 s 释放时刻，不重做已认可的手部动作。 |
| P2 | 无地面落点也会生成毒池。向坑洞/平台外投掷，起始向下查询和全程碰撞均未命中时，1.5 s 后仍直接 `Land(Goal, UpVector)`，凭空假定水平地面。 | `WitchProjectile.cpp::Launch` 未记录地面查询失败；`Tick` 末尾无条件落池。 | 只有真实有效的地面命中才生成毒池；未命中应继续坠落或销毁。不能凭目标点伪造地面与法线。 |
| 优化 | 22 m 处布料权重从 1 跳到 0，跨阈值又恢复并重置模拟；反复在阈值附近移动可能出现衣摆跳变和重复重置。 | `WitchRebuiltMonster.cpp::Tick`。 | 增加不同的启用/停用距离与短过渡；不要扩大近景布料复杂度。 |
| 优化 | 当前只有 1 级模型 LOD，含 193,755 个渲染顶点、6 个渲染分区；22 m 外停止布料，但没有随距离减少模型复杂度，动画仍继承 `AlwaysTickPoseAndRefreshBones`。 | 非 PIE 资产读取 `lod_count=1`、`lod0_vertices=193755`、`lod0_sections=6`，`NurseZombie` 构造函数与派生类距离策略。 | 多只怪物场景优先补中远距离几何/动画预算；需同场景多只 A/B 数据后确定阈值，不能只凭这一配置断言当前卡顿。 |

## 已确认的接入状态

- F6 默认怪物目录只有 `WitchRebuilt / 巫婆·重建候选` 一条巫婆入口。
- 8 段当前动画都能加载，全部引用独立的 `SKEL_WitchRebuilt`；施法、投瓶和死亡片段各 1.5 s，受击及左右转身各 1 s，步行 1.5 s。
- 上下袍和内衬都引用 Fabric09 实例；两个布料资产仍为 `LowerDrape07` / `UpperDrape07`。主材质为不透明、双面；作者脚本使用两张细节纹理，没有透明叠层或材质位移。
- 当前死亡代码先播放死亡动画，再进入有约束的全身物理；没有重新引入独立腿部模型。当前 Physics Asset 只读检查为 16 个刚体、15 个关节，各关节参考锚点误差均为 0 cm；没有重写物理资产。此结论不代表本轮完成倒地视觉测试。
- 当前保存的两层布料配置均为 4 次目标迭代、最多 6 次、1 子步；上袍不开 CCD 和自碰撞，下袍保留 CCD 与球体自碰撞。未恢复早期高成本配置。

## 检查边界与记录

- 资产记录：`SourceAssets/WitchRebuilt20260921/Revision11/asset_audit.json`。
- 首次采样时已有另一场 PIE，场景内没有巫婆，因此未启动专用测试。材质修复后只读资产检查时已退出 PIE，取得了模型统计；随后再尝试采样时编辑器已经正常退出，桥未找到节点。没有停止其他试玩、关闭或重新启动编辑器。未取得本轮实际帧率、布料模拟耗时或投瓶画面，不引用旧版测试数字作为当前结论。
- PIE 期间编辑器顶点/分区统计 API 不可用；早期返回的 0 / -1 是接口拒绝执行，不能解释为模型没有顶点。
- 投瓶两项为确定的代码分支问题；本轮未改原生战斗代码。优化项为下一轮建议，不宣称已造成实际掉帧。
- 材质修复记录：`Revision11/fabric_repair.json`，`before_inputs=[null,null,null]`，`after_inputs=[MaterialExpressionAdd_1,null,null]`，`compile_errors=[]`、`saved=true`；只改现有主材质的一条连接，未增加采样、透明效果或物理开销。原材质备份在 `Revision11/Before/`，记录含 SHA-256。
- 桥两次等待超时后重试成功；没有绕过互斥、联系其他任务、结束其他试玩或重启编辑器。修复后的布纹/颜色视觉未在本轮试玩确认，不能把编译通过等同于画面验收。
- 本轮新增检查与修复脚本已通过 Python 语法解析；临时 PIE 采样两次均未启动，没有生成任何帧率/死亡测试结果。

建议顺序：先修布料编译，再修瓶子离手交接和无地面落池，最后根据多只怪物采样调整距离预算。当前已认可的近景模型和动画不需要整体重做。
