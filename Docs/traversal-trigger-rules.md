# 翻越与攀爬触发规则

## 高度与动作

高度 H 从人物当前脚下支撑面的高度量到障碍顶部，使用厘米。不能从相机、胶囊中心或蹲下后的视点量高度。

| 高度 | 候选行为 |
|---|---|
| 0 < H ≤ 50 cm | 原有 CharacterMovement 自动上台阶，不播放翻越或攀爬 |
| 50 < H ≤ 120 cm | 优先翻越，几何条件不满足时尝试爬上平台 |
| 120 < H ≤ 200 cm | 攀爬到顶部 |
| H > 200 cm | 不触发本系统 |

精确边界：50 cm 属于台阶；120 cm 属于优先翻越；200 cm 允许攀爬。当前胶囊体半高 96 cm、半径 42 cm，台阶高度 50 cm。高度是玩法选定范围，不代表官方动画已经在这些高度上通过手部接触验收。

## 共同触发条件

- 玩家站立且在地面，贴墙并面向障碍持续按住空格 0.25 秒。走路贴墙不自动触发。蹲下、滑铲、空中、武器忙碌和已有攀爬过程中不触发。
- 胶囊前缘到障碍近侧面的水平间距不超过 20 cm（沿墙面法线测量）；朝向与障碍迎面方向夹角不超过 40°。
- 贴墙短按在松键后执行普通跳跃；没有合格墙面时仍按下立即走原有跳跃流程。长按等待期间转身、离墙、切换目标墙、离地或武器忙碌会取消，本次按住不自动重试，需松开重按。UI 禁止移动时也取消。
- 第一阶段只处理静态、阻挡人物的实体障碍；植被视觉模型、移动物体不作为候选。
- 必须存在可行走的顶部、无遮挡的接近空间以及对应动作路线。
- 规则返回 None 时不能消费跳跃输入；返回 Step 仅表示交由原有移动组件处理，并非强制抬升人物。

## 翻越与爬台的区别

翻越：高度不超过 120 cm，沿前进方向的障碍厚度不超过 120 cm，路线畅通，背面有可站立落点。背面落点相对起始地面允许低 60 cm、最多高 50 cm，边界包含。无安全落点的薄栏杆、悬崖边缘不翻越。

爬台：高度不超过 200 cm，顶部沿前进方向至少有 100 cm 可用深度，顶部站立空间和爬上路线均畅通。宽矮箱子也采用爬台；背面被挡但顶部能站立的低平台也可以爬上去。高而薄、顶上不能站立的墙不触发；悬挂、骑墙和自由攀岩不属于本阶段。

检测器接入时，顶部/落点的站立空间需用实际站立胶囊查询，四周增加余量；按现有角色先采用至少约 100 cm 宽、202 cm 净高的空间校验。不能仅凭一条射线命中就把布尔条件设为 true。人物经过的路线也必须进行碰撞检查。

## 实现与验证范围

- `Config/DefaultGame.ini` 的 `[/Script/FPSGAME.FPSTraversalSettings]` 是阈值配置。
- `Movement/FPSTraversalRules.h/.cpp` 提供高度分类和完整候选判定，暴露给蓝图；无 Tick，不移动角色，不读写存档。
- `FFPSTraversalProbe` 是后续碰撞检测器的输入合同，所有布尔条件默认 false，避免未测量即准入。
- `FPSTraversalRulesAudit` commandlet 检查阈值分界、朝向/距离、宽低平台回退、背面落差、缺失条件以及与现有台阶高度的一致性。
- `FPSTraversalComponent` 已实现场景射线、顶部边缘宽度、落点支撑范围、站立净空和分段胶囊路线检测。角色创建组件，跳跃按键调用检测，只有等待长按成立期间逐帧复核同一墙面和可用状态；闲置不扫描。真正开始动作时保存 `LastJumpTarget`。
- `FPSTraversalExecution.cpp` 已接入单机执行器。动画存在且检测通过时消费跳跃，执行带碰撞检测的位移和原手模动作；不合格或缺失动画时保留普通跳跃。

### 本次验证（2026-09-11）

Editor 构建成功，模块 `UnrealEditor-FPSGAME-2026093107.dll`。独立进程运行 `-run=FPSTraversalRulesAudit -nullrhi -unattended`，日志 `Saved/Logs/TraversalRulesAudit.log`：`TRAVERSAL_RULE_RESULT checks=39 failures=0`。

规则断言全部通过；进程整体退出码为 1，启动阶段另有 GameFeatureData 扫描规则缺失和 MCP 8000 端口占用错误，不能将此次报告称为引擎启动零错误。没有为规则验证修改其他系统或关闭用户编辑器。

### 场景碰撞检测验证（2026-09-11，后续）

最新 Editor 构建 `UnrealEditor-FPSGAME-2026091103.dll` 成功。独立测试进程用 `-DisablePlugins=ModelContextProtocol,AllToolsets` 避开不需要的编辑器工具插件，项目配置不变。`Saved/Logs/TraversalWorldAudit-v3.log` 中规则 39 项、独立物理世界碰撞 18 项全部通过，退出码 0，0 错误、0 警告。

物理测试使用实际 Character 胶囊和静态碰撞盒，覆盖 50/50.1、120/120.1、200/200.1 cm 边界，薄矮墙、宽平台、超距、空中、接近路线低顶、落点受阻、高薄墙、过窄支撑边、移动障碍、平台净空不足、背面受阻改爬台以及背后无地面的拒绝。不是只向规则函数传入预设布尔值。

检测实现采用保守的“抬升—跨过—下降”胶囊路线作为几何准入。后续动画执行器必须继续验证自己的实际运动路线，不能把这些路径点直接用于无动画瞬移。厚度查询仅针对目标碰撞组件，避免背面其他物体干扰宽平台爬台回退。

运行游戏时可附加 `-TraversalProbeDebug`，长按到触发时日志输出 `TRAVERSAL_PROBE`；合格目标画黄色边缘标记与绿色路线，持续 3 秒。此开关只控制调试显示。正常游戏不显示调试标记。


### 动画执行器（2026-09-11）

薄矮墙使用 Vault 前 0.8 秒；宽矮平台使用 2 秒 Mantle；120–200 cm 高台使用 3.3333 秒 Climb。资产路径由同一配置节指定，当前手模为 `SK_TraversalArms_AnimatedBounds`。它保留原网格、蒙皮、骨长和材质，增加按实际变形网格测量的动画包围盒，避免高位手臂被错误裁剪。

角色按抬升、跨过、下降路线逐段 Sweep；长帧也访问转折点，途中受阻、障碍销毁或移动时退出并恢复重力。相机跟随手部参考动作，保留视角输入，另做相机球体碰撞查询。执行期间锁住射击、ADS、换弹和滚轮切枪，外部换装备会中断动作；滑铲原有战斗功能不变。检测只在请求跳跃时运行，动画仅在动作期间求值。

规则与物理场景最近通过记录为 `Saved/Logs/TraversalWorldAudit-v4.log`：39 + 18 项，退出码 0。实际游戏五组检查见 `TraversalRuntime-v3.log`：0 失败；图像确认当前手模可见。镜头与抓边精修后的 `TraversalRuntime-v4.log` 六组双武器检查以及 `TraversalSlideRegression.log` 滑铲战斗回归均为 0 失败；完整状态见 `gasp-traversal-20260910.md`。运行覆盖不代表已测得 FPS 提升，也不包括联机或自由攀岩。

### 贴墙长按输入更新（2026-09-11）

12 cm / 25 度 / 0.35 秒已接入。Editor 模块 `UnrealEditor-FPSGAME-9111237.dll` 构建成功。`TraversalHoldRules.log` 中规则 39 项、碰撞 18 项全部通过；commandlet 退出码受已有 GameFeatureData 配置及编辑器工具端口占用错误影响，不将其记为干净退出。`TraversalHoldRuntime.log` 六组实机验证 0 失败，独立游戏退出码 0，覆盖短按、未达长按时长、转身/离墙取消、两套武器的动作执行和状态恢复。帧图位于 `Saved/TraversalRuntimeAudit/ColdSteel_TraversalRuntimeAudit_Hold_20260911/`。已打开的旧编辑器需要重启以加载新模块。

### 用户反馈只跳跃：复现范围（2026-09-11）

新增 `-TraversalCourseAudit`，直接使用初始地图已保存的四个测试障碍。角色通过正常移动顶墙，再通过 PlayerController 空格输入事件长按。`TraversalCourseBefore.log`（距墙 6 cm）、`TraversalContactBefore.log`（移动顶墙）、`TraversalKeyBefore.log`（空格输入事件）及 `TraversalPIEBefore.log`（编辑器内运行）均四组 0 失败。这些结果尚未复现用户的失败操作，不能作为该反馈已修复的证明。`BeginJumpHold` 新增 `TRAVERSAL_INPUT_REJECT` 日志，记录未触发原因、角色位置、距离、朝向、地面及可用状态，供下一次用户复现定位。未改变现有高度和长按门槛。

使用 `ColdSteelPlayer_A/B` 的独立存档副本执行 `TraversalUserCopy.log`：四种墙均能通过空格长按进入并完成动作。原测试断言假定持有武器，因此空手存档产生 4 个武器可见性断言失败；这属于原持枪测试断言不适用于空手状态，不能把整组测试记为全通过。原存档未改动。用户操作失败仍待当前编辑器拒绝日志确认。

### 已定位：轻微斜视导致墙沿误拒绝（2026-09-11）

用户真实输入日志 `FPSGAME.log` 04:10:58–04:11:02 UTC 在绿色矮墙和蓝色高墙前连续报告 `NarrowOrBrokenEdge`，距墙 0.12–0.40 cm，FacingDot 0.992–0.997，均在朝向限制内。旧探测沿人物 Right 方向在距前缘仅 2 cm 处左右采样约 49 cm；稍有偏转就把一侧采样点移出实体。之前测试全部正对墙面，未覆盖此问题。

修复保留玩家朝向作为入口约束，命中墙面后改用墙面水平内法线与切线建立几何采样坐标系，统一用于顶部、深度、支撑区域和路径目标。新增宽台各高度及薄墙的 ±7°、±24° 场景检查，并保留 26° 拒绝；初始地图四障碍实机测试使用 +7°、-7°、+20°、-20° 后经空格输入触发。

验证：Editor 模块 `UnrealEditor-FPSGAME-9111512.dll` 构建成功。`TraversalWallBasisRules.log` 规则 39 项、物理场景 39 项全部通过，退出码 0；`TraversalWallBasisRuntime.log` 初始地图四种障碍运行用例全部通过（cases=4, failures=0），退出码 0。帧图位于 `Saved/TraversalRuntimeAudit/ColdSteel_TraversalRuntimeAudit_WallBasis/`。当前已打开的编辑器需保存并重启以加载修复；本轮未修改动画资产或触发门槛。

### 触发手感放宽（2026-09-11）

贴墙距离调整为 20 cm，最大朝向偏角 40°，长按时间 0.25 秒；配置与 C++ 默认值同步。检测射线按允许偏角延长，实际距离沿墙面水平法线测量，避免在允许范围内斜视时触发距离缩水。高度、顶部支撑、落点及路径碰撞条件保持原规则。物理场景增加 ±39° 和接近 20 cm 距离的组合检查；规则边界为 20 cm/40° 可用、20.1 cm/41° 拒绝。

验证：Editor 模块 `UnrealEditor-FPSGAME-9111628.dll` 构建成功。`TraversalRelaxedRules.log` 规则 39 项、物理场景 52 项全部通过；`TraversalRelaxedCourse2.log` 初始地图四种障碍在约 17 cm 间距、±35°/±39° 朝向下运行 4 组零失败；`TraversalRelaxedInput.log` 短按、长按、转身/离墙取消、障碍销毁中断及两把武器恢复共 6 组零失败。已查看高墙动作帧。首次 `TraversalRelaxedCourse.log` 因测试脚本跨帧使用重置的局部 Origin 将角色移到空中而失败，修正为从障碍实际包围盒定位后重跑，不作为玩法失败或通过依据。用户编辑器需保存并重启加载。

### 镜头交接、尾段压缩与表面接触（2026-09-11）

- 源动作仍使用 Native/authoring.json 的 60 Hz 非循环动画：Vault 0.8 s、Mantle 2 s、Climb 3.3333 s；接触/松手时点分别为 0.27/0.58、0.345/1.1、0.74/2.25 s。先检查官方动作表及原生手模抓墙/收手帧，再增加运行时姿态修正，未改动源网格、蒙皮、骨长、FBX 或 Blend。
- 保留抓墙/撑起时序，松手后重映射源时间，运行总长分别为 0.8、1.4、2.55 s。人体沿完整碰撞校验路线到达安全落点才结束，不按镜头是否越墙判断成功。结束时重新找地面，并衔接仍按住的移动方向，取消额外原地等待。
- 动作面向实际墙面法线。抓墙阶段视角逐渐收至墙面左右 20 度、上下 15 度，直接约束 Controller，避免隐形鼠标角度积累。松手后 0.12 s 收手离开画面，然后逐渐恢复自由观察和持枪。相机位置在松手到结束之间回归站立视点，保留沿途球体避碰。
- FPSTraversalArmsComponent 在手臂骨骼缓冲发布前修正姿态：每只手独立探测近侧面与顶部，锁定自身接触位置及表面法线；手套、手指和前臂用有半径的扫掠探测修正。两节骨骼 IK 保持骨长，手指和 twist 辅助骨沿原局部姿态重建。查询遵循人物碰撞 Profile，仅在动作接触区间执行，姿态数组复用。
- 落脚区域从四角采样扩为九点，允许相对局部支撑平面最多 8 cm 的起伏，采用采样最高位置检查站立胶囊和过沿高度。缺失支撑、陡面、头顶不足和路径阻挡继续拒绝。
- 这是基于碰撞形状和有限采样的通用适配，不是逐三角形的手指物理模拟。可用接触范围受手臂可达性约束；缺失/过粗碰撞、深凹槽、大悬挑及超出采样容差的平台需要修整碰撞或专用动作，不能据平墙/斜台测试宣称任意模型零穿模。

验证：最终 Editor 模块 `UnrealEditor-FPSGAME-9111750.dll` 编译成功。`TraversalSurfaceRulesFinal.log` 规则 39 项、碰撞场景 55 项（含 ±6 度斜台和非方盒、倒角凸面起伏平台）全部通过，退出码 0。`TraversalSurfaceFinal.log` 六组运行检查零失败，覆盖强制转头、骨长保持、表面修正、短按/取消、移动立即接续和两把武器恢复；`TraversalSurfaceCourseFinal.log` 初始地图四障碍零失败，均退出码 0。后者固定 60 Hz 捕获用于观察动作，不是性能测量。预览为 `Saved/TraversalRuntimeAudit/ColdSteel_TraversalRuntimeAudit_SurfaceCourseFinal/traversal_runtime.gif`，按捕获顺序播放动作片段，不额外添加结尾停顿。

中间 `TraversalSurface2.log` 的六项失败来自旧位置断言要求恢复移动后仍停在终点，以及一次长渲染帧跨过长按时限。断言改为记录 Finish 当刻的位置后再检查持续移动，另以显式 150 ms 子阈值输入覆盖短按；最终日志以上述两组为准。当前用户编辑器需保存并重启以加载新模块。
