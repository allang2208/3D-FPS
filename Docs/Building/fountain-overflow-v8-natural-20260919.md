# 蛋糕塔喷泉：V8 自然随机性（2026-09-19）

用户认为 V7 方向正确，但水流之间仍然太规则。本轮只调整边缘落水和附加飞沫，保留盆水、
V7 单层水膜几何、石材、碰撞、占格与存档。未运行游戏、截图或性能测试，由用户体验。

## 水膜

- 新材质放在 `/Game/Props/RomanFountain20260917/OverflowV8/`；V7 资产保留。
- 原固定频率正弦条纹换成原创 256×256 RGBA 周期噪声数据，环向无硬接缝。
  噪声由作者脚本按固定种子生成，源文件为 `fountain_flow_noise_v8.tga`，无需外部素材。
- 慢变化控制出水强弱、水束宽度和侧向摆动，较快的小尺度变化控制断裂、法线与局部白沫。
  使用 `当前时间 - 飞行时间`，使变化随下落传播；各层、各座喷泉使用不同偏移。
- 盘沿的位移固定为零，离开盘沿才逐渐增强；新增少量竖直扰动，在起止端归零。
  不改变水膜顶点数，不新增水膜层或绘制组件。
- 近景噪声场两次纹理采样，远景一次；近景的小尺度细节在 30–40 m 渐退，
  两档共享同一慢变化场和材质参数，减少切档时水束位置变化。

## 飞沫

- `NS_FountainLandingSprayV8` 以 V7 系统副本制作，仍是一个 GPU 发射器。
- 层级从固定轮流分配改为加权随机（从上到下 15% / 30% / 42% / 13%）。
- 落点从均匀绕圈改为不等间距的小簇，并混合 22% 的分散飞沫。簇的位置缓慢漂移。
- 半径、上抛/外向/切向速度、宽高和透明度分别随机；只在出生时计算并保存。
  更新阶段只计算解析轨迹，不重新随机，避免飞沫瞬移或闪烁。
- 发射速率以平滑随机值在原预算的 62%–100% 之间变化；默认/高档上限仍为 360/480 粒子每秒，
  最大寿命仍约 0.357 s。20/40/62 m 分级及每世界最多 4 座附加飞沫系统保持不变。
- 随机簇提供落点疏密变化，并非逐滴流体模拟；不宣称飞沫与水膜每个缺口精确耦合。

## 接入与制作入口

- 作者脚本：`SourceAssets/RomanFountain20260917/build_cascade_v8_natural_20260919.py`。
  V7 脚本的制作入口加了 main guard，可导入公用函数而不重写 V7 资产。
- `AColdSteelFountain` 使用新材质/飞沫资产，并在 BeginPlay 向 Niagara 传入本座种子。
  不新增反射字段或每帧 CPU 更新；既有 0.25 s 更新负责距离细节参数。
- 制作回执和构建记录放 `Saved/FountainOverflowV8/`；图形表现与 GPU 帧时间未测。
  资产已生成并保存，Editor/Game 原生构建均返回 `Succeeded`；Editor 模块后缀为 `9191808`。
  未热重载或重启正在运行的编辑器；用户重启后加载新代码。
  资产命令行制作日志为 `Saved/FountainOverflowV8-authoring-cmd.log`。
- 正式水声与塔尖水柱改造仍属原有待办，不包含在本次随机性改进中。
