# 2×2 m 双开门（2026-09-18）

用户要求：在做完 100×100 双开窗之后，做一扇 **2×2 m 的双开门**。开合逻辑完全沿用窗那套（本来就是"双扇平开"），
门只是另一组尺寸与网格。**按 AGENTS.md 全局规则：本批未做实机验收，进游戏测试由用户执行。**

## 1. 成品一览

| 项 | 位置／入口 | 说明 |
| --- | --- | --- |
| 门框网格 | `/Game/Props/DoubleDoor20260918/SM_DoubleDoorFrame_200` | 20 × 200 × 200 cm，48 三角面，4 个闭合盒体，碰撞 `AlignedBoxes` |
| 门扇网格 | `/Game/Props/DoubleDoor20260918/SM_DoubleDoorLeaf_200` | 板 5 × 91.5 × 183（包围盒 X 含把手）＋圆形把手，236 三角面，8 个闭合盒体，左右两扇共用 |
| 门 Actor | `Source/FPSGAME/Building/ColdSteelDoubleDoor.h/.cpp` → `/Script/FPSGAME.ColdSteelDoubleDoor` | `AColdSteelWindow` 的子类，E 键开关（入口 `ToggleWindow`） |
| 建造面板 | 木材／石头／大理石 → 「其他构造」 | `double_door_wood` / `double_door_stone` / `double_door_marble`，占格 **(1,10,10)** = 20×200×200 cm |
| 渲染 | `SourceAssets/DoubleDoor20260918/preview_20260918/*.png` | 三材质对照表＋正视、内侧、开门外／开门内、俯视、中缝把手特写 |

几何口径（cm）：门框 20(X 进深) × 200(Y 宽) × 200(Z 高)，边梃 **8** → 洞口 **184×184**；
两扇各 91.5 × 183 × 5 对开（门扇比窗扇厚一档），中间留 1 cm 缝；门扇＝边梃 10 ＋ **下冒头 20**（门扇特征）
＋ 2 cm 凹面板 ＋ 靠中缝一侧的圆形把手（Ø8 圆盘、贯穿杆，距门扇底边 100 cm ≈ 离地 1.08 m）。
门框 20 cm 厚 ＝**一格体素**，正好填满玩家在 20 cm 墙上挖的 10×10 洞；洞口从地面起，门槛就是框的下边梃。

## 2. 为什么做成子类

`AColdSteelWindow` 本来就是"两扇绕竖直铰链对开"的实现，门与窗的差别只有四样：网格、**边梃宽**、
**板厚**、**自动关**。所以：

1. 把 `FrameMemberCm`（窗 6／门 8）与 `LeafHalfThicknessCm`（窗 2／门 2.5）从 .cpp 的文件级常量
   提升为 `AColdSteelWindow` 的 **protected 成员**（默认值仍是窗的值）；
2. 三个网格组件（`Frame` / `LeafLeft` / `LeafRight`）与开角／开合时长／自动关三个属性也移到 protected；
3. `AColdSteelDoubleDoor` 的构造函数只做三件事：覆盖 `FrameMemberCm=8`、`LeafHalfThicknessCm=2.5`、
   `AutoCloseSeconds=6`（门沿用单扇门那套自动关，窗是保持打开），再把两个网格换成门的。

于是：铰链贴面、被挡反向、玩家不参与阻塞判定、开关过程不挡玩家、整体换材质、构件支撑判定——
**全部零重复**；`SpawnPrefab()` 里的 `Cast<AColdSteelWindow>→Configure()` 也自动覆盖门（子类 is-a 窗）。
E 键入口沿用的是窗的 `ToggleWindow`（`UColdSteelDoorInteraction` 的入口表里已有）。
开角仍是 **85°**（把手贯通、摆向那侧会扫框；门把手距铰链 85.5 cm、凸出 2.5 cm，85° 时余量约 5 cm）。

## 3. 网格怎么来的

`SourceAssets/DoubleDoor20260918/build_double_door_meshes_20260918.py`（无头 commandlet）——
与窗的脚本同源（同一套 `append_box`／`append_cylinder` 闭合盒体拼法、`XAtlas` UV、`AlignedBoxes` 碰撞、
导出三角汤 OBJ 给离线渲染），差别只有尺寸常量与洞口位置：

- 门扇按门的构造分件：两条边梃（10）、上冒头（10）、**下冒头（20）**、凹面板（2 厚，居中于"边梃与冒头之间"的洞口）。
- 把手与窗同一套：贯穿杆 ＋ 两面圆盘，做在网格 **+Y 侧**，右扇在 Actor 里绕 Z 转 180° 让把手朝中缝。

复现（编辑器须关闭）：

```
"E:/Program Files (x86)/UE_5.8/Engine/Binaries/Win64/UnrealEditor-Cmd.exe" D:/FPS3D/FPSGAME/FPSGAME.uproject \
  -run=pythonscript -Script=D:/FPS3D/FPSGAME/SourceAssets/DoubleDoor20260918/build_double_door_meshes_20260918.py \
  -unattended -nop4 -nosplash -NullRHI -nosound \
  -abslog=D:/FPS3D/FPSGAME/SourceAssets/DoubleDoor20260918/build_door_meshes.log
```

自检结果：门框 `20 × 200 × 200`、占格 **(1,10,10) OK**；门扇 `10 × 91.5 × 183`（X 含把手凸出 ±5）、
`open_edges=0`、`RESULT: PASS`。

## 4. 调色板与渲染

`SourceAssets/DoubleDoor20260918/register_double_door_prefabs_20260918.py` 读改写整份 `Components`，
登记三条（ID／显示名／材质行）：

| 条目 | 显示名 | ActorClass | Surface | 归属 | 占格 |
| --- | --- | --- | --- | --- | --- |
| `double_door_wood` | 木双开门（E 键开关·2×2 m） | `/Script/FPSGAME.ColdSteelDoubleDoor` | `Rounded/M_Voxel_Wood` | 木材 | (1,10,10) |
| `double_door_stone` | 石双开门（E 键开关·2×2 m） | 同上 | `Rounded/M_Voxel_Stone` | 石头 | (1,10,10) |
| `double_door_marble` | 大理石双开门（E 键开关·2×2 m） | 同上 | `Props/RomanColumn20260915/M_RomanStone_V2` | 大理石 | (1,10,10) |

独立进程只读核对（`verify_double_door_20260918.py`，日志 `verify_door.log`）：

```
palette=True components=19 materials=['wood','stone','marble']
class=ColdSteelDoubleDoor
SM_DoubleDoorFrame_200   bbox 20.0 x 200.0 x 200.0 origin=(0,0,100) tris=48  box=4 convex=0
SM_DoubleDoorLeaf_200    bbox 10.0 x 91.5 x 183.0   origin=(0,0,0)   tris=236 box=8 convex=0
```

`box=4`／`box=8` ＝每个闭合盒体一个盒：门框的 184×184 洞口**没有被碰撞堵住**。

渲染（`render_door_materials.py`，纯 numpy 离线渲染器，与窗同源）：`door_material_sheet.png`（三材质关门 3/4 对照）、
`door_stone_isolated.png`（去掉墙看比例）、`door_stone_front.png`、`door_marble_inside.png`、
`door_marble_handle_detail.png`（中缝两个把手）、`door_marble_open_isolated.png`／`door_marble_open.png`（向外开）、
`door_wood_open_inward.png`（向内开）、`door_wood_open_top.png`（俯视看两扇摆向）。装配自检：

```
关·  左扇 x=−5.0..5.0   y=−92.0..−0.5   z=8.5..191.5   ← 关着时两扇居中于门框进深（X 含把手 ±5）
开外 左扇 x=2.1..93.7   y=−92.0..−76.7                  ← 铰链贴外面，两扇都摆到 +X（墙外）
开内 左扇 x=−93.7..−2.1 y=−92.0..−76.7                  ← 铰链贴内面，两扇都摆到 −X
```

## 5. 构建状态（2026-09-18 10:55）

| 目标 | 结果 | 证据 |
| --- | --- | --- |
| Game `FPSGAME Win64 Development` | **Succeeded** | `Saved/BuildEditor/door2-game-20260918.log`；`FPSGAME.exe` 10:54:29 |
| Editor `FPSGAMEEditor Win64 Development` | **Succeeded** | `Saved/BuildEditor/door2-editor-20260918.log`；`UnrealEditor-FPSGAME.dll` 10:54:34 |

**中途踩的坑（已修，教训写进工作流 §6）**：加门类时把窗类的成员从 private 挪到 protected（**类布局变了**），
但项目构建脚本带 `-NoUBTMakefiles`，门类的 obj 仍然按**旧布局**编译 → 运行时读 `Frame` 读到 0，
`AColdSteelDoubleDoor` 的 CDO 构造直接 `EXCEPTION_ACCESS_VIOLATION`（崩溃栈就指在门构造函数那一行，
日志里能看到同一构造函数里 `leafL/leafR/hingeL=1` 而 `frame=0` 的错位特征）。补一次让依赖它的 .cpp 重编即恢复。

## 发布记录（2026-09-18）

- 提交：`502bb27`（`origin/main`），发布目录 `D:/FPS3D/FPSGAME`，普通推送 `HEAD:main`，`git ls-remote` 回读一致。
- 验证：Game／Editor 两目标编译 Succeeded；调色板与网格经**独立进程**读回（`verify_*.py`，日志在对应 `SourceAssets/` 目录）。
- 内容依赖：复用既有体素材质与包内 `SM_Door`/`SM_DoorFrame`（未修改包资产）；预览图与日志按忽略规则只留本地。
- **运行时验收仍由用户执行**（清单见本文末节）。

## 6. 已知边界与未做

- 与窗同一套已知边界：没有玻璃（整扇同材质）、只适合一格厚的墙、铰链是"贴面旋转"的简化口径、
  没有音效、构件不参与承重（但**有着落判定**：悬空放置被拒、周围拆光会脱落）。
- 门默认 **6 秒自动关**（沿用单扇门口径）；要改成常开把 `AColdSteelDoubleDoor` 构造函数里的
  `AutoCloseSeconds` 填 0 即可（一处）。
- 门洞是 10×10 格、从地面起，所以**墙至少要比洞口宽两格**（左右各留一格），否则洞口上方那段墙没有侧向连接会塌。
- 未实机实测：编译、资产读回与离线渲染都通过，但**没有进游戏**验证放置、预览对齐、按 E 开合、被挡反向、脱落与存档。

## 7. 交给用户的实测清单

1. 进游戏按 B → 木材／石头／大理石任一栏 →「其他构造」，应看到六个构件卡：三扇窗（100×100）＋三扇双开门（2×2 m）。
2. 在 20 cm 墙上挖出 **10×10、落地**的洞（或先摆门再补墙），放置门：预览与落地应重合。
3. 按 E：两扇朝玩家反侧对开；6 秒后自动关上；被墙挡住时应反向开；拆掉周围方块后门应脱落（提示「构件脱落」）。
4. 拆除（右键门本身）与存档（退出重进）照旧。
