# 背包拖出后落地拾取 · 2026-09-10

用户规则：拖到背包面板外且没有落在快捷栏时，松手即把物品丢到地面，保留可重新拾取的实例。

- ColdSteelHotbarDrag.cpp：有效快捷格仍优先绑定；面板外的视口空白区域释放实体物品，调用已有 StatusModel.Drop。校验实例、原位置，排除快捷栏整体范围、快捷引用拖动及视口外释放。回到面板后的拖动继续走原内部接收规则。取消和应用失焦不执行 Drop。
- ColdSteelProfileRuntime.cpp：Drop 仍把同一实例转为 Place=2 并保存当前地图、数量和属性；新增地面检测，将现有 20cm 拾取物底部放到地面。前方有遮挡时退到遮挡前，前方没有附近地面时尝试脚边；没有可达落点时保留背包物品。存档提交失败时不生成地面物体。
- ColdSteelInventoryDragAudit.cpp：增加实际鼠标外部释放、单一拾取 Actor、地面高度、存档恢复、拾回同一堆叠和保存失败回滚验收。保留既有快捷栏与取消拖动检查。

原件与 SHA256：trash/drag-ground-drop-20260910/。本次精确差异、运行截图和日志：SourceAssets/DragGroundDrop20260910/。未修改其他并行工作，未提交或推送。

原生编译：FPSGAMEEditor Development，2026091093 模块，Succeeded。地面外观使用工程既有拾取物表现，不在此任务中制作物品专属落地模型。运行验收结果待补充。

最终验收：1280×720 独立 InventoryDragAudit 存档、实际鼠标拖出释放；65 项全部通过，进程退出 0。新增测试覆盖失效来源不丢弃、保存失败无地面 Actor、释放后同一实例转地面且堆叠 8 件完整、仅一个拾取 Actor、底部落在地面、重载仍在地面且不重复生成、通过现有 Pickup 事务拾回后地面 Actor 移除且背包仍为 8 件。既有 1–4 号快捷栏绑定、返回面板及失焦取消继续通过。实际检查 ground-drop-1280.png 和 ground-recovered-1280.png。

本轮通过现有模型 Pickup 事务验证拾回，没有修改或单独模拟控制器 E 键优先级。拾取物沿用原有方块占位外观与标签。本轮未运行打包测试。当前打开的 UE 编辑器需重启后加载编译模块 2026091093。
