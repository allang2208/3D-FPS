# 旋转技能：镜头帧同步与连续蓄势

案例：大旋风V4，用户已确认旋转错位修复成功，后续蓄势优化后反馈整体没问题。动作与业务入口见 [大旋风技能](../../ue5-skill-magic-workflow/references/whirlwind-melee.md)。这些数值是当前剑的合同，不是所有武器通用参数。

## 先分清错帧与模糊

1. 先读取实际加载的动画、姿态层及挂点链。手臂和剑应共用同一骨骼采样及 `WPN_root`，技能活动分支应跳过待机、走路、普通攻击覆盖。只凭手和剑重影，不能断言存在两套动画。
2. 如果动作逻辑旋转相机，要看它相对 `PlayerCameraManager` 缓存POV的时序。UE5.8本案例原 `TG_PostUpdateWork` 晚于相机缓存，造成新前景姿态与旧渲染视角错一帧。早期修复在技能活动时改为 `TG_PostPhysics`；2026-09-23 已将共用剑组件全状态固定在该组，并保留角色与移动组件前置依赖，结束/取消不再恢复 `TG_PostUpdateWork`。镜头、手臂和武器先更新，再让引擎缓存视角；突进首帧与袖口边界见 [突刺肘部与开口保护](thrust-elbow-clearance.md)。不要通过同帧反复更新相机掩盖顺序问题。
3. 高速镜头运动的全屏模糊与TSR历史是另一层问题。当前大旋风暂时关闭全屏运动模糊，色调映射后只采样背景；前景手/肩/武器使用模板231和边缘保护。该遮罩不能消除已经进入TSR结果的历史重影。
4. 本案例为前景原材质制作独立副本，输出 Temporal Responsiveness，施放时复制动态参数并临时替换，结束恢复原材质、覆盖层和模板状态。依赖启动配置 `r.Velocity.TemporalResponsiveness.Supported=1`；原贴图和全局TSR保留。用户确认的是综合效果，不能把所有重影单独归因于TSR，也不能将这套材质强制应用到其他动作。

## 0.5秒蓄势的节奏

- 不要把回拉、蓄住、展开每一段都做独立零速度的 smoothstep；段数越多越容易形成停一下再走的跳帧观感。
- 当前V4使用共享节点速度的五次Hermite曲线，节点加速度连续：0–140ms回拉、140–340ms带微动减速蓄住、340–500ms展开。500ms节点带非零速度进入横扫；只在真正反向的轴上减速到零。
- 武器位置、握柄方向、刃面展示角共用作者时钟，双手以同一握柄为约束，保留抓握和骨段长度。镜头的-720度在运行时驱动，FBX不重复烘焙。
- 业务总时长1.82秒，480Hz资产为874个采样间隔，实际资产约1.820833秒；由运行时阶段映射处理，不用资产尾差改变接触或冷却合同。

## 从实际显示姿态进入

- 切换技能clip之前捕获当前显示的完整骨骼姿态。第零帧保持该姿态，前100ms混合至技能当前采样；这100ms包含在蓄势时间内，不额外增加施放延迟。
- 在骨骼父空间混合，保持骨段长度。武器骨与双臂共用权重；按两只手相对共同武器骨的握持变换求目标，之后两骨IK修正肩肘，必要时锁骨小幅协助。
- 手指与辅助骨保留局部关系。武器骨及其子骨作为握持目标，不随手臂IK再次搬动，否则会重现双重偏移。
- 本例由剑专用 `URuneSwordMeshComponent` 在骨骼缓冲区发布前完成姿态混合，再更新剑身挂点。混合到期或动作退出释放快照。不要让独立Tween在另一个tick中再次写手/武器变换。

## 源与发布边界

当前作者入口为 `SourceAssets/Whirlwind20260920/ImpactV5`，再接 `RecoverV6`；V6仍读取V5的Blend与动作函数。普通柄与加长柄分别输出FBX及可编辑源。WindupV4保留已接受基线，V2保留背景材质制作，V3保留前景时域材质制作，不能把版本号较旧的有效依赖一起归档。

公开Git包含原创曲线、作者/导入脚本及运行代码；Manny/授权武器资产、Blend/FBX/uasset与密集逐帧姿态留在合法本机备份。仓库源码不等同完整可运行素材包。按用户默认规则，制作与编译不触发额外预览、测试或视觉验收。

## 旋转接触反馈与外展收势（V5/V6）

- 停帧、控制器旋转、扫掠、武器姿态和镜头反馈使用同一暂停后的动作时钟。命中可发生在角度子步中，完成该帧最后一次接触反馈后再发布一次可见骨骼姿态；不要把新手臂与平滑到上一帧的镜头位置混用。
- 命中音与短促身体反应不依赖停帧预算还有没有余额。相邻命中可合并强度，第二圈挥动声按动作相位触发；不增加第二次蓄势或重复伤害脉冲。
- 用户要求旋转结束后外展武器再回待机时，只重做 recover 窗口。旋转结束及之前的轨道从当前运行资产读取，保留技能总时长、通知和接触窗；标准柄与长柄分别保存。V6制作资产写回既有V5运行对象，不能只看文件名认定仍在播放旧后摇。

## 施放时四周黄褐色边框

本例先尝试模糊UV外缘保护；用户截图后进一步定位到后处理默认替代材质。UE的原材质shader不可用时可能回退到 `DefaultPostProcessMaterial`，它绘制边缘着色，且不识别自定义 `Strength` 参数。将参数设为0仍以权重1挂载，不能禁用替代画面。

当前只在强度大于0且对应shader platform的完整shader map就绪时挂载模糊；否则移除该blendable，不阻塞技能等待编译。保留全局引擎材质与其他后处理。源码／材质定位与用户画面验收分别记录，当前边框修订未作游戏视觉验收；细节见 `SourceAssets/Whirlwind20260920/BorderFix/README.md`。
