---
name: ue5-fluid-vfx-workflow
description: 为 FPSGAME 制作和优化 UE5 流体特效，包括枪口烟、火焰与爆燃余烟、水花与涟漪、毒液与毒池、血液及冰系冷雾；贯通 Mantaflow 烘焙、Niagara、材质、SVT 和运行时性能预算。用于同类效果新增、自然度和可见性调整、透明方块修复及全水体接入；技能数值、枪械动作和怪物 AI 仍走对应领域技能。
---

# UE5 流体特效工作流

将真实流体形态、清晰的战斗反馈和有界的运行成本一起制作。适用于 `D:/FPS3D/FPSGAME`，当前引擎为 UE 5.8.2。旧 Godot 工程只作明确需要的历史参考。

## 用户确定的执行规则

- 默认后台制作、必要编译、导入和保存；不为查看效果、连接 MCP 或收尾而打开 UE、游戏或 PIE。
- 用户未明确要求时，不执行测试、检查、审计、预览、截图、验收渲染或性能采集。任务所需的源码／资产读取、流体烘焙、生产贴图输出和必要构建照常完成。修复请求中的针对性排查限于用户指定问题。
- 已运行的编辑器不主动关闭。已加载资产通过现有 `Tools/AssetPipeline/mcp_call_codex.ps1` 批次互斥处理，不另起进程覆盖；必要操作确实只能在编辑器完成时，先说明操作与后台方式不能完成的原因，再按已有授权执行。
- 禁止主动向其他任务／对话发送协调消息，也不以共享留言板或轮询其他对话协调。真实文件冲突、未保存目标资产或 DLL 占用，仅向当前用户说明；独立制作继续完成。
- 保护并行修改和用户已认可效果；不全库清理、回滚、暂存、提交或推送。本技能不授权购买、领取第三方许可或公开分发资源。

这些规则覆盖引用文档中的旧默认验收步骤。执行细节按需读 [后台开发与编辑器条件](../ue5-auto-assistant/references/editor-open-development.md)。

## 开始制作

读取工程 `AGENTS.md`、`WORKFLOW.md` 和本次目标的实际加载引用。先确定触发者、有效资产版本、源文件、作者脚本及已有对象池；旧文档中的路径、粒子数和“当前”字样不优先于实际源码。

用简短任务记录明确：事件与接触面、近／中／远观察距离、主体与装饰层、生命周期、风向／坐标空间、受保护的玩法、需要修改的源与资产。用户已明确的范围不重复询问；“全部水体”要列出实际覆盖的水体种类。

根据任务只读所需参考：

| 任务 | 参考 |
| --- | --- |
| 选择实时／离线路线、烘焙与导入 | [制作管线](references/production-pipeline.md) |
| 枪口烟、火焰、水花、毒液、血液、冷雾的层次与时序 | [效果制作配方](references/effect-recipes.md) |
| 对象池、距离分级、风、碰撞、危险边界 | [运行接入与预算](references/runtime-budget.md) |
| 方块、卡片、过淡、图集串帧、材质异常 | [透明与可见性问题](references/transparency-and-visibility.md) |
| 找到当前源码、作者入口、资产与后台落盘方式 | [FPSGAME 接入地图](references/fpsgame-map.md) |

## 制作流程

1. **选表现路线。** 重复战斗效果默认复用离线 Mantaflow 形态／已有图集，配合 Niagara、ISM、贴花及解析水面波纹。SVT、Geometry Cache、局部 2D／3D 模拟只在体积视差或动态交互确实需要时使用，并定义并发、距离、内存与降级策略。启用插件不等于已替换场景效果。
2. **分层与定时。** 先保证主体轮廓、运动方向和命中瞬间可读，再增加翻卷、液丝、碎滴、贴地扩散、余烟。分开形状、运动速度、大小、密度、发光和消散曲线；“加强”不自动等于加粒子或加灯。沿已认可单项扩展时调整各部位时序，不直接套同一强度。
3. **制作可重建源。** 保存模拟参数、原始缓存／必要导出、通道说明、帧清单、HLSL 和作者入口。材质或 Niagara 修改同步到原始生成器；增量安装用唯一节点标识，重复执行更新本层，不重复叠加、不清空其他人的图表。
4. **完成运行接入。** 使用实际枪口／命中／脚部／水面接触位置，明确世界空间和局部空间。同步运行资产引用、必要的预加载与池初始化，重用时重置种子、时间、风、碰撞平面和水面所属。伤害、弹道、资源、修炼、存档与输入默认保持现有合同。
5. **实现成本边界。** 沿已有共享调度与池分配，定义触发限流、实例上限、寿命、纹理预算和远距简化。危险区域主体与伤害不受装饰预算裁掉；屏幕透明覆盖面积和采样数与粒子数一起考虑。
6. **实际落盘。** 执行本次必要的烘焙／导出、导入、材质／Niagara 编译和指定包保存；涉及原生修改则完成常规构建。只写了作者脚本时应标为待执行，遇到真实阻塞保留产物并列出剩余步骤。
7. **按事实交付。** 简述变化、覆盖对象、预算、已保存资产与构建状态，给出源文件／回执位置。没有用户测试就注明“未测试，观感与实际性能由用户确认”；不能把无界面保存、编译完成或历史用户反馈写成本轮运行验收。

## 与其他领域衔接

- 法术数值、施法动作、状态与修炼遵循 [技能／魔法工作流](../ue5-skill-magic-workflow/SKILL.md)；本技能承接其中流体表现。
- 枪口挂点、开火时序、换枪和瞄具状态遵循 [武器工作流](../ue5-weapon-workflow/SKILL.md)。
- 毒蛆、巫婆攻击、投掷和死亡清理遵循 [怪物工作流](../ue5-monster-workflow/SKILL.md)。
- 风从现有天气接口取；不为一个特效重做天气。性能实现同时遵循 [FPSGAME 性能开发约束](../ue5-performance-packaging/references/fpsgame-performance-development.md)。

## 标准的维护

个人目录 `C:/Users/allan/.codex/skills/ue5-fluid-vfx-workflow/` 与工程 `skills/ue5-fluid-vfx-workflow/` 同步维护。通用规则保留在本文件，项目参数与有日期的案例放在引用文档；新用户规则优先。用户已确认水花方块修复和枪口烟基本效果，不能据此推断 2026-09-24 第二轮五项效果已通过实机验收。
