# 5080 工具入口与后握把案例

以下是 2026-09-13 本机记录，不代表未来服务状态或所有类别均已测试。

## 工具与路线

工程工具：`D:/FPS3D/FPSGAME/Tools/AssetPipeline/Mechanical3D`。历史服务 `http://192.168.3.142:8188`，SSH 别名 `r5080`，远端 ComfyUI 为 `D:/开发文件/ComfyUI`。进行生成时按当前部署读取接口与队列；只整理文档时不探测、不重启远端。

- `workflows/trellis_ss32.api.json`、`trellis_ss64.api.json`、`trellis_ss128.api.json`：结构分辨率候选，128 未证明本例跑通。
- `workflows/trellis_multiview_refiner.api.json`：需已有 raw GLB 与方向一致的视图；不是本次选中版本的生成器，可能改变几何，母版须保留。
- `workflows/pixal_singleview.api.json`：Pixal 单图路线，不能传包装器不支持的多视图输入或混接普通 TRELLIS 多视图采样器。
- `build_workflows.py`、`patch_pixal_compat.py`、`naf_torch_neighborhood.py`：保留的工作流生成和兼容处理入口。先读脚本作用范围，不因使用技能自动执行安装/补丁。

历史部署 README 的“未运行”描述只属于部署阶段；后续执行证据看下述 SourceAssets 案例。不要覆盖旧记录伪造时间线。

## 本次实际采用

`D:/FPS3D/FPSGAME/SourceAssets/PhantomRearGripMultiview_20260913`：

- `reference_prompt.txt`、`three_views.png`：原截图生成的三视图，未见侧为推断设计，不是工程尺寸图。
- `generate.py`：本例裁切为三个 627×836 区域后分别去背景，宽侧对应 front/back，窄侧对应 left。坐标仅适用于这张拼图。
- `seed_91627/workflow.json`、`receipt.json`、`history.json`：TRELLIS.2-4B / Trellis2MeshWithVoxelMultiViewGenerator，1024_cascade、SS64、16/32/24、4K、50 万面目标，关闭填洞和 only-shell。
- `seed_91627/textured_master_00001_.glb`：用户后续明确选择并要求接入的版本；91603 保留为未选候选。
- 本例三视角候选的外轮廓、两侧孔洞与横向握纹相较早期 Pixal 单图候选更规整；下方细杆仍并成较宽支撑，部分棱边偏软。不能据此宣布所有模型多视图都更优。
- `generate.py` 的 fetch 分支还会启动 Blender 渲染。未获预览授权时应拆出仅下载逻辑，不能照搬运行触发默认验收。

## Pixal 对照的兼容经验

`D:/FPS3D/FPSGAME/SourceAssets/PhantomRearGripPixal_20260913/README.md` 保存单图生成成功记录与缺陷。该路线曾修复 MoGe 的 utils3d 版本、native SDPA、DINO/Transformers 接口，以及 NAF 间接依赖 NATTEN 的问题。

“包装器不直接 import NATTEN”不证明间接链路不依赖它。本例使用 NAF 原权重与分块 PyTorch 邻域注意力，low_vram 的 naf_tile_factor=2；未进行数值等价测试。不要把这些特定版本补丁自动应用到其他环境，也不要为重复抽样重新安装整套 Torch/CUDA。

## 装配反馈与修订

- `SourceAssets/PhantomRearGripIntegration20260913`：三枪接入、原厂握把分区和作者入口。最初按整体包围盒拟合，翘尾误作最高连接基准，用户反馈三把枪都有明显间隙。这一方法不是合格的装配标准。
- `SourceAssets/PhantomRearGripSeamFit20260913`：`author_fit.py`、`fit_mount.py`、`import_mounts.py` 记录原厂接面提取、连接头局部形变和接口过渡。握持下半部与底座保留；毫米偏移和 WPN_root 的 0.01 比例抵消仅适用于这些枪。
- 运行引用：`Source/FPSGAME/Weapons/PhantomRearGripVisual.cpp`，修订静态资产位于各枪 `/Game/Weapons/PhantomRearGrip/<family>/ReceiverFit/SM_PhantomRearGrip`。
- 旧路径文件占用导致覆盖失败，改为新路径并保留当前主体材质。新路径导入完成、Editor 编译完成；既有 GameFeatureData 错误使导入进程非零退出，另有部分近零切线提示。未完成游戏或动画测试，也未得到用户对最终接缝修订的确认。

这些资产路径用于继续本例或查阅作者方法，不把枪型专用切分索引、裁切尺寸、变形参数及骨骼假设复制到怪物或建材。

## 共振握把：着色诊断与用户指定 Meshy（2026-09-13）

TRELLIS seed91871 的 raw 为 11,622,012 三角面，纹理母版为 487,083 面。无贴图常量材质仍能看到局部折面；M4 金属没有结构法线贴图，关闭聚合物法线与底色并未消除内框扭曲。重算法线未显著修复起伏，部分本地螺钉修整还恶化了邻近表面。不能把全部缺陷归因于降面，或把增大粗糙度称为修复。Blender 近似金属与 UE Phong 转换不同，本次诊断不是 UE 渲染结论。证据保留在 `SourceAssets/ResonanceGrip20260913/ShadingDiagnosis`，依赖 seed91871 与 Repaired91871。

浅灰参考 TRELLIS seed91903 仍有孔洞/接缝缺陷；Pixal seed91927 宽侧与斜侧对照未成为最终版本，斜侧还出现长裂缝。不能把改变视角或生成器写成必然改善。淘汰输出移到任务 trash，精确路径与散列见仓库 `Docs/AssetArchives/resonance-meshy-20260913.json`。

用户随后指定 Meshy Titanium Tactical Grip FBX。当前入口 `SourceAssets/ResonanceGrip20260913/MeshyIntegration/README.md`：112,466 面，原文件无 UV/材质，保持主体拓扑和自定义法线，新增一致的 UV0/UV1；各枪独立适配并保留现有动画。M4 idle 手套表面用于毫米级刚体微调，AKM/QBZ 使用拟合范围映射，不能称为逐枪全动作接触验证。旧 Repaired91871/Game 仍用于材质区域转移，不能当废案移走。当前运行路径是 `/Game/Weapons/ResonanceGrip20260913/MeshyIntegration/{M4,AKM,QBZ191}/SM_ResonanceGrip`。已制作、导入并完成必要编译，未进行本轮游戏测试；选中资产也不等于所有细节无缺陷。
