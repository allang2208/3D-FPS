# 枪械主体与改造件材质统一

用户于 2026-09-13 指定：每把枪的改造件都要针对该枪的主体材质做统一。这是新枪接入、新配件制作和跨枪复用的默认制作要求；不额外启用未授权的检查、渲染或玩法测试。

用户对 715 指定例外：手电、激光器和全息瞄准镜使用各自聚合物外壳，不使用左轮 Chrome 涂层。当前这三种配件与深色安装座在 `DanWesson715/AccessoryPolymer20260914`，独立雨滴汇总表也在此目录；主体及全景红点仍为 Chrome。保留源 UV0 底色、法线、AO、标记与光学区域；外壳金属度 0、粗糙度 .56。不得在后续统一材质工作中自动改回枪身金属。说明 `Docs/Weapons/dan-wesson715-accessory-polymer-20260914.md`；已制作接入，未实机测试。

## 以当前枪身为基准

- 读取实际运行枪身网格及材质覆盖，确定机匣涂层的 BaseColor、Roughness、Metallic、Normal 和细纹尺度。记录材质/贴图来源及参考区域；目录名相同或材质名含 Metal 不代表已统一。
- 同一配件在 M4、AKM、QBZ191 上可以复用几何，但金属外壳按各自枪身选用专用材质或网格变体。不能把 M4 的材料直接传给 AKM，也不能把所有枪统一染成一种灰黑色。
- 统一覆盖握把金属框架、枪口外壳、弹匣/弹鼓金属壳、瞄具外壳、夹具、底座、螺钉、枪托金属连接件和 LPVO 活动倍率环。按实际材质槽与区域分类，避免只处理安装座。
- 聚合物、橡胶、木材、镜片、分划、发光部分、白色刻字和有意保留的钛色饰件保持各自材质身份；镜筒内壁、枪口内腔保留消光。统一的是相应金属的涂层语言，不将整件配件强制金属化。

## 制作与接入

- 原配件已经正确使用该枪的材质及适配 UV 时保留；有差异才制作专用变体，不能为材料名一致而破坏现有正常效果。
- **原厂同类槽是材质来源，不是统一完成的证明**（2026-09-19 修订）：原件延长且 UV 对应时，可沿用该枪原厂弹匣槽；还需区分实际绑定、贴图采样与表面风格。只有原槽本身已经匹配该枪、原 UV 与材质通道确实对应时，才有依据直接沿用。若仍有色调、粗糙度、金属度或尺度差异，再按本页做枪型专用变体；不能仅凭同名或同路径宣称材质统一。
- **先处理绑定错误，再考虑重做涂层**：2026-09-19 用户反馈三枪扩容弹匣泛白、材质不统一，读取实际资源发现三者均为 `WorldGridMaterial`。旧脚本记录的是计划使用的宿主材质，不是资源实际槽值。UE Python 的结构数组元素按副本处理：`slot = slots[i]` → 修改 `slot.material_interface` → `slots[i] = slot` → `mesh.set_editor_property('static_materials', slots)` → 保存。或使用对应网格的 `set_material` API。只写 `slots[i].material_interface = material` 会丢失修改。导入回执记录实际绑定值，不用目标变量伪装读回；这属于制作/接入结果记录，不自动开启游戏测试。
- **绑定原槽与烘焙机匣图集不同**：整张机匣 BaseColor/ORM 通过盒式投影铺到小弹匣上，会带入零件边界、花斑和错误尺度。原厂同类件 UV 能复用时优先保留；需要新涂层时只使用合适的金属区域或程序化涂层，保留弹匣自身结构法线与非金属身份。此前 `ExtMagUniversal20260917` 的“定稿”描述撤销，不能把旧导入回执当成视觉通过。

- **导入时不要合并材质槽**：把原厂多槽并成一个通用槽会丢掉原厂材质分区，也不符合本页"保持原材质槽及顺序"的要求。配件沿用其可编辑源的槽位命名，由上述绑定步骤逐槽接到宿主材质。
- 禁止把整张枪身 UV 图集直接套给无关模型。优先从机匣选择不含木纹、铭文及零件边界的金属区域制作可平铺贴图，或复用枪身程序化涂层并按配件自己的 UV 烘焙。
- 同时匹配底色、粗糙度、金属反射和磨损尺度，仅调 tint 不算完整统一。沿用结构法线和 AO；新增涂层不要抹平滚花、沟槽、防滑区或刻字。原 metallic 遮罩只有在确认其语义后才能作为区域遮罩。
- 新投射使用独立 UV 通道，记录通道号及物理尺度，保留 UV0、几何、分离法线、安装原点、单位和蒙皮。导入时禁止自动光照 UV 覆盖材质 UV；依赖角点色的遮罩必须随网格正确导入。
- 复制目标枪型材质，保留实例实际生效的纹理、标量、向量和静态开关覆盖；不修改跨枪共享母材质。保持原材质槽及顺序，供隐藏原厂部件、换弹分件和展示系统继续使用。
- 读取实际材质图的转换语义。若现用枪身仍经 FBX Phong 转换，记录 Shininess、sRGB 和 Specular 的真实连接；不要将纹理文件名当作线性 PBR 通道直接接入，或在配件任务中顺带改变枪身母材质。
- 源网格被打开的编辑器占用时，优先保存枪型专用网格变体，保留原短名、槽名与顺序，并同时修改装配引用和打包目录；不为了覆盖源资产终止用户编辑器。活动倍率环也按同样规则处理。
- 修改必须落到当前角色、枪匠展览、未装备实例预览、图标和掉落使用的装配入口。动态附件和倍率环的重建也使用该枪型材料；拆卸恢复原厂材质。
- 在目标资产元数据或配套清单中记录枪型、参考枪身材质、金属区域、保留区域、UV/尺度及当前绑定路径。保留可编辑源、导入入口和修改前备份；不以副本已生成代替完成运行资源绑定。

## 用户要求检查或预览时

按授权范围逐枪逐槽核对材料实际绑定和 PBR 通道，检查瞄具内壁、刻字及非金属区域。需要视觉对比时，在相同光照、曝光和距离下并排查看枪身、握把、枪口、弹匣；不要用更强高光掩盖色差。材料读取、导入读回、实际渲染和玩法回归分别记录，未运行的项目交由用户测试。

当前案例入口：`SourceAssets/WeaponAttachmentFinish20260913` 为 M4/AKM 配件逐槽复核与补齐；`SourceAssets/QBZ191MetalCoat20260913` 为 QBZ191 独立 UV 涂层烘焙。案例参数不作为其他枪型的通用金属数值。

## 保留生成主体时的切线与着色

- 结构法线保留原 UV 和切线基准；独立涂层可使用 UV1，但不能仅把 Normal 纹理改采 UV1 而仍沿用不匹配的 UV0 切线。Blender Normal Map 节点明确指定对应 UV，UE 通道与导入切线保持一致。无 UV 的共振握把本例新增相同 UV0/UV1；这不是所有网格必须复制 UV 的规则。
- 新增连接座的面需要有效 UV、法线和明确材质索引，不采样主体不相关的结构法线。后托本例为转接座使用独立涂层材质，避免主体凹槽被映射到连接面。
- 用户反馈白模好、反光扭曲时，按通用模型技能的受控排查区分真实几何、角点法线、底色、结构法线和反射放大；保留原网格对照，不用更粗糙的材质掩盖几何。Blender 工作材质不能替代 UE 实际材质图的结论。

## 715 缎面金属制作案例（2026-09-14）

- 作者入口 `SourceAssets/DanWesson715MetalFinish20260914`，说明 `Docs/Weapons/dan-wesson715-metal-finish-20260914.md`。715 采用不锈钢分件反射，不照搬 QBZ191 的深色涂层或 M1911 的发蓝钢底色。
- 旧颜色图的阴影不宜以高权重乘入金属反射底色；本例将旧标记混合从 0.86 降至 0.22，并单独制作局部凹腔 AO。源雕刻法线先在高模材质内调整，再向已接入的 UV0 烘焙，使几何倒角保留完整投射。
- 拉丝/车削纹按零件局部米制坐标制作，区分枪身纵向与弹仓周向；微纹主要表现于小幅粗糙度变化，避免以密集且强烈的凹凸制造颗粒。上述强度与反射数值只用于本次 715，不是通用金属标准。
- 浅粗糙度和标记可使用 BC7 降低通道压缩量化，Normal 保留专用压缩和正确绿通道。使用现有 UV0/导入切线，复制已接入骨骼网格后换材质槽，保留动画、蒙皮和非金属区域；同步扩展现有湿润材质映射。
- 本例仅制作、导入和必要构建，未渲染或实机测试；不作为已验收效果引用。

## 715 镜面抛光修订（2026-09-14）

- 用户要求更光滑的镜面后，曾将运行入口改为 `DanWesson715/Mirror20260914`，配件在其 `Attachments` 下；作者源为 `SourceAssets/DanWesson715Mirror20260914`。后续主体表面修订见下方 Detail 案例。
- 枪身/弹仓/钢件粗糙度中心为 0.045/0.035/0.060，金属度 1；去掉拉丝凹凸、粗糙度噪声和旧底色阴影乘色。刻字、凹槽稍增粗糙度，凸边轻微抛光。数值仅为本次镜面要求的制作参数。
- 源刻字法线中心值减去 3×3 高斯邻域均值，分离局部刻字与宽范围渐变；XY 细节以 0.45 强度重建法线后烘焙。几何倒角仍由高模完整投射，不跟随源贴图细节衰减。枪身、弹仓、钢件均为 4K。
- 同步复制四种配件及其七类干/湿材质，替换金属图块并保留光学遮罩、尺寸和挂点。镜面湿润层不沿用缎面粗糙度下限：本例水膜最低 0.018，水滴目标 0.035，新增映射合并到现有天气资源表。
- 本次仅制作、导入、接入和必要构建成功，未做游戏或视觉测试；前一轮雨滴检查不作为新镜面版本的验收记录。

## 715 两次模型修改的回退教训

- Precision 全面重建与 Polish 局部精修均被用户否定，已从运行入口撤回并归档至 `trash/dan-wesson715-rejected-models-20260914`。简要记录及散列清单入口：`Docs/Rejected/dan-wesson715-models-20260914.md`。不要重新把这些废案参数当成升级标准。
- 面数、倒角段数或贴图分辨率增加不能证明视觉改善。用户确认整体形态正确时，优先区分结构法线、角点法线/切线、局部遮罩及表面材质的问题；不要削弱整张结构法线，也不要全枪平均法线。获授权比较时使用同一光照和视角，并以用户接受结果冻结基线。
- 当前保留原形体与完整结构法线，主体使用用户反馈明显改善的 Chrome；聚合物附件按本页开头的 715 例外处理。旧制作目录只有确认不再被现用制作链或引擎资源引用后才作为废案归档。

## 715 保留形态的线条与细节修订（2026-09-14）

- 用户明确认为整体形态没问题，要求参考 QBZ191 处理检视时金属细节混在一起的观感。`DanWesson715/Detail20260914` 是 Mirror 的材质副本，运行几何、UV0、切线、蒙皮和动作不变，当时配件使用 Mirror；后续 Chrome 继续以此网格和完整结构法线为输入。
- 参考 QBZ191 Hero 的结构/表面分离方法：源件高模取回原角点法线，使用未过滤、强度 1 的源结构法线，再向实际运行低模重新烘焙。不要把整图减弱或高通过滤当作保留结构细节的方法。
- 本例细法线差分仅制作局部粗糙度/颜色反差遮罩，不改结构法线。已有倒角完整参与；枪架、护罩、击锤、扳机、锁扣及弹仓端面分别设置抛光程度，不照搬 QBZ191 深色涂层，也不增加全枪颗粒。
- 每对高低模作相同的临时位移后烘焙，以隔离邻件投射；局部 AO 保留在独立通道，不把活动件阴影乘进底色。Frame/Cylinder/Steel 各三张 4K 贴图，BC7 底色/ORM、专用法线压缩及单次绿通道转换。
- 新版的三组干湿映射保存在 Detail 目录的独立 `DA_DW715_WetMaterials`，从 `DanWesson715WeaponAssets::WetMaterialsPath` 在天气初始化合并。Precision 的历史表不参与。现有 Cook 武器目录覆盖此资源。
- 制作与运行路径见 `Docs/Weapons/dan-wesson715-detail-20260914.md`。未运行游戏、渲染或测试，暂不作为视觉验收通过案例。

## 715 官方 Fab Chromium 接入（2026-09-14）

- 用户下载 Epic Automotive Substrate Materials 后，主体和配件曾切到 `DanWesson715/Chrome20260914`，用户反馈效果明显改善。作者入口 `SourceAssets/DanWesson715Chrome20260914`，说明 `Docs/Weapons/dan-wesson715-chrome-20260914.md`。来源为实际 `MI_Chromium → MTP_Metal → M_Metallic`；复制有效继承参数和官方母材质/金属函数，不将自制灰色材料命名为 Fab Chrome。随后尝试的 Polish 已撤回，主体保持 Chrome；配件遵循本页开头的聚合物例外。
- 原预设带砂喷法线、磨损纹理及非几何 UV 设置；适配枪械时用已接入的 UV0/切线与完整结构法线，关闭额外指纹、灰尘、划痕等表面叠加。官方金属 F0/F90 与原生 Substrate Slab 保留，局部 AO 独立接入，不用旧阴影染脏底色。当前粗糙度 Frame/Cylinder/Steel 为 .025/.020/.034，结构差异权重 .35，仅为此版制作值。
- 原生 Substrate 材质的雨滴必须接到实际 Slab 的 Normal/Roughness，不能只改旧 MP_NORMAL/MP_ROUGHNESS 输出。当前 3 组主体和 7 组配件的干湿映射放在本枪独立 `DA_DW715_WetMaterials`，沿用运行时合并与 WeaponWetness 参数。
- 四种配件保留当前尺寸、光心、槽序和区域遮罩；混合材质的金属分支复用官方金属颜色函数和 Chromium 参数，光学与非金属保留已有材质管线。不要将整件透明瞄具直接覆盖为不透明 Chrome。
- 本例在 UE 5.8 的 `MaterialEditingLibrary.cpp` 中发现实例参数 setter 会执行写入但固定返回初始 false；不能把此返回值当成写入失败。读取引擎实际实现后适配作者脚本，不因此追加未授权测试。
- 已制作、保存并切换加载入口；用户随后反馈材质效果明显改善。开发过程未运行游戏、渲染或测试，保留 Detail/Mirror 原资源用于回退。

## 新增面的材质区域遮罩（ASH-12 扩容案例）

- 绑定同一个材质仍可能出现色块：追踪该材质实际读取的顶点颜色层、通道和默认值，再区分 BaseColor、AO、法线与区域遮罩。BMesh 新面须显式复制/初始化每个有着色语义的颜色层，不能依赖新角点默认白色。
- ASH-12 原厂弹匣两个槽的 SurfaceRegions 均为 (0,0,0,1)，新增面却为白色；B 通道误启用枪口内壁深色分区，也影响湿润外表面遮罩。本例按原厂填回零 RGB、alpha 1，并在生成入口修复，导入顶点色使用 REPLACE。其他部件有不同分区，不能全枪清零。
- 修复入口 `SourceAssets/ASH12MagFinish20260919/author.py`；基础 `ASH12ExtendedMagazine20260919/author.py` 同步赋值。已完成定位、导入及必要构建；实机换弹/天气仍由用户测试。

## SVD 缎面升级与接口案例

参考另一把枪升级涂层、同步干湿材质或修复镜筒假性镂空时，读 [SVD 涂层与安装接口](svd-finish-and-interfaces.md)。本例区分已保存的制作结果与用户视觉认可，不把粗糙度参数定为通用标准。
