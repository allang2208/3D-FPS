# 无尽轮回 3D FPS · 设计 DNA（Design.md）

> 本文件是 UI 线唯一的风格事实源（单一真源）。所有 UI 组件、页面、情绪板生成
> 都必须以本文件为基准；改风格 = 改本文件 + 更新 token，禁止逐页打补丁。
> 参考情绪板：`assets/ui/moodboard/`（v1 探索稿 + v2 参考即梦全境封锁风三状态）。

## 1. 设计哲学

- **战术信息密度**：UI 是战场情报，不是装饰。信息按“可读性优先级”分层，
  一屏只保留一个视觉焦点。
- **场景即画布**：UI 与场景色调统一（冷色低饱和），面板半透明不遮视野，
  沉浸优先。
- **克制**：每屏强调色不超过两个；动效只服务于状态变化，不炫技。

## 2. 品牌色（Token 基准）

| Token | 色值 | 用途 |
|---|---|---|
| `bg_scene` | `#0A0E16`（深蓝黑） | 场景/画布底色、全屏遮罩 |
| `panel_bg` | `rgba(10,13,20,0.72)` | 面板底（半透明磨砂） |
| `accent_cyan` | `#4FC3F7` | 科技/战术主强调：发光边框、准星、小地图网格 |
| `accent_gold` | `#FFD84A` | 游戏次强调：六边形徽章、金币、等级、稀有度 |
| `hp_green` | `#7FD26A` | 生命/健康状态 |
| `warn_orange` | `#E0A94F` | 低血量/换弹中/警告 |
| `danger_red` | `#D95B4A` | 危险/受击/死亡 |
| `mp_blue` | `#5A8FE0` | 魔力/资源条 |
| `text_primary` | `#E8E4D8` | 主文本（白偏米） |
| `text_secondary` | `#AAB0BA` | 次级文本/数值 |
| `text_weak` | `#6B7280` | 弱化/占位文本 |

规则：`accent_cyan` 与 `accent_gold` 是仅有的两个跨屏强调色；
红/绿/橙只作为“状态色”出现（血量、警告、友军），禁止当装饰色。

## 3. 字体规范

- 主字体：`Microsoft YaHei`（系统默认，中文优先），数字可开等宽特性保持对齐。
- 字号阶梯（Godot `font_size`）：
  - `12` 辅助/角标
  - `14` 正文/提示
  - `16` HUD 主标签（生命/弹药等）
  - `22` 大数值（弹药计数）
  - `40` 标题/死亡面板
- 文字发光：仅 HUD 主数值允许极淡发光/描边（黑描边 1-2px 保证暗场景可读），
  正文禁用发光。
- 层级规则：数值 > 标签 > 装饰；同屏字号档位 ≤3 档。

## 4. 间距与布局

- 基准网格 4px；HUD 四边距 16-24px；面板内边距 8-12px；元素间距 6-10px。
- HUD 四角环绕布局（FPS 惯例，中心视野禁区 ≈ 画布 15%）：
  - 左上：生命/等级/经验（状态）
  - 中上：波次/击杀（轻量）
  - 右上：金币/资源条
  - 左下：状态列表/队友/背包（重信息）
  - 右下：弹药 + 技能网格
  - 底部中央：武器栏
- 三态布局模板：菜单（左右面板 + 底栏）/ 战斗（四角 + 准星）/ 探索（左上小地图 +
  左侧垂直状态条 + 右下道具栏），对照 `moodboard_v2_*.jpg`。

## 5. 面板与卡片

- 半透明磨砂玻璃：`panel_bg` + 圆角 6px + 1px `accent_cyan` 发光描边 +
  顶部 1px 高光（玻璃感）。
- 层次区分：核心面板透明度略低（alpha ~0.8），次要面板 alpha ~0.55；
  发光强度随信息优先级递增。
- 禁止实心纯色大面板；禁止同一面板内混用多种描边色。

## 6. 图标规范

- 风格：扁平几何（六边形/圆形/菱形/箭头/电池），统一发光光晕（冷色）。
- 技能图标：六边形 + 金边徽章 + 底部浮雕宝石底座（既有体系，延续）。
- 状态图标：高饱和单色（红=警告/绿=状态/蓝=科技），无渐变拟物。
- 禁止：写实 3D 图标与扁平图标混用；同屏两种以上描边粗细。

## 7. 动效原则

- 时长 150-250ms，`ease-out`；仅状态变化触发。
- 受击红闪 180ms、命中标记 120ms、换弹提示 1.5s（现状保留）。
- 面板进出：200ms fade + 8px slide；禁止无意义弹跳/旋转。

## 8. 一致性检查清单（提交前必过）

- [ ] 一屏视觉焦点 ≤1，数值/标签/装饰对比度阶梯正确
- [ ] 每屏强调色 ≤2（青蓝 + 金），状态色只表状态
- [ ] 所有颜色/字号/间距来自 Token，无硬编码
- [ ] 同一含义复用同一组件（所有按钮/血条/面板同源）
- [ ] 新页面先对照三态模板与已有页面，不改出“第 2 套风格”
- [ ] 改样式只改 Token/组件层，未逐页补丁
- [ ] 无头测试通过（`tests/test_status_bar.gd` 等）
