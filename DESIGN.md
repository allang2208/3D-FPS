> 字体补充：原 CSS 优先 Microsoft YaHei UI（TTC face 1）；背包物品名按用户指定使用 SimHei（黑体）12px，堆叠数量与容量统一 Consolas，“背包”标题16px。物品名是用户明确指定的字体例外。右侧栏目使用原项目八张 PNG，68px 图、74px 按钮、25px 间距；详见 docs/backpack-reference.md。

> 2026-09-05 最新背包截图优先：背包装备页按用户提供的白色装备底板、白色空槽、深灰已装备槽和深灰背包格还原；具体值见 `ui/backpack_reference_style.gd`，布局证据见 `docs/backpack-reference.md`。这组局部规则覆盖上一版统一深灰槽，不改变其他面板的冷钢主题。

# 当前生效：冷钢档案（2026-09-05）

本项目当前主题为 `cold_steel`。本节取代下方历史金色/灰白设计约定。
源规范：原 game-dev 的 docs/ui-cold-steel-design-system.md；源色值：ui/panel-theme-backpack.css。
Godot 唯一颜色入口 ui/palette.json，字体/尺寸入口 ui/style-config.json，组件统一消费 ui/style.gd。
炭黑外壳、冷灰控件、银白信息；普通强调为银灰，金色仅用于金币/升级等游戏语义。
字号 24/20/16/14/12/11，单面板最多四档；微软雅黑、Consolas 数字。
右侧背包占屏宽45%、全高，0.25秒水平滑入；圆角10、格子8、滚动条8。
按钮有默认、hover、pressed、disabled、focus；禁止持续弹跳/旋转。
背包操作保留数量及实例属性，取消/失败不消耗；保存包括背包、装备、仓库、快捷栏和加工暂存。

---
以下保留为历史参考，不再作为当前主题验收基准。

# 无尽轮回 3D FPS · 设计 DNA（Design.md）

> 本文件是 UI 线唯一的风格事实源（单一真源）。所有 UI 组件、页面、情绪板生成
> 都必须以本文件为基准；改风格 = 改本文件 + 更新 Token，禁止逐页打补丁。
> 定稿基准：`assets/ui/moodboard/ref_division_ui_improvement.png`
> （《全境封锁 1+2》UI 改进情绪板：金主色 + 白信息 + 深灰底）。
> 布局模板：`assets/ui/moodboard/moodboard_v2_*.jpg`（菜单/战斗/探索三状态）。

## 1. 设计哲学

- **战术信息密度**：UI 是战场情报，不是装饰。信息按“可读性优先级”分层，
  一屏只保留一个视觉焦点。
- **金与白、深灰底**：深灰承载信息层级，金色只用于强调与操作反馈，
  白色用于主信息文本；场景保持低饱和，UI 发光仅限关键元素。
- **克制**：每屏强调色不超过两个；动效只服务于状态变化，不炫技。

## 2. 品牌色（Token 基准，定稿）

| Token | 色值 | 用途 |
|---|---|---|
| `THEME_BG` | `#0F0F10`（深灰黑） | 场景/画布底、面板底、遮罩 |
| `THEME_GOLD` | `#D4AF37` | 核心强调：进度条、等级/评分、图标、按钮悬停 |
| `THEME_WHITE` | `#FFFFFF` | 主信息文本 |
| `THEME_GRAY_LIGHT` | `#B5B5B5` | 次级文本、禁用态文字 |
| `THEME_GRAY_MID` | `#3A3A3C` | 面板边框、分隔线、按钮默认底、禁用底 |
| `hp_green` | `#7FD26A` | 生命/健康状态（状态专用） |
| `warn_orange` | `#E0A94F` | 低血量/换弹中/警告（状态专用） |
| `danger_red` | `#D95B4A` | 危险/受击/死亡（状态专用） |
| `mp_blue` | `#5A8FE0` | 魔力/资源条（状态专用） |

规则：金色是唯一跨屏强调色；白/灰负责层级；红/绿/橙/蓝只作为状态色出现，
禁止当装饰色。旧 2D 暗金棕（`COLOR_*`）为历史色板，逐步废弃。

## 3. 字体规范（黑体 SimHei，对齐原项目 game-style.css）

- 主字体：**黑体 SimHei**（原项目 `font-family: SimHei, "Microsoft YaHei", "黑体"`；Godot 加载 `assets/ui/fonts/simhei.ttf`）。
- 字重阶梯：标题/加粗用微软雅黑 Bold（黑体无粗体文件），正文用黑体；等宽数字用 Consolas（原项目 monospace）。
- 换字体 = 改 `ui/style.gd` 的 `make_font`（唯一字体入口），组件零改动。
- 字号阶梯（映射到游戏 HUD）：
  - `48`（H1）→ 封面/大标题；游戏内死亡面板用 `40`
  - `32`（H2）→ 面板标题
  - `22` → 大数值（弹药计数）
  - `16` → HUD 主标签（生命/波次/击杀）
  - `14` → 正文/提示
  - `12` → 辅助/角标/Caption
- 文字发光：仅 HUD 主数值允许极淡发光/描边；正文禁用。
- 层级规则：数值 > 标签 > 装饰；同屏字号档位 ≤3 档。

## 4. 间距与布局

- 基准网格 4px；HUD 四边距 16-24px；面板内边距 8-12px；元素间距 6-10px。
- HUD 四角环绕布局（FPS 惯例，中心视野禁区 ≈ 画布 15%）：
  - 左上：生命/等级/经验；中上：波次/击杀；右上：金币/资源条
  - 左下：状态列表/队友/背包；右下：弹药 + 技能网格；底部中央：武器栏
- 三态布局模板（菜单/战斗/探索）对照 `moodboard_v2_*.jpg`。

## 5. 面板、按钮、进度条、分隔线

- **面板**：玻璃质感（半透明深灰底 `THEME_BG` alpha 0.55-0.8）+ 细线分割
  （`THEME_GRAY_MID` 1px）+ 金色强调（关键信息行/进度条）+ 圆角 6-8px。
- **按钮三态**：
  - 默认：`THEME_GRAY_MID` 底 + `THEME_WHITE` 字，圆角 4-6px
  - 悬停：`THEME_GOLD` 底 + `THEME_WHITE` 字
  - 禁用：`THEME_GRAY_MID` 底 + `THEME_GRAY_LIGHT` 字
- **进度条**：深灰底 + `THEME_GOLD` 填充（状态条除外，状态条用状态色）。
- **分隔线**：1px `THEME_GRAY_MID`；强调分隔线用 `THEME_GOLD`（alpha 0.6）。

## 6. 图标规范

- 风格：极简科技线条（盾=防御/指南针=导航/鹰=组织）+ 统一发光光晕（金色系）。
- 技能图标：六边形 + 金边徽章 + 底部浮雕宝石底座（既有体系，延续）。
- 状态图标：高饱和单色（红/绿/橙/蓝=状态语义），无渐变拟物。
- 禁止：写实 3D 图标与线条图标混用；同屏两种以上描边粗细。

## 7. 动效原则

- 时长 150-250ms，`ease-out`；仅状态变化触发。
- 受击红闪 180ms、命中标记 120ms、换弹提示 1.5s（现状保留）。
- 面板进出：200ms fade + 8px slide；按钮悬停 120ms 变色。

## 8. 一致性检查清单（提交前必过）

> 换肤预设（`ui/style-config.json` 的 `active_theme`）：
> `dark_gold`（默认深灰金）→ `gold_white_gray`（金白深灰）→ `gray_white`（灰白毛玻璃）。
> `gray_white`：浅色玻璃面板（`COLOR_PANEL_BG` 亮底）+ 深色文字 + 中性灰边框，
> 状态色（红/绿/橙/蓝）与稀有度徽章语义保留；装备槽位不放图标，仅文字。

- [ ] 一屏视觉焦点 ≤1，数值/标签/装饰对比度阶梯正确
- [ ] 强调色只有金色；白/灰管层级；状态色只表状态
- [ ] 所有颜色/字号/字重/间距来自 Token，无硬编码
- [ ] 同一含义复用同一组件（按钮/血条/面板/进度条同源）
- [ ] 按钮三态（默认/悬停/禁用）全部实现，禁用态不悬停
- [ ] 新页面先对照三态模板与已有页面，不改出“第 2 套风格”
- [ ] 改样式只改 Token/组件层，未逐页补丁
- [ ] 无头测试通过（`test_ui_tokens.gd`、`test_status_bar.gd` 等）
