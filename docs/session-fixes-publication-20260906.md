# 本次修复发布边界

从 origin/main 8e15114 隔离整理，未合并共享 master 的 25 个未发布提交。

本批运行代码：玩家及动态子节点禁止投影、子弹禁止投影、游戏时钟显式整数小时。附阴影设置回归检查及枪械技能经验。

本地已实现、未随本批发布：五枪图鉴/图标/音频/改造及相关数值、HK416 消音器和制退器接入、真实 loading、动态腰射半径、祭坛空对象修复，以及依赖本地新面板的警告清理。远端缺少这些模块及其依赖，不能通过覆盖共享脚本安全发布；需要按完整模块及资源依赖单独整理。本记录不将本地验证称为远端功能完成。

保留最终预览、可编辑资产和可复现测试；仅清理当前会话 TEMP 目录中的已被正式测试替代的诊断脚本。缓存修复不会作为二进制 .godot 缓存提交。

验证：隔离存档下阴影回归通过，主场景无头 180 帧退出码 0、无脚本错误。首次编辑器导入存在远端既有资源缺口（旧 AKM.mtl、部分 Polyhaven 和树木源贴图）及退出清理告警；这些文件未在本批修改，不能将导入退出码 0 称为全资源无错误。

## 第二批：真实场景加载

继续从 a9c6988 整理。将本地已验证的加载生命周期移植到远端原有界面，不引入本地背景图和新面板依赖：覆盖层移到根节点避免旧场景销毁；资源加载占 75%，随后等待 scene_changed、主场景 HUD 桥接完成及实际渲染，再显示 100% 并恢复原暂停状态。重复加载、重复实例与加载失败均有明确出口。

主场景 readiness 在 NPC 面板和武器修正初始化之后置位。天气雨势插值仅重命名局部变量以消除遮蔽警告，数值不变。

验证：独立存档的延迟初始化场景连续切换两次，在无头和 D3D12 模式均通过；实际 main.tscn 加载完成及暂停恢复检查通过。第一批清单中的真实 loading 逻辑现已在本批补齐，其他五枪集成与新面板依赖仍未发布；原素材背景外观不在本批。
