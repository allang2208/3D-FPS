# 旷野性能优化：暂停点与下次接续

2026-09-08，按用户要求结束本轮测试，不再展开新优化。

## 本次发布边界

以远端 `origin/main` 的 `8aadbde` 为基线建立独立发布工作区。正式接入只包含 `ui/backpack_hud.gd` 的隐藏刷新优化、对应独立测试和技能/报告文档。

`local-experiment.zip` 保存本轮独立编写的性能检测模块、F10 面板、持续刷怪样板、复现脚本、日志和最终预览；详见 manifest.json。它是 **本地实验交接源码，不是可直接安装的插件**。本地 `E:/3d/3-dfps` 中的诊断接线和战斗实验仍可继续使用；远端尚未完整迁入它们所需的枪械、天气、地形与部分怪物接口。本轮未复制混合修改的枪械、HUD、怪物文件，也未整批推送本地额外历史。

压缩包中的 historical/trace_stalls.py 是已执行的历史改写记录，不幂等，不应直接重复运行。只有确实需要迁移时才逐项对照当前接口。日志保留原测试警告和未归因长帧，不伪装为无错误基线。

## 已完成与剩余

- 换弹扣备弹触发隐藏背包全量刷新已修复。真实雨天持续刷怪中，换弹结算平均 5.828 ms 降至 0.171 ms；不降低画质。详细报告在 ../../wilderness-stall-fix-20260908.md。
- 常驻快捷栏保持实时，打开面板前同步，关闭动效及中途重开正常。实际扣弹与本地跨进程弹药存档恢复均已验证。
- 186.089 ms 原始偶发停顿尚未归因；复测首次开火附近出现约 255 ms 长帧，不能直接判为同一个原因。稳定 140 FPS 尚未验收。
- 已加入 details_ms、刷怪计时、怪物数量、实际子场景上下文和 pipeline_compilations。嵌套计时不计入 script_ms 两次。

## 下次从这里开始

1. 阅读 ../../wilderness-stall-fix-20260908.md 和 ../../../skills/godot-terrain-vegetation/references/combat-performance.md，核对实际项目/远端差异，保留并行修改。
2. 使用本地 `tests/test_combat_stalls.gd` 与独立存档。固定天气、分辨率、种子与视角；记录是否有并行测试/编辑器干扰，未经授权不关闭其他进程。
3. 围绕首次开火、首次弹着特效、资源准备与管线编译做单变量 A/B；将采样启用本身与首次效果触发分开。比较峰值发生前后的计数，不把累计计数直接当根因。
4. 再复现用户第 25 秒附近的 186 ms 事件；若未复现，明确保留未知，不以平均 FPS 掩盖问题。跨区移动和正式刷怪持久化仍未完整验收。

## 原始报告

均位于本机 Godot `user://performance_reports/`（无尽轮回 3D FPS）：

- 用户：performance-2026-09-08T17-43-36-56264-422437.json
- 修复前：performance-2026-09-08T17-52-34-61320-103044.json
- 修复后：performance-2026-09-08T18-00-04-58440-102893.json
- 最终复测：performance-2026-09-08T18-04-19-62472-103090.json

一次性改写脚本及过时的暂存副本已移至本地 `trash/wilderness-performance-20260908/`，原路径与去向见归档清单。有效测试、最终预览和日志保留。

远端回归使用已发布的 iron_ore 物品触发同一 take_items→changed 刷新链（8 项）；本地完整弹药与嵌套计时回归保留在交接包（10 项）。未为测试搬入未发布的弹药系统。

## 独立发布验证（2026-09-08）

- 全项目导入执行完毕；既有 AKM.mtl 缺失、空 glTF 和退出 RID 警告单独记录，未扩展修复。
- 本次可见性回归 8 项 PASS；180 帧无头冒烟退出 0，无 SCRIPT ERROR。
- 原 test_combat.gd 的移动、命中、玩家受伤、特效清理和 ADS 检查均通过，退出 0；退出时仍报告 1 个资源未释放。
- 原 test_reload.gd 自动与手动换弹均 ok=true，退出 0。
- 交接包 39 个条目，CRC 与清单校验通过；无新增第三方资产包，仅保留本任务源码、报告和既有游戏的测试截图。
