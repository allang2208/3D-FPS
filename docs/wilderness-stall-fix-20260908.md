# 旷野实战停顿排查：2026-09-08

## 已确认并修复

真实换弹结束调用 backpack.take_items，changed 信号使关闭的背包重新刷新全部空间格和装备卡。独立 CPU 测量完整刷新平均 8.306 ms；其中装备卡约 5.092 ms，格子约 2.358 ms。

关闭且已结束退场动画的背包现在仅更新常驻快捷栏、标记卡片脏状态；下次打开时在首个可见帧之前同步。打开和关闭动画期间仍立即刷新。未改变弹药扣除、存档、武器动画、伤害、怪物或旷野画质。

相同独立测量的关闭刷新降至 0.0084 ms。雨天 1440p 连续补怪、真实射击与自动换弹，修复前 7 次换弹结算平均 5.828 ms / 最大 5.999 ms；修复后 7 次平均 0.183 ms / 最大 0.219 ms。开火平均 0.850→0.854 ms；存档写入平均 1.333→1.373 ms。可确认换弹结算约节省 5.65 ms。

## 数据与限制

- 用户报告：performance-2026-09-08T17-43-36-56264-422437.json。排除面板约 142 FPS，但有 186.089 ms 大停顿；尚未证明其根因。
- 修复前：performance-2026-09-08T17-52-34-61320-103044.json，60 秒，240 发，30 击杀，60 累计生成；128.45 FPS，P99 10.666 ms，最大 21.607 ms。
- 首次修复后：performance-2026-09-08T18-00-04-58440-102893.json，60 秒，240 发，32 击杀，62 累计生成；117.80 FPS，P99 15.611 ms，最大 546.355 ms。前两帧为 250.078 / 546.355 ms，已测脚本分别 2.201 / 11.289 ms。不能把这组大峰值归因于换弹。
- 测试期间弹着特效被其他工作更新，并出现其他无头测试进程；自动瞄准和随机怪物也使视角不同。因此上述整体 FPS 不是严格的 A/B，不据此宣称整体提升或下降。
- 本轮实弹测试在导出之后才截图，截图读回不计入报告。

## 定位能力

F10 JSON 增加 details_ms：开火细项、换弹结算、存档写入；独立采集周边刷怪回调，并记录存活、目标、累计生成和回收数。嵌套耗时不会重复加进 script_ms。

采样补入五类累计 pipeline_compilations。战斗样板包裹的真实旷野子场景现在也导出流送、草地、天气等上下文。编译计数只提供相关性线索，不能单凭计数断言尖峰根因。

## 回归

- test_inventory_refresh_visibility.gd：10 项 PASS，涵盖关闭跳过卡片、常驻快捷栏实时值、打开同步、关闭动画与中途重开、实际扣弹、嵌套计时不重复和每帧清空。
- test_caliber_ammunition.gd：三口径真实 HUD 绑定、普通/不足/空备弹换弹、卸弹匣返还及存档读回 PASS。

日志、实际截图：tools/stall-study-20260908/。大尖峰仍需继续定位，本轮不宣称稳定 140 FPS 验收通过。

## 最终复测

报告 `performance-2026-09-08T18-04-19-62472-103090.json`：60 秒、240 发、31 击杀、累计生成 61 只；7 次换弹平均 0.171 ms、最大 0.181 ms，再次确认修复。平均 112.64 FPS、P99 15.396 ms，首次开火附近仍出现 254.977 ms 尖峰，其后最大 22.516 ms。不能把首次大停顿与用户第 25 秒的 186 ms 断言为同一根因。五类管线编译计数及天气/怪物/地形上下文已实际出现在导出中。未发现 SCRIPT ERROR / ERROR，原有 Terrain3D 弃用与树桩 UID 回退警告保留。

`test_weapon_inventory_persistence.gd` 分别写入、重启读取，验证两把同型号武器独立弹仓和共同备弹池，以及场景切换后恢复。实际战斗截图已检查，30 只存活与雨天草地保留。后续优先做首次开火/弹着资源预热的隔离 A/B，并排除并行编辑器/测试进程干扰，再判断原始偶发长停顿。
