# 面板与栏目工作流

2026-09-13 起，FPSGAME 新建或改造面板、页签、栏目、卡片和弹窗均使用本流程。当前宿主为 `D:/FPS3D/FPSGAME`，实现使用 UE5 UMG／Slate。

先按现行规则规划，再制作和接入。用户已要求实现时，完成必要规划后继续执行；只有用户限定“方案／候选，确定后再接入”时才停在该阶段。默认不启动游戏、不运行测试、不生成截图；用户明确要求的预览、检查和测试按指定范围执行。

## 1. 入口与依据

依次读取本文件、[冷钢 UI 正式规则](Docs/UI/ui-cold-steel-design-system.md)、[UE UI 技能](skills/ue5-ui-umg-slate/SKILL.md)，再定位最接近的现有面板及其数据模型。数值、颜色、字体以正式规则和 `ColdSteelUIStyle` 为单一来源；新要求与旧条款冲突时，更新正式规则的对应条款，不并存两套规范。

| 任务 | 优先复用的结构与实现入口 |
| --- | --- |
| 改造、强化、附魔或类似工作台 | `M4GunsmithLayout.cpp`、`M4GunsmithOverview.cpp`、`ColdSteelEnhancementLayout.cpp`：左目录、中央真实预览／项目、右详细汇总、固定底部操作 |
| 背包、装备、仓库栏目 | `ColdSteelHUDWidget`、`ColdSteelInventoryWidget`、`ColdSteelInventoryPresentation.cpp`：共享抽屉、空间网格、绘制与命中几何 |
| 状态、属性或详情栏目 | `ColdSteelHUDWidget`、`ColdSteelStatusModel`：身份摘要、分区卡片、名称与实值、现有详情入口 |
| 图标、字体与基础控件 | `ColdSteelUIStyle`；`GunsmithUIStyle` 仅为同源兼容入口；动态图标沿用 `UColdSteelWeaponIcons`（`ColdSteelWeaponIcons.h/.cpp`） |

以上为定位入口，实施前按当前文件确认职责。历史实现与验收记录位于 [UI 索引](Docs/UI/README.md)，不能代替最新规则或本次测试。

## 2. 先形成可实施的规划

使用 [面板／栏目规划模板](Docs/UI/panel-column-plan-template.md)，在 `Docs/UI/<topic>-plan-YYYYMMDD.md` 记录。只有一个小栏目时保留必要字段即可，不为每张卡片建立独立文档。

1. **目的与范围**：入口、玩家要完成的动作、此次交付阶段、保留的原有内容。工作台保留原枪械库背景及右侧详细信息汇总。
2. **信息结构**：面板→页签→栏目→卡片／数据行，每层的名称、顺序、主次与可见条件。分区结合标题、内容与留白设计。
3. **布局**：宽屏、紧凑、窄窗／矮窗下的位置和滚动归属；固定标题、页签、主要操作与页脚。参考实际内容区重排，不整体缩小固定画布。
4. **视觉角色**：复用的主题值、字号档位、图标来源、按钮层级；工作台与抽屉分别使用正式规则中的玻璃参数。内部卡片共享所属区域模糊。
5. **数据合同**：每个字段的真实来源、单位、统计范围、过滤、排序与更新事件；每个动作的业务入口、消耗来源、提交和保存路径。
6. **状态与输入**：空列表、未选择、已选择、不可兼容、资源不足、数据改变、图标等待／失败；鼠标、键盘、焦点、滚动、关闭和返回行为。
7. **文件与资源范围**：本次修改路径、复用资源、新增资源、确认退役的文件；涉及容量／占格／存档结构变化时单列迁移规则。

预览图和生成图只有用户要求时制作。图像中的示例数据、装饰、配装存档、热点等不自动成为新增玩法。

## 3. 统一外壳与栏目

- 大背景为中性黑灰，文字与选中强调为银白；生命、危险、收益、代价和稀有度沿用语义色。降低透明度通过背景 Tint 提高遮挡，文字和图标保持清晰。
- 中文／普通 UI 使用 Noto Sans SC，纯数字使用 JetBrains Mono；从工程字体资源加载。六档字号及 Slate 点数／像素换算遵守正式规则，普通面板通常使用 20／16／14／12px。
- 字体与按钮从当前公共样式入口取得；`ColdSteelUI::TextFont/NumberFont` 点数入口和 `GunsmithUI` 像素入口各自保持调用合同，不重复换算，不依赖其他任务尚未保留的新增接口。
- 通过分区标题、细边、明度、留白和对齐区分栏目。名称与数值分列；长文本换行或给出全文入口，不靠缩小正文解决截断。
- 工作台左目录和右汇总遵守现有响应布局；数值表固定表头、表体滚动。仓库继续从左侧动画弹出，与右侧背包装备抽屉等大，窗口变窄时一起适配。
- 同一行平级按钮等宽、等高、等间隔，文字相对按钮整体居中；下拉箭头占位需要对侧补偿。固定操作区不能被内容挤出窗口。
- 新增栏目先复用主题和组件。用户改变视觉规则时修改公共规范；业务专属内容放在相应布局／展示文件中。

## 4. 数据、状态与交互共同设计

列表、默认选择、数量、预览、确认和提交必须使用同一业务范围；数量变化时同步清除失效选择。不能只过滤显示，却继续从其他位置报价或扣除。

| 现有业务 | 必须保留的合同 |
| --- | --- |
| 附魔卷轴 | 只计背包中数量大于零的附魔卷轴；未持有或仅在仓库直接隐藏。已持有但不兼容时禁用并说明原因；空列表显示“背包中暂无附魔卷轴”。默认选择、报价和扣除同样只使用背包 |
| 强化／附魔其他消耗 | 魔法粉尘、强化石、金币继续按现有背包＋仓库规则计算，不因卷轴过滤而一起缩小范围；不足时可预览、不可确认 |
| 仓库“存入同类” | 按操作开始时仓库已有 Definition 匹配背包物品；排除武器及所有装备，保留堆叠、容量约束和保存事务 |
| 背包／仓库空间网格 | 复用物品占格、命中、拖放、堆叠、拆分与整理；渲染坐标和输入坐标来自同一布局，拖动快照与最终 Generation 校验各司其职 |
| 属性与对比表 | 使用真实值和公式；缺失数值显示“—”。收益绿、代价红、不变灰；开镜耗时、换弹耗时、后坐等“越低越好”的指标反向判断，不能机械地增加标绿 |

新增业务自己定义显示／隐藏／禁用条件；不把“未持有卷轴隐藏”套用到所有装备目录、空装备槽或功能入口。升级外观不修改物品容量、可用部件数量、战斗公式和存档字段。

## 5. 实现顺序与复用经验

1. 先接入共享样式与容器，完成宽／窄布局和滚动归属，再接入卡片内容与真实数据。
2. UMG 宿主管理 Slate 生命周期，模型／系统管理报价、提案、提交和保存。按钮调用现有业务入口，展示控件不另写扣除与存档逻辑。
3. Construct 注册事件，Destruct 解绑并释放预览、定时器和临时引用。业务更新由现有 OnChanged 等事件驱动；Tick 仅处理必要几何档位或已有实时捕获。
4. 自绘物品控件在数据／画刷变化后使 Paint 缓存失效。动态图标等待或失败时显示有效目录图；完成事件只刷新图片，避免重建正在操作的选择按钮、丢焦点或滚动位置。
5. 一个图标任务的材质错误或等待超时必须结束该任务并继续队列；复用当前子系统的失败通知与回退，不为每个面板另建捕获队列。
6. 复用打开／关闭输入入口，明确 UIOnly／GameOnly、移动视角锁定、焦点返回、拖动取消与菜单关闭顺序；关闭输入不得穿透成世界攻击。新增弹层按实际视口限位。
7. 需要原生构建时完成对应构建并说明模块加载方式；构建成功只代表编译结果，不代表实际画面、操作、保存或打包已测试。

## 6. 资源与废案管理

图标风格沿用已采用的银灰拉丝面、石墨凹槽、细倒角和左上柔光；图中不烘焙文字、数量、选中框或禁用文案。分类图标、物品图标和真实武器预览分别承担类别辨识、物品辨识与当前组合展示。

已有枪械库背景、九类部件图标及其生成记录是有效来源；即使路径含 `Candidates` 也不能当作废案。RGB 源图经 UE 材质去黑不等于获得 RGBA 源图；工具未提供模型选择时，不声称指定了 image2.5。

确定不再采用且属于本次范围的方案、图片和过期文件移到 `trash/<topic>-YYYYMMDD/`。先确认替代物及当前引用，验证源／目标绝对路径在项目范围内；记录原路径、目标、字节数、SHA-256、原因、替代物，移动后读回散列并更新入口链接。有效源图、许可、生成参数、恢复工具、历史交付记录和回滚证据继续保留，不按文件年龄批量清理。

`trash` 不进 Git，公开的文字归档清单放 `Docs/UI/`。本轮示例见 [2026-09-13 归档记录](Docs/UI/panel-workflow-archive-20260913.md)。资源发布与恢复遵守 [AssetSetup](Docs/AssetSetup.md)。

## 7. 交付与发布

交付说明列出已完成规划／代码／资源／必要构建、实际执行的用户请求预览或测试、尚未测试的内容。用户只要求开发时，不追加验收动作；历史案例中的通过数字不覆盖本次修改。

需要发布时按 [WORKFLOW 第 8 节](WORKFLOW.md#8-仓库整理与推送) 处理：确认目标、fetch、审阅所有待推提交、精确暂存、检查发布差异与来源、普通推送并回读远端 SHA。保留并行修改与已有暂存内容；不把仓库整理当作提交全部工作区的授权。

将可复用经验写入 `skills/ue5-ui-umg-slate/references/`，同步个人技能与工程镜像；数值规范只在正式设计规则维护，案例只记录对应日期的实现和结果。
